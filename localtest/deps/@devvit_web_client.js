import { n as __require, r as __toESM, t as __commonJSMin } from "./rolldown-runtime-DEy6nfl2.js";
//#region crystal-cascade/node_modules/@devvit/protos/json/devvit/ui/effects/v1alpha/effect.js
/**
* #effect.ts
*
* Code generated by ts-proto. DO NOT EDIT.
* @packageDocumentation
*/
var EffectType;
(function(EffectType) {
	/**
	* EFFECT_REALTIME_SUB - Notify client to synchronize its realtime subscriptions
	*
	* @deprecated
	*/
	EffectType[EffectType["EFFECT_REALTIME_SUB"] = 0] = "EFFECT_REALTIME_SUB";
	/**
	* EFFECT_RERENDER_UI - Trigger a re-render for apps that have visible UI
	*
	* @deprecated
	*/
	EffectType[EffectType["EFFECT_RERENDER_UI"] = 1] = "EFFECT_RERENDER_UI";
	/**
	* EFFECT_RELOAD_PART - Notify the client that parts of a subreddit, post, or comment should be reloaded
	*
	* @deprecated
	*/
	EffectType[EffectType["EFFECT_RELOAD_PART"] = 2] = "EFFECT_RELOAD_PART";
	/** EFFECT_SHOW_FORM - Display a user input form */
	EffectType[EffectType["EFFECT_SHOW_FORM"] = 3] = "EFFECT_SHOW_FORM";
	/** EFFECT_SHOW_TOAST - Display a transient toast message */
	EffectType[EffectType["EFFECT_SHOW_TOAST"] = 4] = "EFFECT_SHOW_TOAST";
	/** EFFECT_NAVIGATE_TO_URL - Notify the client to navigate to a URL */
	EffectType[EffectType["EFFECT_NAVIGATE_TO_URL"] = 5] = "EFFECT_NAVIGATE_TO_URL";
	/**
	* EFFECT_SET_INTERVALS - This updates the list of active timers.
	*
	* @deprecated
	*/
	EffectType[EffectType["EFFECT_SET_INTERVALS"] = 7] = "EFFECT_SET_INTERVALS";
	/**
	* EFFECT_CREATE_ORDER - This starts a purchase flow
	*
	* @deprecated
	*/
	EffectType[EffectType["EFFECT_CREATE_ORDER"] = 8] = "EFFECT_CREATE_ORDER";
	/**
	* EFFECT_WEB_VIEW - Control and communicate with WebViews
	*
	* @deprecated
	*/
	EffectType[EffectType["EFFECT_WEB_VIEW"] = 9] = "EFFECT_WEB_VIEW";
	/**
	* EFFECT_CAN_RUN_AS_USER - Check if the app can execute some actions as the user
	*
	* @deprecated
	*/
	EffectType[EffectType["EFFECT_CAN_RUN_AS_USER"] = 11] = "EFFECT_CAN_RUN_AS_USER";
	/**
	* EFFECT_TELEMETRY - Statistical data event for analytics.
	*
	* @deprecated
	*/
	EffectType[EffectType["EFFECT_TELEMETRY"] = 12] = "EFFECT_TELEMETRY";
	/**
	* EFFECT_UPDATE_REQUEST_CONTEXT - Request an updated signed request context
	*
	* @deprecated
	*/
	EffectType[EffectType["EFFECT_UPDATE_REQUEST_CONTEXT"] = 13] = "EFFECT_UPDATE_REQUEST_CONTEXT";
	/**
	* EFFECT_SCREENSHOT_RESPONSE - Send the screenshot response from web view to the client
	*
	* @deprecated
	*/
	EffectType[EffectType["EFFECT_SCREENSHOT_RESPONSE"] = 14] = "EFFECT_SCREENSHOT_RESPONSE";
	/**
	* EFFECT_LOGIN_PROMPT - Request a login/signup prompt
	*
	* @deprecated
	*/
	EffectType[EffectType["EFFECT_LOGIN_PROMPT"] = 15] = "EFFECT_LOGIN_PROMPT";
	EffectType[EffectType["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(EffectType || (EffectType = {}));
//#endregion
//#region crystal-cascade/node_modules/@devvit/protos/json/devvit/ui/effects/web_view/v1alpha/immersive_mode.js
/**
* #immersive_mode.ts
*
* Code generated by ts-proto. DO NOT EDIT.
* @packageDocumentation
*/
/** How a Devvit app is presented. */
var WebViewImmersiveMode$1;
(function(WebViewImmersiveMode) {
	WebViewImmersiveMode[WebViewImmersiveMode["UNSPECIFIED"] = 0] = "UNSPECIFIED";
	/**
	* INLINE_MODE - Conventional post presentation. May appear adjacent other posts in a feed
	* or with comments.
	*/
	WebViewImmersiveMode[WebViewImmersiveMode["INLINE_MODE"] = 1] = "INLINE_MODE";
	/**
	* IMMERSIVE_MODE - Large modal expanded presentation. Appears popped out from the rest of the
	* Reddit experience which is disabled.
	*/
	WebViewImmersiveMode[WebViewImmersiveMode["IMMERSIVE_MODE"] = 2] = "IMMERSIVE_MODE";
	WebViewImmersiveMode[WebViewImmersiveMode["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(WebViewImmersiveMode$1 || (WebViewImmersiveMode$1 = {}));
//#endregion
//#region crystal-cascade/node_modules/@devvit/protos/json/devvit/ui/effects/web_view/v1alpha/post_message.js
/**
* #post_message.ts
*
* Code generated by ts-proto. DO NOT EDIT.
* @packageDocumentation
*/
/**
* Blocks only.
*
* @deprecated
*/
var WebViewInternalMessageScope;
(function(WebViewInternalMessageScope) {
	/** CLIENT - Message is intended for the client only */
	WebViewInternalMessageScope[WebViewInternalMessageScope["CLIENT"] = 0] = "CLIENT";
	WebViewInternalMessageScope[WebViewInternalMessageScope["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(WebViewInternalMessageScope || (WebViewInternalMessageScope = {}));
//#endregion
//#region crystal-cascade/node_modules/@protobufjs/aspromise/index.js
var require_aspromise = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = asPromise;
	/**
	* Callback as used by {@link util.asPromise}.
	* @typedef asPromiseCallback
	* @type {function}
	* @param {Error|null} error Error, if any
	* @param {...*} params Additional arguments
	* @returns {undefined}
	*/
	/**
	* Returns a promise from a node-style callback function.
	* @memberof util
	* @param {asPromiseCallback} fn Function to call
	* @param {*} ctx Function context
	* @param {...*} params Function arguments
	* @returns {Promise<*>} Promisified function
	*/
	function asPromise(fn, ctx) {
		var params = new Array(arguments.length - 1), offset = 0, index = 2, pending = true;
		while (index < arguments.length) params[offset++] = arguments[index++];
		return new Promise(function executor(resolve, reject) {
			params[offset] = function callback(err) {
				if (pending) {
					pending = false;
					if (err) reject(err);
					else {
						var params = new Array(arguments.length - 1), offset = 0;
						while (offset < params.length) params[offset++] = arguments[offset];
						resolve.apply(null, params);
					}
				}
			};
			try {
				fn.apply(ctx || null, params);
			} catch (err) {
				if (pending) {
					pending = false;
					reject(err);
				}
			}
		});
	}
}));
//#endregion
//#region crystal-cascade/node_modules/@protobufjs/base64/index.js
var require_base64 = /* @__PURE__ */ __commonJSMin(((exports) => {
	/**
	* A minimal base64 implementation for number arrays.
	* @memberof util
	* @namespace
	*/
	var base64 = exports;
	/**
	* Calculates the byte length of a base64 encoded string.
	* @param {string} string Base64 encoded string
	* @returns {number} Byte length
	*/
	base64.length = function length(string) {
		var p = string.length;
		if (!p) return 0;
		var n = 0;
		while (--p % 4 > 1 && string.charAt(p) === "=") ++n;
		return Math.ceil(string.length * 3) / 4 - n;
	};
	var b64 = new Array(64);
	var s64 = new Array(123);
	for (var i = 0; i < 64;) s64[b64[i] = i < 26 ? i + 65 : i < 52 ? i + 71 : i < 62 ? i - 4 : i - 59 | 43] = i++;
	/**
	* Encodes a buffer to a base64 encoded string.
	* @param {Uint8Array} buffer Source buffer
	* @param {number} start Source start
	* @param {number} end Source end
	* @returns {string} Base64 encoded string
	*/
	base64.encode = function encode(buffer, start, end) {
		var parts = null, chunk = [];
		var i = 0, j = 0, t;
		while (start < end) {
			var b = buffer[start++];
			switch (j) {
				case 0:
					chunk[i++] = b64[b >> 2];
					t = (b & 3) << 4;
					j = 1;
					break;
				case 1:
					chunk[i++] = b64[t | b >> 4];
					t = (b & 15) << 2;
					j = 2;
					break;
				case 2:
					chunk[i++] = b64[t | b >> 6];
					chunk[i++] = b64[b & 63];
					j = 0;
					break;
			}
			if (i > 8191) {
				(parts || (parts = [])).push(String.fromCharCode.apply(String, chunk));
				i = 0;
			}
		}
		if (j) {
			chunk[i++] = b64[t];
			chunk[i++] = 61;
			if (j === 1) chunk[i++] = 61;
		}
		if (parts) {
			if (i) parts.push(String.fromCharCode.apply(String, chunk.slice(0, i)));
			return parts.join("");
		}
		return String.fromCharCode.apply(String, chunk.slice(0, i));
	};
	var invalidEncoding = "invalid encoding";
	/**
	* Decodes a base64 encoded string to a buffer.
	* @param {string} string Source string
	* @param {Uint8Array} buffer Destination buffer
	* @param {number} offset Destination offset
	* @returns {number} Number of bytes written
	* @throws {Error} If encoding is invalid
	*/
	base64.decode = function decode(string, buffer, offset) {
		var start = offset;
		var j = 0, t;
		for (var i = 0; i < string.length;) {
			var c = string.charCodeAt(i++);
			if (c === 61 && j > 1) break;
			if ((c = s64[c]) === void 0) throw Error(invalidEncoding);
			switch (j) {
				case 0:
					t = c;
					j = 1;
					break;
				case 1:
					buffer[offset++] = t << 2 | (c & 48) >> 4;
					t = c;
					j = 2;
					break;
				case 2:
					buffer[offset++] = (t & 15) << 4 | (c & 60) >> 2;
					t = c;
					j = 3;
					break;
				case 3:
					buffer[offset++] = (t & 3) << 6 | c;
					j = 0;
					break;
			}
		}
		if (j === 1) throw Error(invalidEncoding);
		return offset - start;
	};
	/**
	* Tests if the specified string appears to be base64 encoded.
	* @param {string} string String to test
	* @returns {boolean} `true` if probably base64 encoded, otherwise false
	*/
	base64.test = function test(string) {
		return /^(?:[A-Za-z0-9+/]{4})*(?:[A-Za-z0-9+/]{2}==|[A-Za-z0-9+/]{3}=)?$/.test(string);
	};
}));
//#endregion
//#region crystal-cascade/node_modules/@protobufjs/eventemitter/index.js
var require_eventemitter = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = EventEmitter;
	/**
	* Constructs a new event emitter instance.
	* @classdesc A minimal event emitter.
	* @memberof util
	* @constructor
	*/
	function EventEmitter() {
		/**
		* Registered listeners.
		* @type {Object.<string,*>}
		* @private
		*/
		this._listeners = Object.create(null);
	}
	/**
	* Event listener as used by {@link util.EventEmitter}.
	* @typedef EventEmitterListener
	* @type {function}
	* @param {...*} args Arguments
	* @returns {undefined}
	*/
	/**
	* Registers an event listener.
	* @param {string} evt Event name
	* @param {EventEmitterListener} fn Listener
	* @param {*} [ctx] Listener context
	* @returns {this} `this`
	*/
	EventEmitter.prototype.on = function on(evt, fn, ctx) {
		(this._listeners[evt] || (this._listeners[evt] = [])).push({
			fn,
			ctx: ctx || this
		});
		return this;
	};
	/**
	* Removes an event listener or any matching listeners if arguments are omitted.
	* @param {string} [evt] Event name. Removes all listeners if omitted.
	* @param {EventEmitterListener} [fn] Listener to remove. Removes all listeners of `evt` if omitted.
	* @returns {this} `this`
	*/
	EventEmitter.prototype.off = function off(evt, fn) {
		if (evt === void 0) this._listeners = Object.create(null);
		else if (fn === void 0) this._listeners[evt] = [];
		else {
			var listeners = this._listeners[evt];
			if (!listeners) return this;
			for (var i = 0; i < listeners.length;) if (listeners[i].fn === fn) listeners.splice(i, 1);
			else ++i;
		}
		return this;
	};
	/**
	* Emits an event by calling its listeners with the specified arguments.
	* @param {string} evt Event name
	* @param {...*} args Arguments
	* @returns {this} `this`
	*/
	EventEmitter.prototype.emit = function emit(evt) {
		var listeners = this._listeners[evt];
		if (listeners) {
			var args = [], i = 1;
			for (; i < arguments.length;) args.push(arguments[i++]);
			for (i = 0; i < listeners.length;) listeners[i].fn.apply(listeners[i++].ctx, args);
		}
		return this;
	};
}));
//#endregion
//#region crystal-cascade/node_modules/@protobufjs/float/index.js
var require_float = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = factory(factory);
	/**
	* Reads / writes floats / doubles from / to buffers.
	* @name util.float
	* @namespace
	*/
	/**
	* Writes a 32 bit float to a buffer using little endian byte order.
	* @name util.float.writeFloatLE
	* @function
	* @param {number} val Value to write
	* @param {Uint8Array} buf Target buffer
	* @param {number} pos Target buffer offset
	* @returns {undefined}
	*/
	/**
	* Writes a 32 bit float to a buffer using big endian byte order.
	* @name util.float.writeFloatBE
	* @function
	* @param {number} val Value to write
	* @param {Uint8Array} buf Target buffer
	* @param {number} pos Target buffer offset
	* @returns {undefined}
	*/
	/**
	* Reads a 32 bit float from a buffer using little endian byte order.
	* @name util.float.readFloatLE
	* @function
	* @param {Uint8Array} buf Source buffer
	* @param {number} pos Source buffer offset
	* @returns {number} Value read
	*/
	/**
	* Reads a 32 bit float from a buffer using big endian byte order.
	* @name util.float.readFloatBE
	* @function
	* @param {Uint8Array} buf Source buffer
	* @param {number} pos Source buffer offset
	* @returns {number} Value read
	*/
	/**
	* Writes a 64 bit double to a buffer using little endian byte order.
	* @name util.float.writeDoubleLE
	* @function
	* @param {number} val Value to write
	* @param {Uint8Array} buf Target buffer
	* @param {number} pos Target buffer offset
	* @returns {undefined}
	*/
	/**
	* Writes a 64 bit double to a buffer using big endian byte order.
	* @name util.float.writeDoubleBE
	* @function
	* @param {number} val Value to write
	* @param {Uint8Array} buf Target buffer
	* @param {number} pos Target buffer offset
	* @returns {undefined}
	*/
	/**
	* Reads a 64 bit double from a buffer using little endian byte order.
	* @name util.float.readDoubleLE
	* @function
	* @param {Uint8Array} buf Source buffer
	* @param {number} pos Source buffer offset
	* @returns {number} Value read
	*/
	/**
	* Reads a 64 bit double from a buffer using big endian byte order.
	* @name util.float.readDoubleBE
	* @function
	* @param {Uint8Array} buf Source buffer
	* @param {number} pos Source buffer offset
	* @returns {number} Value read
	*/
	function factory(exports$1) {
		if (typeof Float32Array !== "undefined") (function() {
			var f32 = new Float32Array([-0]), f8b = new Uint8Array(f32.buffer), le = f8b[3] === 128;
			function writeFloat_f32_cpy(val, buf, pos) {
				f32[0] = val;
				buf[pos] = f8b[0];
				buf[pos + 1] = f8b[1];
				buf[pos + 2] = f8b[2];
				buf[pos + 3] = f8b[3];
			}
			function writeFloat_f32_rev(val, buf, pos) {
				f32[0] = val;
				buf[pos] = f8b[3];
				buf[pos + 1] = f8b[2];
				buf[pos + 2] = f8b[1];
				buf[pos + 3] = f8b[0];
			}
			/* istanbul ignore next */
			exports$1.writeFloatLE = le ? writeFloat_f32_cpy : writeFloat_f32_rev;
			/* istanbul ignore next */
			exports$1.writeFloatBE = le ? writeFloat_f32_rev : writeFloat_f32_cpy;
			function readFloat_f32_cpy(buf, pos) {
				f8b[0] = buf[pos];
				f8b[1] = buf[pos + 1];
				f8b[2] = buf[pos + 2];
				f8b[3] = buf[pos + 3];
				return f32[0];
			}
			function readFloat_f32_rev(buf, pos) {
				f8b[3] = buf[pos];
				f8b[2] = buf[pos + 1];
				f8b[1] = buf[pos + 2];
				f8b[0] = buf[pos + 3];
				return f32[0];
			}
			/* istanbul ignore next */
			exports$1.readFloatLE = le ? readFloat_f32_cpy : readFloat_f32_rev;
			/* istanbul ignore next */
			exports$1.readFloatBE = le ? readFloat_f32_rev : readFloat_f32_cpy;
		})();
		else (function() {
			function writeFloat_ieee754(writeUint, val, buf, pos) {
				var sign = val < 0 ? 1 : 0;
				if (sign) val = -val;
				if (val === 0) writeUint(1 / val > 0 ? 0 : 2147483648, buf, pos);
				else if (isNaN(val)) writeUint(2143289344, buf, pos);
				else if (val > 34028234663852886e22) writeUint((sign << 31 | 2139095040) >>> 0, buf, pos);
				else if (val < 11754943508222875e-54) writeUint((sign << 31 | Math.round(val / 1401298464324817e-60)) >>> 0, buf, pos);
				else {
					var exponent = Math.floor(Math.log(val) / Math.LN2), mantissa = Math.round(val * Math.pow(2, -exponent) * 8388608) & 8388607;
					writeUint((sign << 31 | exponent + 127 << 23 | mantissa) >>> 0, buf, pos);
				}
			}
			exports$1.writeFloatLE = writeFloat_ieee754.bind(null, writeUintLE);
			exports$1.writeFloatBE = writeFloat_ieee754.bind(null, writeUintBE);
			function readFloat_ieee754(readUint, buf, pos) {
				var uint = readUint(buf, pos), sign = (uint >> 31) * 2 + 1, exponent = uint >>> 23 & 255, mantissa = uint & 8388607;
				return exponent === 255 ? mantissa ? NaN : sign * Infinity : exponent === 0 ? sign * 1401298464324817e-60 * mantissa : sign * Math.pow(2, exponent - 150) * (mantissa + 8388608);
			}
			exports$1.readFloatLE = readFloat_ieee754.bind(null, readUintLE);
			exports$1.readFloatBE = readFloat_ieee754.bind(null, readUintBE);
		})();
		if (typeof Float64Array !== "undefined") (function() {
			var f64 = new Float64Array([-0]), f8b = new Uint8Array(f64.buffer), le = f8b[7] === 128;
			function writeDouble_f64_cpy(val, buf, pos) {
				f64[0] = val;
				buf[pos] = f8b[0];
				buf[pos + 1] = f8b[1];
				buf[pos + 2] = f8b[2];
				buf[pos + 3] = f8b[3];
				buf[pos + 4] = f8b[4];
				buf[pos + 5] = f8b[5];
				buf[pos + 6] = f8b[6];
				buf[pos + 7] = f8b[7];
			}
			function writeDouble_f64_rev(val, buf, pos) {
				f64[0] = val;
				buf[pos] = f8b[7];
				buf[pos + 1] = f8b[6];
				buf[pos + 2] = f8b[5];
				buf[pos + 3] = f8b[4];
				buf[pos + 4] = f8b[3];
				buf[pos + 5] = f8b[2];
				buf[pos + 6] = f8b[1];
				buf[pos + 7] = f8b[0];
			}
			/* istanbul ignore next */
			exports$1.writeDoubleLE = le ? writeDouble_f64_cpy : writeDouble_f64_rev;
			/* istanbul ignore next */
			exports$1.writeDoubleBE = le ? writeDouble_f64_rev : writeDouble_f64_cpy;
			function readDouble_f64_cpy(buf, pos) {
				f8b[0] = buf[pos];
				f8b[1] = buf[pos + 1];
				f8b[2] = buf[pos + 2];
				f8b[3] = buf[pos + 3];
				f8b[4] = buf[pos + 4];
				f8b[5] = buf[pos + 5];
				f8b[6] = buf[pos + 6];
				f8b[7] = buf[pos + 7];
				return f64[0];
			}
			function readDouble_f64_rev(buf, pos) {
				f8b[7] = buf[pos];
				f8b[6] = buf[pos + 1];
				f8b[5] = buf[pos + 2];
				f8b[4] = buf[pos + 3];
				f8b[3] = buf[pos + 4];
				f8b[2] = buf[pos + 5];
				f8b[1] = buf[pos + 6];
				f8b[0] = buf[pos + 7];
				return f64[0];
			}
			/* istanbul ignore next */
			exports$1.readDoubleLE = le ? readDouble_f64_cpy : readDouble_f64_rev;
			/* istanbul ignore next */
			exports$1.readDoubleBE = le ? readDouble_f64_rev : readDouble_f64_cpy;
		})();
		else (function() {
			function writeDouble_ieee754(writeUint, off0, off1, val, buf, pos) {
				var sign = val < 0 ? 1 : 0;
				if (sign) val = -val;
				if (val === 0) {
					writeUint(0, buf, pos + off0);
					writeUint(1 / val > 0 ? 0 : 2147483648, buf, pos + off1);
				} else if (isNaN(val)) {
					writeUint(0, buf, pos + off0);
					writeUint(2146959360, buf, pos + off1);
				} else if (val > 17976931348623157e292) {
					writeUint(0, buf, pos + off0);
					writeUint((sign << 31 | 2146435072) >>> 0, buf, pos + off1);
				} else {
					var mantissa;
					if (val < 22250738585072014e-324) {
						mantissa = val / 5e-324;
						writeUint(mantissa >>> 0, buf, pos + off0);
						writeUint((sign << 31 | mantissa / 4294967296) >>> 0, buf, pos + off1);
					} else {
						var exponent = Math.floor(Math.log(val) / Math.LN2);
						if (exponent === 1024) exponent = 1023;
						mantissa = val * Math.pow(2, -exponent);
						writeUint(mantissa * 4503599627370496 >>> 0, buf, pos + off0);
						writeUint((sign << 31 | exponent + 1023 << 20 | mantissa * 1048576 & 1048575) >>> 0, buf, pos + off1);
					}
				}
			}
			exports$1.writeDoubleLE = writeDouble_ieee754.bind(null, writeUintLE, 0, 4);
			exports$1.writeDoubleBE = writeDouble_ieee754.bind(null, writeUintBE, 4, 0);
			function readDouble_ieee754(readUint, off0, off1, buf, pos) {
				var lo = readUint(buf, pos + off0), hi = readUint(buf, pos + off1);
				var sign = (hi >> 31) * 2 + 1, exponent = hi >>> 20 & 2047, mantissa = 4294967296 * (hi & 1048575) + lo;
				return exponent === 2047 ? mantissa ? NaN : sign * Infinity : exponent === 0 ? sign * 5e-324 * mantissa : sign * Math.pow(2, exponent - 1075) * (mantissa + 4503599627370496);
			}
			exports$1.readDoubleLE = readDouble_ieee754.bind(null, readUintLE, 0, 4);
			exports$1.readDoubleBE = readDouble_ieee754.bind(null, readUintBE, 4, 0);
		})();
		return exports$1;
	}
	function writeUintLE(val, buf, pos) {
		buf[pos] = val & 255;
		buf[pos + 1] = val >>> 8 & 255;
		buf[pos + 2] = val >>> 16 & 255;
		buf[pos + 3] = val >>> 24;
	}
	function writeUintBE(val, buf, pos) {
		buf[pos] = val >>> 24;
		buf[pos + 1] = val >>> 16 & 255;
		buf[pos + 2] = val >>> 8 & 255;
		buf[pos + 3] = val & 255;
	}
	function readUintLE(buf, pos) {
		return (buf[pos] | buf[pos + 1] << 8 | buf[pos + 2] << 16 | buf[pos + 3] << 24) >>> 0;
	}
	function readUintBE(buf, pos) {
		return (buf[pos] << 24 | buf[pos + 1] << 16 | buf[pos + 2] << 8 | buf[pos + 3]) >>> 0;
	}
}));
//#endregion
//#region crystal-cascade/node_modules/@protobufjs/inquire/index.js
var require_inquire = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = inquire;
	/**
	* Requires a module only if available.
	* @memberof util
	* @param {string} moduleName Module to require
	* @returns {?Object} Required module if available and not empty, otherwise `null`
	* @deprecated Legacy optional require helper. Will be removed in a future release.
	*/
	function inquire(moduleName) {
		try {
			if (typeof __require !== "function") return null;
			var mod = __require(moduleName);
			if (mod && (mod.length || Object.keys(mod).length)) return mod;
			return null;
		} catch (err) {
			return null;
		}
	}
}));
//#endregion
//#region crystal-cascade/node_modules/@protobufjs/utf8/index.js
var require_utf8 = /* @__PURE__ */ __commonJSMin(((exports) => {
	/**
	* A minimal UTF8 implementation for number arrays.
	* @memberof util
	* @namespace
	*/
	var utf8 = exports, replacementChar = "�";
	/**
	* Calculates the UTF8 byte length of a string.
	* @param {string} string String
	* @returns {number} Byte length
	*/
	utf8.length = function utf8_length(string) {
		var len = 0, c = 0;
		for (var i = 0; i < string.length; ++i) {
			c = string.charCodeAt(i);
			if (c < 128) len += 1;
			else if (c < 2048) len += 2;
			else if ((c & 64512) === 55296 && (string.charCodeAt(i + 1) & 64512) === 56320) {
				++i;
				len += 4;
			} else len += 3;
		}
		return len;
	};
	/**
	* Reads UTF8 bytes as a string.
	* @param {Uint8Array} buffer Source buffer
	* @param {number} start Source start
	* @param {number} end Source end
	* @returns {string} String read
	*/
	utf8.read = function utf8_read(buffer, start, end) {
		if (end - start < 1) return "";
		var str = "";
		for (var i = start; i < end;) {
			var t = buffer[i++];
			if (t <= 127) str += String.fromCharCode(t);
			else if (t >= 192 && t < 224) {
				var c2 = (t & 31) << 6 | buffer[i++] & 63;
				str += c2 >= 128 ? String.fromCharCode(c2) : replacementChar;
			} else if (t >= 224 && t < 240) {
				var c3 = (t & 15) << 12 | (buffer[i++] & 63) << 6 | buffer[i++] & 63;
				str += c3 >= 2048 ? String.fromCharCode(c3) : replacementChar;
			} else if (t >= 240) {
				var t2 = (t & 7) << 18 | (buffer[i++] & 63) << 12 | (buffer[i++] & 63) << 6 | buffer[i++] & 63;
				if (t2 < 65536 || t2 > 1114111) str += replacementChar;
				else {
					t2 -= 65536;
					str += String.fromCharCode(55296 + (t2 >> 10));
					str += String.fromCharCode(56320 + (t2 & 1023));
				}
			}
		}
		return str;
	};
	/**
	* Writes a string as UTF8 bytes.
	* @param {string} string Source string
	* @param {Uint8Array} buffer Destination buffer
	* @param {number} offset Destination offset
	* @returns {number} Bytes written
	*/
	utf8.write = function utf8_write(string, buffer, offset) {
		var start = offset, c1, c2;
		for (var i = 0; i < string.length; ++i) {
			c1 = string.charCodeAt(i);
			if (c1 < 128) buffer[offset++] = c1;
			else if (c1 < 2048) {
				buffer[offset++] = c1 >> 6 | 192;
				buffer[offset++] = c1 & 63 | 128;
			} else if ((c1 & 64512) === 55296 && ((c2 = string.charCodeAt(i + 1)) & 64512) === 56320) {
				c1 = 65536 + ((c1 & 1023) << 10) + (c2 & 1023);
				++i;
				buffer[offset++] = c1 >> 18 | 240;
				buffer[offset++] = c1 >> 12 & 63 | 128;
				buffer[offset++] = c1 >> 6 & 63 | 128;
				buffer[offset++] = c1 & 63 | 128;
			} else {
				buffer[offset++] = c1 >> 12 | 224;
				buffer[offset++] = c1 >> 6 & 63 | 128;
				buffer[offset++] = c1 & 63 | 128;
			}
		}
		return offset - start;
	};
}));
//#endregion
//#region crystal-cascade/node_modules/@protobufjs/pool/index.js
var require_pool = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = pool;
	/**
	* An allocator as used by {@link util.pool}.
	* @typedef PoolAllocator
	* @type {function}
	* @param {number} size Buffer size
	* @returns {Uint8Array} Buffer
	*/
	/**
	* A slicer as used by {@link util.pool}.
	* @typedef PoolSlicer
	* @type {function}
	* @param {number} start Start offset
	* @param {number} end End offset
	* @returns {Uint8Array} Buffer slice
	* @this {Uint8Array}
	*/
	/**
	* A general purpose buffer pool.
	* @memberof util
	* @function
	* @param {PoolAllocator} alloc Allocator
	* @param {PoolSlicer} slice Slicer
	* @param {number} [size=8192] Slab size
	* @returns {PoolAllocator} Pooled allocator
	*/
	function pool(alloc, slice, size) {
		var SIZE = size || 8192;
		var MAX = SIZE >>> 1;
		var slab = null;
		var offset = SIZE;
		return function pool_alloc(size) {
			if (size < 1 || size > MAX) return alloc(size);
			if (offset + size > SIZE) {
				slab = alloc(SIZE);
				offset = 0;
			}
			var buf = slice.call(slab, offset, offset += size);
			if (offset & 7) offset = (offset | 7) + 1;
			return buf;
		};
	}
}));
//#endregion
//#region crystal-cascade/node_modules/protobufjs/src/util/longbits.js
var require_longbits = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = LongBits;
	var util = require_minimal$1();
	/**
	* Constructs new long bits.
	* @classdesc Helper class for working with the low and high bits of a 64 bit value.
	* @memberof util
	* @constructor
	* @param {number} lo Low 32 bits, unsigned
	* @param {number} hi High 32 bits, unsigned
	*/
	function LongBits(lo, hi) {
		/**
		* Low bits.
		* @type {number}
		*/
		this.lo = lo >>> 0;
		/**
		* High bits.
		* @type {number}
		*/
		this.hi = hi >>> 0;
	}
	/**
	* Zero bits.
	* @memberof util.LongBits
	* @type {util.LongBits}
	*/
	var zero = LongBits.zero = new LongBits(0, 0);
	zero.toNumber = function() {
		return 0;
	};
	zero.zzEncode = zero.zzDecode = function() {
		return this;
	};
	zero.length = function() {
		return 1;
	};
	/**
	* Zero hash.
	* @memberof util.LongBits
	* @type {string}
	*/
	var zeroHash = LongBits.zeroHash = "\0\0\0\0\0\0\0\0";
	/**
	* Constructs new long bits from the specified number.
	* @param {number} value Value
	* @returns {util.LongBits} Instance
	*/
	LongBits.fromNumber = function fromNumber(value) {
		if (value === 0) return zero;
		var sign = value < 0;
		if (sign) value = -value;
		var lo = value >>> 0, hi = (value - lo) / 4294967296 >>> 0;
		if (sign) {
			hi = ~hi >>> 0;
			lo = ~lo >>> 0;
			if (++lo > 4294967295) {
				lo = 0;
				if (++hi > 4294967295) hi = 0;
			}
		}
		return new LongBits(lo, hi);
	};
	/**
	* Constructs new long bits from a number, long or string.
	* @param {Long|number|string} value Value
	* @returns {util.LongBits} Instance
	*/
	LongBits.from = function from(value) {
		if (typeof value === "number") return LongBits.fromNumber(value);
		if (util.isString(value))
 /* istanbul ignore else */
		if (util.Long) value = util.Long.fromString(value);
		else return LongBits.fromNumber(parseInt(value, 10));
		return value.low || value.high ? new LongBits(value.low >>> 0, value.high >>> 0) : zero;
	};
	/**
	* Converts this long bits to a possibly unsafe JavaScript number.
	* @param {boolean} [unsigned=false] Whether unsigned or not
	* @returns {number} Possibly unsafe number
	*/
	LongBits.prototype.toNumber = function toNumber(unsigned) {
		if (!unsigned && this.hi >>> 31) {
			var lo = ~this.lo + 1 >>> 0, hi = ~this.hi >>> 0;
			if (!lo) hi = hi + 1 >>> 0;
			return -(lo + hi * 4294967296);
		}
		return this.lo + this.hi * 4294967296;
	};
	/**
	* Converts this long bits to a long.
	* @param {boolean} [unsigned=false] Whether unsigned or not
	* @returns {Long} Long
	*/
	LongBits.prototype.toLong = function toLong(unsigned) {
		return util.Long ? new util.Long(this.lo | 0, this.hi | 0, Boolean(unsigned)) : {
			low: this.lo | 0,
			high: this.hi | 0,
			unsigned: Boolean(unsigned)
		};
	};
	var charCodeAt = String.prototype.charCodeAt;
	/**
	* Constructs new long bits from the specified 8 characters long hash.
	* @param {string} hash Hash
	* @returns {util.LongBits} Bits
	*/
	LongBits.fromHash = function fromHash(hash) {
		if (hash === zeroHash) return zero;
		return new LongBits((charCodeAt.call(hash, 0) | charCodeAt.call(hash, 1) << 8 | charCodeAt.call(hash, 2) << 16 | charCodeAt.call(hash, 3) << 24) >>> 0, (charCodeAt.call(hash, 4) | charCodeAt.call(hash, 5) << 8 | charCodeAt.call(hash, 6) << 16 | charCodeAt.call(hash, 7) << 24) >>> 0);
	};
	/**
	* Converts this long bits to a 8 characters long hash.
	* @returns {string} Hash
	*/
	LongBits.prototype.toHash = function toHash() {
		return String.fromCharCode(this.lo & 255, this.lo >>> 8 & 255, this.lo >>> 16 & 255, this.lo >>> 24, this.hi & 255, this.hi >>> 8 & 255, this.hi >>> 16 & 255, this.hi >>> 24);
	};
	/**
	* Zig-zag encodes this long bits.
	* @returns {util.LongBits} `this`
	*/
	LongBits.prototype.zzEncode = function zzEncode() {
		var mask = this.hi >> 31;
		this.hi = ((this.hi << 1 | this.lo >>> 31) ^ mask) >>> 0;
		this.lo = (this.lo << 1 ^ mask) >>> 0;
		return this;
	};
	/**
	* Zig-zag decodes this long bits.
	* @returns {util.LongBits} `this`
	*/
	LongBits.prototype.zzDecode = function zzDecode() {
		var mask = -(this.lo & 1);
		this.lo = ((this.lo >>> 1 | this.hi << 31) ^ mask) >>> 0;
		this.hi = (this.hi >>> 1 ^ mask) >>> 0;
		return this;
	};
	/**
	* Calculates the length of this longbits when encoded as a varint.
	* @returns {number} Length
	*/
	LongBits.prototype.length = function length() {
		var part0 = this.lo, part1 = (this.lo >>> 28 | this.hi << 4) >>> 0, part2 = this.hi >>> 24;
		return part2 === 0 ? part1 === 0 ? part0 < 16384 ? part0 < 128 ? 1 : 2 : part0 < 2097152 ? 3 : 4 : part1 < 16384 ? part1 < 128 ? 5 : 6 : part1 < 2097152 ? 7 : 8 : part2 < 128 ? 9 : 10;
	};
}));
//#endregion
//#region crystal-cascade/node_modules/protobufjs/src/util/minimal.js
var require_minimal$1 = /* @__PURE__ */ __commonJSMin(((exports) => {
	var util = exports;
	util.asPromise = require_aspromise();
	util.base64 = require_base64();
	util.EventEmitter = require_eventemitter();
	util.float = require_float();
	util.inquire = require_inquire();
	util.utf8 = require_utf8();
	util.pool = require_pool();
	util.LongBits = require_longbits();
	/**
	* Whether running within node or not.
	* @memberof util
	* @type {boolean}
	*/
	util.isNode = Boolean(typeof global !== "undefined" && global && global.process && global.process.versions && global.process.versions.node);
	/**
	* Global object reference.
	* @memberof util
	* @type {Object}
	*/
	util.global = util.isNode && global || typeof window !== "undefined" && window || typeof self !== "undefined" && self || exports;
	/**
	* An immuable empty array.
	* @memberof util
	* @type {Array.<*>}
	* @const
	*/
	util.emptyArray = Object.freeze ? Object.freeze([]) : 	/* istanbul ignore next */ [];
	/**
	* An immutable empty object.
	* @type {Object}
	* @const
	*/
	util.emptyObject = Object.freeze ? Object.freeze({}) : 	/* istanbul ignore next */ {};
	/**
	* Tests if the specified value is an integer.
	* @function
	* @param {*} value Value to test
	* @returns {boolean} `true` if the value is an integer
	*/
	util.isInteger = Number.isInteger || function isInteger(value) {
		return typeof value === "number" && isFinite(value) && Math.floor(value) === value;
	};
	/**
	* Tests if the specified value is a string.
	* @param {*} value Value to test
	* @returns {boolean} `true` if the value is a string
	*/
	util.isString = function isString(value) {
		return typeof value === "string" || value instanceof String;
	};
	/**
	* Tests if the specified value is a non-null object.
	* @param {*} value Value to test
	* @returns {boolean} `true` if the value is a non-null object
	*/
	util.isObject = function isObject(value) {
		return value && typeof value === "object";
	};
	/**
	* Checks if a property on a message is considered to be present.
	* This is an alias of {@link util.isSet}.
	* @function
	* @param {Object} obj Plain object or message instance
	* @param {string} prop Property name
	* @returns {boolean} `true` if considered to be present, otherwise `false`
	*/
	util.isset = util.isSet = function isSet(obj, prop) {
		var value = obj[prop];
		if (value != null && obj.hasOwnProperty(prop)) return typeof value !== "object" || (Array.isArray(value) ? value.length : Object.keys(value).length) > 0;
		return false;
	};
	/**
	* Any compatible Buffer instance.
	* This is a minimal stand-alone definition of a Buffer instance. The actual type is that exported by node's typings.
	* @interface Buffer
	* @extends Uint8Array
	*/
	/**
	* Node's Buffer class if available.
	* @type {Constructor<Buffer>}
	*/
	util.Buffer = (function() {
		try {
			var Buffer = util.inquire("buffer").Buffer;
			return Buffer.prototype.utf8Write ? Buffer : 			/* istanbul ignore next */ null;
		} catch (e) {
			/* istanbul ignore next */
			return null;
		}
	})();
	util._Buffer_from = null;
	util._Buffer_allocUnsafe = null;
	/**
	* Creates a new buffer of whatever type supported by the environment.
	* @param {number|number[]} [sizeOrArray=0] Buffer size or number array
	* @returns {Uint8Array|Buffer} Buffer
	*/
	util.newBuffer = function newBuffer(sizeOrArray) {
		/* istanbul ignore next */
		return typeof sizeOrArray === "number" ? util.Buffer ? util._Buffer_allocUnsafe(sizeOrArray) : new util.Array(sizeOrArray) : util.Buffer ? util._Buffer_from(sizeOrArray) : typeof Uint8Array === "undefined" ? sizeOrArray : new Uint8Array(sizeOrArray);
	};
	/**
	* Array implementation used in the browser. `Uint8Array` if supported, otherwise `Array`.
	* @type {Constructor<Uint8Array>}
	*/
	util.Array = typeof Uint8Array !== "undefined" ? Uint8Array : Array;
	/**
	* Any compatible Long instance.
	* This is a minimal stand-alone definition of a Long instance. The actual type is that exported by long.js.
	* @interface Long
	* @property {number} low Low bits
	* @property {number} high High bits
	* @property {boolean} unsigned Whether unsigned or not
	*/
	/**
	* Long.js's Long class if available.
	* @type {Constructor<Long>}
	*/
	util.Long = util.global.dcodeIO && util.global.dcodeIO.Long || util.global.Long || util.inquire("long");
	/**
	* Regular expression used to verify 2 bit (`bool`) map keys.
	* @type {RegExp}
	* @const
	*/
	util.key2Re = /^true|false|0|1$/;
	/**
	* Regular expression used to verify 32 bit (`int32` etc.) map keys.
	* @type {RegExp}
	* @const
	*/
	util.key32Re = /^-?(?:0|[1-9][0-9]*)$/;
	/**
	* Regular expression used to verify 64 bit (`int64` etc.) map keys.
	* @type {RegExp}
	* @const
	*/
	util.key64Re = /^(?:[\\x00-\\xff]{8}|-?(?:0|[1-9][0-9]*))$/;
	/**
	* Converts a number or long to an 8 characters long hash string.
	* @param {Long|number} value Value to convert
	* @returns {string} Hash
	*/
	util.longToHash = function longToHash(value) {
		return value ? util.LongBits.from(value).toHash() : util.LongBits.zeroHash;
	};
	/**
	* Converts an 8 characters long hash string to a long or number.
	* @param {string} hash Hash
	* @param {boolean} [unsigned=false] Whether unsigned or not
	* @returns {Long|number} Original value
	*/
	util.longFromHash = function longFromHash(hash, unsigned) {
		var bits = util.LongBits.fromHash(hash);
		if (util.Long) return util.Long.fromBits(bits.lo, bits.hi, unsigned);
		return bits.toNumber(Boolean(unsigned));
	};
	/**
	* Merges the properties of the source object into the destination object.
	* @memberof util
	* @param {Object.<string,*>} dst Destination object
	* @param {Object.<string,*>} src Source object
	* @param {boolean} [ifNotSet=false] Merges only if the key is not already set
	* @returns {Object.<string,*>} Destination object
	*/
	function merge(dst, src, ifNotSet) {
		for (var keys = Object.keys(src), i = 0; i < keys.length; ++i) if (dst[keys[i]] === void 0 || !ifNotSet) {
			if (keys[i] !== "__proto__") dst[keys[i]] = src[keys[i]];
		}
		return dst;
	}
	util.merge = merge;
	/**
	* Recursion limit.
	* @memberof util
	* @type {number}
	*/
	util.recursionLimit = 100;
	/**
	* Makes a property safe for assignment as an own property.
	* @memberof util
	* @param {Object.<string,*>} obj Object
	* @param {string} key Property key
	* @returns {undefined}
	*/
	util.makeProp = function makeProp(obj, key) {
		Object.defineProperty(obj, key, {
			enumerable: true,
			configurable: true,
			writable: true
		});
	};
	/**
	* Converts the first character of a string to lower case.
	* @param {string} str String to convert
	* @returns {string} Converted string
	*/
	util.lcFirst = function lcFirst(str) {
		return str.charAt(0).toLowerCase() + str.substring(1);
	};
	/**
	* Creates a custom error constructor.
	* @memberof util
	* @param {string} name Error name
	* @returns {Constructor<Error>} Custom error constructor
	*/
	function newError(name) {
		function CustomError(message, properties) {
			if (!(this instanceof CustomError)) return new CustomError(message, properties);
			Object.defineProperty(this, "message", { get: function() {
				return message;
			} });
			/* istanbul ignore next */
			if (Error.captureStackTrace) Error.captureStackTrace(this, CustomError);
			else Object.defineProperty(this, "stack", { value: (/* @__PURE__ */ new Error()).stack || "" });
			if (properties) merge(this, properties);
		}
		CustomError.prototype = Object.create(Error.prototype, {
			constructor: {
				value: CustomError,
				writable: true,
				enumerable: false,
				configurable: true
			},
			name: {
				get: function get() {
					return name;
				},
				set: void 0,
				enumerable: false,
				configurable: true
			},
			toString: {
				value: function value() {
					return this.name + ": " + this.message;
				},
				writable: true,
				enumerable: false,
				configurable: true
			}
		});
		return CustomError;
	}
	util.newError = newError;
	/**
	* Constructs a new protocol error.
	* @classdesc Error subclass indicating a protocol specifc error.
	* @memberof util
	* @extends Error
	* @template T extends Message<T>
	* @constructor
	* @param {string} message Error message
	* @param {Object.<string,*>} [properties] Additional properties
	* @example
	* try {
	*     MyMessage.decode(someBuffer); // throws if required fields are missing
	* } catch (e) {
	*     if (e instanceof ProtocolError && e.instance)
	*         console.log("decoded so far: " + JSON.stringify(e.instance));
	* }
	*/
	util.ProtocolError = newError("ProtocolError");
	/**
	* So far decoded message instance.
	* @name util.ProtocolError#instance
	* @type {Message<T>}
	*/
	/**
	* A OneOf getter as returned by {@link util.oneOfGetter}.
	* @typedef OneOfGetter
	* @type {function}
	* @returns {string|undefined} Set field name, if any
	*/
	/**
	* Builds a getter for a oneof's present field name.
	* @param {string[]} fieldNames Field names
	* @returns {OneOfGetter} Unbound getter
	*/
	util.oneOfGetter = function getOneOf(fieldNames) {
		var fieldMap = {};
		for (var i = 0; i < fieldNames.length; ++i) fieldMap[fieldNames[i]] = 1;
		/**
		* @returns {string|undefined} Set field name, if any
		* @this Object
		* @ignore
		*/
		return function() {
			for (var keys = Object.keys(this), i = keys.length - 1; i > -1; --i) if (fieldMap[keys[i]] === 1 && this[keys[i]] !== void 0 && this[keys[i]] !== null) return keys[i];
		};
	};
	/**
	* A OneOf setter as returned by {@link util.oneOfSetter}.
	* @typedef OneOfSetter
	* @type {function}
	* @param {string|undefined} value Field name
	* @returns {undefined}
	*/
	/**
	* Builds a setter for a oneof's present field name.
	* @param {string[]} fieldNames Field names
	* @returns {OneOfSetter} Unbound setter
	*/
	util.oneOfSetter = function setOneOf(fieldNames) {
		/**
		* @param {string} name Field name
		* @returns {undefined}
		* @this Object
		* @ignore
		*/
		return function(name) {
			for (var i = 0; i < fieldNames.length; ++i) if (fieldNames[i] !== name) delete this[fieldNames[i]];
		};
	};
	/**
	* Default conversion options used for {@link Message#toJSON} implementations.
	*
	* These options are close to proto3's JSON mapping with the exception that internal types like Any are handled just like messages. More precisely:
	*
	* - Longs become strings
	* - Enums become string keys
	* - Bytes become base64 encoded strings
	* - (Sub-)Messages become plain objects
	* - Maps become plain objects with all string keys
	* - Repeated fields become arrays
	* - NaN and Infinity for float and double fields become strings
	*
	* @type {IConversionOptions}
	* @see https://developers.google.com/protocol-buffers/docs/proto3?hl=en#json
	*/
	util.toJSONOptions = {
		longs: String,
		enums: String,
		bytes: String,
		json: true
	};
	util._configure = function() {
		var Buffer = util.Buffer;
		/* istanbul ignore if */
		if (!Buffer) {
			util._Buffer_from = util._Buffer_allocUnsafe = null;
			return;
		}
		util._Buffer_from = Buffer.from !== Uint8Array.from && Buffer.from || function Buffer_from(value, encoding) {
			return new Buffer(value, encoding);
		};
		util._Buffer_allocUnsafe = Buffer.allocUnsafe || function Buffer_allocUnsafe(size) {
			return new Buffer(size);
		};
	};
}));
//#endregion
//#region crystal-cascade/node_modules/protobufjs/src/writer.js
var require_writer = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = Writer;
	var util = require_minimal$1();
	var BufferWriter;
	var LongBits = util.LongBits, base64 = util.base64, utf8 = util.utf8;
	/**
	* Constructs a new writer operation instance.
	* @classdesc Scheduled writer operation.
	* @constructor
	* @param {function(*, Uint8Array, number)} fn Function to call
	* @param {number} len Value byte length
	* @param {*} val Value to write
	* @ignore
	*/
	function Op(fn, len, val) {
		/**
		* Function to call.
		* @type {function(Uint8Array, number, *)}
		*/
		this.fn = fn;
		/**
		* Value byte length.
		* @type {number}
		*/
		this.len = len;
		/**
		* Next operation.
		* @type {Writer.Op|undefined}
		*/
		this.next = void 0;
		/**
		* Value to write.
		* @type {*}
		*/
		this.val = val;
	}
	/* istanbul ignore next */
	function noop() {}
	/**
	* Constructs a new writer state instance.
	* @classdesc Copied writer state.
	* @memberof Writer
	* @constructor
	* @param {Writer} writer Writer to copy state from
	* @ignore
	*/
	function State(writer) {
		/**
		* Current head.
		* @type {Writer.Op}
		*/
		this.head = writer.head;
		/**
		* Current tail.
		* @type {Writer.Op}
		*/
		this.tail = writer.tail;
		/**
		* Current buffer length.
		* @type {number}
		*/
		this.len = writer.len;
		/**
		* Next state.
		* @type {State|null}
		*/
		this.next = writer.states;
	}
	/**
	* Constructs a new writer instance.
	* @classdesc Wire format writer using `Uint8Array` if available, otherwise `Array`.
	* @constructor
	*/
	function Writer() {
		/**
		* Current length.
		* @type {number}
		*/
		this.len = 0;
		/**
		* Operations head.
		* @type {Object}
		*/
		this.head = new Op(noop, 0, 0);
		/**
		* Operations tail
		* @type {Object}
		*/
		this.tail = this.head;
		/**
		* Linked forked states.
		* @type {Object|null}
		*/
		this.states = null;
	}
	var create = function create() {
		return util.Buffer ? function create_buffer_setup() {
			return (Writer.create = function create_buffer() {
				return new BufferWriter();
			})();
		} : function create_array() {
			return new Writer();
		};
	};
	/**
	* Creates a new writer.
	* @function
	* @returns {BufferWriter|Writer} A {@link BufferWriter} when Buffers are supported, otherwise a {@link Writer}
	*/
	Writer.create = create();
	/**
	* Allocates a buffer of the specified size.
	* @param {number} size Buffer size
	* @returns {Uint8Array} Buffer
	*/
	Writer.alloc = function alloc(size) {
		return new util.Array(size);
	};
	/* istanbul ignore else */
	if (util.Array !== Array) Writer.alloc = util.pool(Writer.alloc, util.Array.prototype.subarray);
	/**
	* Pushes a new operation to the queue.
	* @param {function(Uint8Array, number, *)} fn Function to call
	* @param {number} len Value byte length
	* @param {number} val Value to write
	* @returns {Writer} `this`
	* @private
	*/
	Writer.prototype._push = function push(fn, len, val) {
		this.tail = this.tail.next = new Op(fn, len, val);
		this.len += len;
		return this;
	};
	function writeByte(val, buf, pos) {
		buf[pos] = val & 255;
	}
	function writeVarint32(val, buf, pos) {
		while (val > 127) {
			buf[pos++] = val & 127 | 128;
			val >>>= 7;
		}
		buf[pos] = val;
	}
	/**
	* Constructs a new varint writer operation instance.
	* @classdesc Scheduled varint writer operation.
	* @extends Op
	* @constructor
	* @param {number} len Value byte length
	* @param {number} val Value to write
	* @ignore
	*/
	function VarintOp(len, val) {
		this.len = len;
		this.next = void 0;
		this.val = val;
	}
	VarintOp.prototype = Object.create(Op.prototype);
	VarintOp.prototype.fn = writeVarint32;
	/**
	* Writes an unsigned 32 bit value as a varint.
	* @param {number} value Value to write
	* @returns {Writer} `this`
	*/
	Writer.prototype.uint32 = function write_uint32(value) {
		this.len += (this.tail = this.tail.next = new VarintOp((value = value >>> 0) < 128 ? 1 : value < 16384 ? 2 : value < 2097152 ? 3 : value < 268435456 ? 4 : 5, value)).len;
		return this;
	};
	/**
	* Writes a signed 32 bit value as a varint.
	* @function
	* @param {number} value Value to write
	* @returns {Writer} `this`
	*/
	Writer.prototype.int32 = function write_int32(value) {
		return value < 0 ? this._push(writeVarint64, 10, LongBits.fromNumber(value)) : this.uint32(value);
	};
	/**
	* Writes a 32 bit value as a varint, zig-zag encoded.
	* @param {number} value Value to write
	* @returns {Writer} `this`
	*/
	Writer.prototype.sint32 = function write_sint32(value) {
		return this.uint32((value << 1 ^ value >> 31) >>> 0);
	};
	function writeVarint64(val, buf, pos) {
		while (val.hi) {
			buf[pos++] = val.lo & 127 | 128;
			val.lo = (val.lo >>> 7 | val.hi << 25) >>> 0;
			val.hi >>>= 7;
		}
		while (val.lo > 127) {
			buf[pos++] = val.lo & 127 | 128;
			val.lo = val.lo >>> 7;
		}
		buf[pos++] = val.lo;
	}
	/**
	* Writes an unsigned 64 bit value as a varint.
	* @param {Long|number|string} value Value to write
	* @returns {Writer} `this`
	* @throws {TypeError} If `value` is a string and no long library is present.
	*/
	Writer.prototype.uint64 = function write_uint64(value) {
		var bits = LongBits.from(value);
		return this._push(writeVarint64, bits.length(), bits);
	};
	/**
	* Writes a signed 64 bit value as a varint.
	* @function
	* @param {Long|number|string} value Value to write
	* @returns {Writer} `this`
	* @throws {TypeError} If `value` is a string and no long library is present.
	*/
	Writer.prototype.int64 = Writer.prototype.uint64;
	/**
	* Writes a signed 64 bit value as a varint, zig-zag encoded.
	* @param {Long|number|string} value Value to write
	* @returns {Writer} `this`
	* @throws {TypeError} If `value` is a string and no long library is present.
	*/
	Writer.prototype.sint64 = function write_sint64(value) {
		var bits = LongBits.from(value).zzEncode();
		return this._push(writeVarint64, bits.length(), bits);
	};
	/**
	* Writes a boolish value as a varint.
	* @param {boolean} value Value to write
	* @returns {Writer} `this`
	*/
	Writer.prototype.bool = function write_bool(value) {
		return this._push(writeByte, 1, value ? 1 : 0);
	};
	function writeFixed32(val, buf, pos) {
		buf[pos] = val & 255;
		buf[pos + 1] = val >>> 8 & 255;
		buf[pos + 2] = val >>> 16 & 255;
		buf[pos + 3] = val >>> 24;
	}
	/**
	* Writes an unsigned 32 bit value as fixed 32 bits.
	* @param {number} value Value to write
	* @returns {Writer} `this`
	*/
	Writer.prototype.fixed32 = function write_fixed32(value) {
		return this._push(writeFixed32, 4, value >>> 0);
	};
	/**
	* Writes a signed 32 bit value as fixed 32 bits.
	* @function
	* @param {number} value Value to write
	* @returns {Writer} `this`
	*/
	Writer.prototype.sfixed32 = Writer.prototype.fixed32;
	/**
	* Writes an unsigned 64 bit value as fixed 64 bits.
	* @param {Long|number|string} value Value to write
	* @returns {Writer} `this`
	* @throws {TypeError} If `value` is a string and no long library is present.
	*/
	Writer.prototype.fixed64 = function write_fixed64(value) {
		var bits = LongBits.from(value);
		return this._push(writeFixed32, 4, bits.lo)._push(writeFixed32, 4, bits.hi);
	};
	/**
	* Writes a signed 64 bit value as fixed 64 bits.
	* @function
	* @param {Long|number|string} value Value to write
	* @returns {Writer} `this`
	* @throws {TypeError} If `value` is a string and no long library is present.
	*/
	Writer.prototype.sfixed64 = Writer.prototype.fixed64;
	/**
	* Writes a float (32 bit).
	* @function
	* @param {number} value Value to write
	* @returns {Writer} `this`
	*/
	Writer.prototype.float = function write_float(value) {
		return this._push(util.float.writeFloatLE, 4, value);
	};
	/**
	* Writes a double (64 bit float).
	* @function
	* @param {number} value Value to write
	* @returns {Writer} `this`
	*/
	Writer.prototype.double = function write_double(value) {
		return this._push(util.float.writeDoubleLE, 8, value);
	};
	var writeBytes = util.Array.prototype.set ? function writeBytes_set(val, buf, pos) {
		buf.set(val, pos);
	} : function writeBytes_for(val, buf, pos) {
		for (var i = 0; i < val.length; ++i) buf[pos + i] = val[i];
	};
	/**
	* Writes a sequence of bytes.
	* @param {Uint8Array|string} value Buffer or base64 encoded string to write
	* @returns {Writer} `this`
	*/
	Writer.prototype.bytes = function write_bytes(value) {
		var len = value.length >>> 0;
		if (!len) return this._push(writeByte, 1, 0);
		if (util.isString(value)) {
			var buf = Writer.alloc(len = base64.length(value));
			base64.decode(value, buf, 0);
			value = buf;
		}
		return this.uint32(len)._push(writeBytes, len, value);
	};
	/**
	* Writes a string.
	* @param {string} value Value to write
	* @returns {Writer} `this`
	*/
	Writer.prototype.string = function write_string(value) {
		var len = utf8.length(value);
		return len ? this.uint32(len)._push(utf8.write, len, value) : this._push(writeByte, 1, 0);
	};
	/**
	* Forks this writer's state by pushing it to a stack.
	* Calling {@link Writer#reset|reset} or {@link Writer#ldelim|ldelim} resets the writer to the previous state.
	* @returns {Writer} `this`
	*/
	Writer.prototype.fork = function fork() {
		this.states = new State(this);
		this.head = this.tail = new Op(noop, 0, 0);
		this.len = 0;
		return this;
	};
	/**
	* Resets this instance to the last state.
	* @returns {Writer} `this`
	*/
	Writer.prototype.reset = function reset() {
		if (this.states) {
			this.head = this.states.head;
			this.tail = this.states.tail;
			this.len = this.states.len;
			this.states = this.states.next;
		} else {
			this.head = this.tail = new Op(noop, 0, 0);
			this.len = 0;
		}
		return this;
	};
	/**
	* Resets to the last state and appends the fork state's current write length as a varint followed by its operations.
	* @returns {Writer} `this`
	*/
	Writer.prototype.ldelim = function ldelim() {
		var head = this.head, tail = this.tail, len = this.len;
		this.reset().uint32(len);
		if (len) {
			this.tail.next = head.next;
			this.tail = tail;
			this.len += len;
		}
		return this;
	};
	/**
	* Finishes the write operation.
	* @returns {Uint8Array} Finished buffer
	*/
	Writer.prototype.finish = function finish() {
		var head = this.head.next, buf = this.constructor.alloc(this.len), pos = 0;
		while (head) {
			head.fn(head.val, buf, pos);
			pos += head.len;
			head = head.next;
		}
		return buf;
	};
	Writer._configure = function(BufferWriter_) {
		BufferWriter = BufferWriter_;
		Writer.create = create();
		BufferWriter._configure();
	};
}));
//#endregion
//#region crystal-cascade/node_modules/protobufjs/src/writer_buffer.js
var require_writer_buffer = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = BufferWriter;
	var Writer = require_writer();
	(BufferWriter.prototype = Object.create(Writer.prototype)).constructor = BufferWriter;
	var util = require_minimal$1();
	/**
	* Constructs a new buffer writer instance.
	* @classdesc Wire format writer using node buffers.
	* @extends Writer
	* @constructor
	*/
	function BufferWriter() {
		Writer.call(this);
	}
	BufferWriter._configure = function() {
		/**
		* Allocates a buffer of the specified size.
		* @function
		* @param {number} size Buffer size
		* @returns {Buffer} Buffer
		*/
		BufferWriter.alloc = util._Buffer_allocUnsafe;
		BufferWriter.writeBytesBuffer = util.Buffer && util.Buffer.prototype instanceof Uint8Array && util.Buffer.prototype.set.name === "set" ? function writeBytesBuffer_set(val, buf, pos) {
			buf.set(val, pos);
		} : function writeBytesBuffer_copy(val, buf, pos) {
			if (val.copy) val.copy(buf, pos, 0, val.length);
			else for (var i = 0; i < val.length;) buf[pos++] = val[i++];
		};
	};
	/**
	* @override
	*/
	BufferWriter.prototype.bytes = function write_bytes_buffer(value) {
		if (util.isString(value)) value = util._Buffer_from(value, "base64");
		var len = value.length >>> 0;
		this.uint32(len);
		if (len) this._push(BufferWriter.writeBytesBuffer, len, value);
		return this;
	};
	function writeStringBuffer(val, buf, pos) {
		if (val.length < 40) util.utf8.write(val, buf, pos);
		else if (buf.utf8Write) buf.utf8Write(val, pos);
		else buf.write(val, pos);
	}
	/**
	* @override
	*/
	BufferWriter.prototype.string = function write_string_buffer(value) {
		var len = util.Buffer.byteLength(value);
		this.uint32(len);
		if (len) this._push(writeStringBuffer, len, value);
		return this;
	};
	/**
	* Finishes the write operation.
	* @name BufferWriter#finish
	* @function
	* @returns {Buffer} Finished buffer
	*/
	BufferWriter._configure();
}));
//#endregion
//#region crystal-cascade/node_modules/protobufjs/src/reader.js
var require_reader = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = Reader;
	var util = require_minimal$1();
	var BufferReader;
	var LongBits = util.LongBits, utf8 = util.utf8;
	/* istanbul ignore next */
	function indexOutOfRange(reader, writeLength) {
		return RangeError("index out of range: " + reader.pos + " + " + (writeLength || 1) + " > " + reader.len);
	}
	/**
	* Constructs a new reader instance using the specified buffer.
	* @classdesc Wire format reader using `Uint8Array` if available, otherwise `Array`.
	* @constructor
	* @param {Uint8Array} buffer Buffer to read from
	*/
	function Reader(buffer) {
		/**
		* Read buffer.
		* @type {Uint8Array}
		*/
		this.buf = buffer;
		/**
		* Read buffer position.
		* @type {number}
		*/
		this.pos = 0;
		/**
		* Read buffer length.
		* @type {number}
		*/
		this.len = buffer.length;
	}
	var create_array = typeof Uint8Array !== "undefined" ? function create_typed_array(buffer) {
		if (buffer instanceof Uint8Array || Array.isArray(buffer)) return new Reader(buffer);
		throw Error("illegal buffer");
	} : function create_array(buffer) {
		if (Array.isArray(buffer)) return new Reader(buffer);
		throw Error("illegal buffer");
	};
	var create = function create() {
		return util.Buffer ? function create_buffer_setup(buffer) {
			return (Reader.create = function create_buffer(buffer) {
				return util.Buffer.isBuffer(buffer) ? new BufferReader(buffer) : create_array(buffer);
			})(buffer);
		} : create_array;
	};
	/**
	* Creates a new reader using the specified buffer.
	* @function
	* @param {Uint8Array|Buffer} buffer Buffer to read from
	* @returns {Reader|BufferReader} A {@link BufferReader} if `buffer` is a Buffer, otherwise a {@link Reader}
	* @throws {Error} If `buffer` is not a valid buffer
	*/
	Reader.create = create();
	Reader.prototype._slice = util.Array.prototype.subarray || util.Array.prototype.slice;
	/**
	* Reads a varint as an unsigned 32 bit value.
	* @function
	* @returns {number} Value read
	*/
	Reader.prototype.uint32 = (function read_uint32_setup() {
		var value = 4294967295;
		return function read_uint32() {
			value = (this.buf[this.pos] & 127) >>> 0;
			if (this.buf[this.pos++] < 128) return value;
			value = (value | (this.buf[this.pos] & 127) << 7) >>> 0;
			if (this.buf[this.pos++] < 128) return value;
			value = (value | (this.buf[this.pos] & 127) << 14) >>> 0;
			if (this.buf[this.pos++] < 128) return value;
			value = (value | (this.buf[this.pos] & 127) << 21) >>> 0;
			if (this.buf[this.pos++] < 128) return value;
			value = (value | (this.buf[this.pos] & 15) << 28) >>> 0;
			if (this.buf[this.pos++] < 128) return value;
			/* istanbul ignore if */
			if ((this.pos += 5) > this.len) {
				this.pos = this.len;
				throw indexOutOfRange(this, 10);
			}
			return value;
		};
	})();
	/**
	* Reads a varint as a signed 32 bit value.
	* @returns {number} Value read
	*/
	Reader.prototype.int32 = function read_int32() {
		return this.uint32() | 0;
	};
	/**
	* Reads a zig-zag encoded varint as a signed 32 bit value.
	* @returns {number} Value read
	*/
	Reader.prototype.sint32 = function read_sint32() {
		var value = this.uint32();
		return value >>> 1 ^ -(value & 1) | 0;
	};
	function readLongVarint() {
		var bits = new LongBits(0, 0);
		var i = 0;
		if (this.len - this.pos > 4) {
			for (; i < 4; ++i) {
				bits.lo = (bits.lo | (this.buf[this.pos] & 127) << i * 7) >>> 0;
				if (this.buf[this.pos++] < 128) return bits;
			}
			bits.lo = (bits.lo | (this.buf[this.pos] & 127) << 28) >>> 0;
			bits.hi = (bits.hi | (this.buf[this.pos] & 127) >> 4) >>> 0;
			if (this.buf[this.pos++] < 128) return bits;
			i = 0;
		} else {
			for (; i < 3; ++i) {
				/* istanbul ignore if */
				if (this.pos >= this.len) throw indexOutOfRange(this);
				bits.lo = (bits.lo | (this.buf[this.pos] & 127) << i * 7) >>> 0;
				if (this.buf[this.pos++] < 128) return bits;
			}
			bits.lo = (bits.lo | (this.buf[this.pos++] & 127) << i * 7) >>> 0;
			return bits;
		}
		if (this.len - this.pos > 4) for (; i < 5; ++i) {
			bits.hi = (bits.hi | (this.buf[this.pos] & 127) << i * 7 + 3) >>> 0;
			if (this.buf[this.pos++] < 128) return bits;
		}
		else for (; i < 5; ++i) {
			/* istanbul ignore if */
			if (this.pos >= this.len) throw indexOutOfRange(this);
			bits.hi = (bits.hi | (this.buf[this.pos] & 127) << i * 7 + 3) >>> 0;
			if (this.buf[this.pos++] < 128) return bits;
		}
		/* istanbul ignore next */
		throw Error("invalid varint encoding");
	}
	/**
	* Reads a varint as a signed 64 bit value.
	* @name Reader#int64
	* @function
	* @returns {Long} Value read
	*/
	/**
	* Reads a varint as an unsigned 64 bit value.
	* @name Reader#uint64
	* @function
	* @returns {Long} Value read
	*/
	/**
	* Reads a zig-zag encoded varint as a signed 64 bit value.
	* @name Reader#sint64
	* @function
	* @returns {Long} Value read
	*/
	/**
	* Reads a varint as a boolean.
	* @returns {boolean} Value read
	*/
	Reader.prototype.bool = function read_bool() {
		return this.uint32() !== 0;
	};
	function readFixed32_end(buf, end) {
		return (buf[end - 4] | buf[end - 3] << 8 | buf[end - 2] << 16 | buf[end - 1] << 24) >>> 0;
	}
	/**
	* Reads fixed 32 bits as an unsigned 32 bit integer.
	* @returns {number} Value read
	*/
	Reader.prototype.fixed32 = function read_fixed32() {
		/* istanbul ignore if */
		if (this.pos + 4 > this.len) throw indexOutOfRange(this, 4);
		return readFixed32_end(this.buf, this.pos += 4);
	};
	/**
	* Reads fixed 32 bits as a signed 32 bit integer.
	* @returns {number} Value read
	*/
	Reader.prototype.sfixed32 = function read_sfixed32() {
		/* istanbul ignore if */
		if (this.pos + 4 > this.len) throw indexOutOfRange(this, 4);
		return readFixed32_end(this.buf, this.pos += 4) | 0;
	};
	function readFixed64() {
		/* istanbul ignore if */
		if (this.pos + 8 > this.len) throw indexOutOfRange(this, 8);
		return new LongBits(readFixed32_end(this.buf, this.pos += 4), readFixed32_end(this.buf, this.pos += 4));
	}
	/**
	* Reads fixed 64 bits.
	* @name Reader#fixed64
	* @function
	* @returns {Long} Value read
	*/
	/**
	* Reads zig-zag encoded fixed 64 bits.
	* @name Reader#sfixed64
	* @function
	* @returns {Long} Value read
	*/
	/**
	* Reads a float (32 bit) as a number.
	* @function
	* @returns {number} Value read
	*/
	Reader.prototype.float = function read_float() {
		/* istanbul ignore if */
		if (this.pos + 4 > this.len) throw indexOutOfRange(this, 4);
		var value = util.float.readFloatLE(this.buf, this.pos);
		this.pos += 4;
		return value;
	};
	/**
	* Reads a double (64 bit float) as a number.
	* @function
	* @returns {number} Value read
	*/
	Reader.prototype.double = function read_double() {
		/* istanbul ignore if */
		if (this.pos + 8 > this.len) throw indexOutOfRange(this, 4);
		var value = util.float.readDoubleLE(this.buf, this.pos);
		this.pos += 8;
		return value;
	};
	/**
	* Reads a sequence of bytes preceeded by its length as a varint.
	* @returns {Uint8Array} Value read
	*/
	Reader.prototype.bytes = function read_bytes() {
		var length = this.uint32(), start = this.pos, end = this.pos + length;
		/* istanbul ignore if */
		if (end > this.len) throw indexOutOfRange(this, length);
		this.pos += length;
		if (Array.isArray(this.buf)) return this.buf.slice(start, end);
		if (start === end) {
			var nativeBuffer = util.Buffer;
			return nativeBuffer ? nativeBuffer.alloc(0) : new this.buf.constructor(0);
		}
		return this._slice.call(this.buf, start, end);
	};
	/**
	* Reads a string preceeded by its byte length as a varint.
	* @returns {string} Value read
	*/
	Reader.prototype.string = function read_string() {
		var bytes = this.bytes();
		return utf8.read(bytes, 0, bytes.length);
	};
	/**
	* Skips the specified number of bytes if specified, otherwise skips a varint.
	* @param {number} [length] Length if known, otherwise a varint is assumed
	* @returns {Reader} `this`
	*/
	Reader.prototype.skip = function skip(length) {
		if (typeof length === "number") {
			/* istanbul ignore if */
			if (this.pos + length > this.len) throw indexOutOfRange(this, length);
			this.pos += length;
		} else do
			/* istanbul ignore if */
			if (this.pos >= this.len) throw indexOutOfRange(this);
		while (this.buf[this.pos++] & 128);
		return this;
	};
	/**
	* Recursion limit.
	* @type {number}
	*/
	Reader.recursionLimit = util.recursionLimit;
	/**
	* Skips the next element of the specified wire type.
	* @param {number} wireType Wire type received
	* @param {number} [depth] Depth of recursion to control nested calls; 0 if omitted
	* @returns {Reader} `this`
	*/
	Reader.prototype.skipType = function(wireType, depth) {
		if (depth === void 0) depth = 0;
		if (depth > Reader.recursionLimit) throw Error("maximum nesting depth exceeded");
		switch (wireType) {
			case 0:
				this.skip();
				break;
			case 1:
				this.skip(8);
				break;
			case 2:
				this.skip(this.uint32());
				break;
			case 3:
				while ((wireType = this.uint32() & 7) !== 4) this.skipType(wireType, depth + 1);
				break;
			case 5:
				this.skip(4);
				break;
			/* istanbul ignore next */
			default: throw Error("invalid wire type " + wireType + " at offset " + this.pos);
		}
		return this;
	};
	Reader._configure = function(BufferReader_) {
		BufferReader = BufferReader_;
		Reader.create = create();
		BufferReader._configure();
		var fn = util.Long ? "toLong" : 		/* istanbul ignore next */ "toNumber";
		util.merge(Reader.prototype, {
			int64: function read_int64() {
				return readLongVarint.call(this)[fn](false);
			},
			uint64: function read_uint64() {
				return readLongVarint.call(this)[fn](true);
			},
			sint64: function read_sint64() {
				return readLongVarint.call(this).zzDecode()[fn](false);
			},
			fixed64: function read_fixed64() {
				return readFixed64.call(this)[fn](true);
			},
			sfixed64: function read_sfixed64() {
				return readFixed64.call(this)[fn](false);
			}
		});
	};
}));
//#endregion
//#region crystal-cascade/node_modules/protobufjs/src/reader_buffer.js
var require_reader_buffer = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = BufferReader;
	var Reader = require_reader();
	(BufferReader.prototype = Object.create(Reader.prototype)).constructor = BufferReader;
	var util = require_minimal$1();
	/**
	* Constructs a new buffer reader instance.
	* @classdesc Wire format reader using node buffers.
	* @extends Reader
	* @constructor
	* @param {Buffer} buffer Buffer to read from
	*/
	function BufferReader(buffer) {
		Reader.call(this, buffer);
		/**
		* Read buffer.
		* @name BufferReader#buf
		* @type {Buffer}
		*/
	}
	BufferReader._configure = function() {
		/* istanbul ignore else */
		if (util.Buffer) BufferReader.prototype._slice = util.Buffer.prototype.slice;
	};
	/**
	* @override
	*/
	BufferReader.prototype.string = function read_string_buffer() {
		var len = this.uint32();
		return this.buf.utf8Slice ? this.buf.utf8Slice(this.pos, this.pos = Math.min(this.pos + len, this.len)) : this.buf.toString("utf-8", this.pos, this.pos = Math.min(this.pos + len, this.len));
	};
	/**
	* Reads a sequence of bytes preceeded by its length as a varint.
	* @name BufferReader#bytes
	* @function
	* @returns {Buffer} Value read
	*/
	BufferReader._configure();
}));
//#endregion
//#region crystal-cascade/node_modules/protobufjs/src/rpc/service.js
var require_service = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = Service;
	var util = require_minimal$1();
	(Service.prototype = Object.create(util.EventEmitter.prototype)).constructor = Service;
	/**
	* A service method callback as used by {@link rpc.ServiceMethod|ServiceMethod}.
	*
	* Differs from {@link RPCImplCallback} in that it is an actual callback of a service method which may not return `response = null`.
	* @typedef rpc.ServiceMethodCallback
	* @template TRes extends Message<TRes>
	* @type {function}
	* @param {Error|null} error Error, if any
	* @param {TRes} [response] Response message
	* @returns {undefined}
	*/
	/**
	* A service method part of a {@link rpc.Service} as created by {@link Service.create}.
	* @typedef rpc.ServiceMethod
	* @template TReq extends Message<TReq>
	* @template TRes extends Message<TRes>
	* @type {function}
	* @param {TReq|Properties<TReq>} request Request message or plain object
	* @param {rpc.ServiceMethodCallback<TRes>} [callback] Node-style callback called with the error, if any, and the response message
	* @returns {Promise<Message<TRes>>} Promise if `callback` has been omitted, otherwise `undefined`
	*/
	/**
	* Constructs a new RPC service instance.
	* @classdesc An RPC service as returned by {@link Service#create}.
	* @exports rpc.Service
	* @extends util.EventEmitter
	* @constructor
	* @param {RPCImpl} rpcImpl RPC implementation
	* @param {boolean} [requestDelimited=false] Whether requests are length-delimited
	* @param {boolean} [responseDelimited=false] Whether responses are length-delimited
	*/
	function Service(rpcImpl, requestDelimited, responseDelimited) {
		if (typeof rpcImpl !== "function") throw TypeError("rpcImpl must be a function");
		util.EventEmitter.call(this);
		/**
		* RPC implementation. Becomes `null` once the service is ended.
		* @type {RPCImpl|null}
		*/
		this.rpcImpl = rpcImpl;
		/**
		* Whether requests are length-delimited.
		* @type {boolean}
		*/
		this.requestDelimited = Boolean(requestDelimited);
		/**
		* Whether responses are length-delimited.
		* @type {boolean}
		*/
		this.responseDelimited = Boolean(responseDelimited);
	}
	/**
	* Calls a service method through {@link rpc.Service#rpcImpl|rpcImpl}.
	* @param {Method|rpc.ServiceMethod<TReq,TRes>} method Reflected or static method
	* @param {Constructor<TReq>} requestCtor Request constructor
	* @param {Constructor<TRes>} responseCtor Response constructor
	* @param {TReq|Properties<TReq>} request Request message or plain object
	* @param {rpc.ServiceMethodCallback<TRes>} callback Service callback
	* @returns {undefined}
	* @template TReq extends Message<TReq>
	* @template TRes extends Message<TRes>
	*/
	Service.prototype.rpcCall = function rpcCall(method, requestCtor, responseCtor, request, callback) {
		if (!request) throw TypeError("request must be specified");
		var self = this;
		if (!callback) return util.asPromise(rpcCall, self, method, requestCtor, responseCtor, request);
		if (!self.rpcImpl) {
			setTimeout(function() {
				callback(Error("already ended"));
			}, 0);
			return;
		}
		try {
			return self.rpcImpl(method, requestCtor[self.requestDelimited ? "encodeDelimited" : "encode"](request).finish(), function rpcCallback(err, response) {
				if (err) {
					self.emit("error", err, method);
					return callback(err);
				}
				if (response === null) {
					self.end(true);
					return;
				}
				if (!(response instanceof responseCtor)) try {
					response = responseCtor[self.responseDelimited ? "decodeDelimited" : "decode"](response);
				} catch (err) {
					self.emit("error", err, method);
					return callback(err);
				}
				self.emit("data", response, method);
				return callback(null, response);
			});
		} catch (err) {
			self.emit("error", err, method);
			setTimeout(function() {
				callback(err);
			}, 0);
			return;
		}
	};
	/**
	* Ends this service and emits the `end` event.
	* @param {boolean} [endedByRPC=false] Whether the service has been ended by the RPC implementation.
	* @returns {rpc.Service} `this`
	*/
	Service.prototype.end = function end(endedByRPC) {
		if (this.rpcImpl) {
			if (!endedByRPC) this.rpcImpl(null, null, null);
			this.rpcImpl = null;
			this.emit("end").off();
		}
		return this;
	};
}));
//#endregion
//#region crystal-cascade/node_modules/protobufjs/src/rpc.js
var require_rpc = /* @__PURE__ */ __commonJSMin(((exports) => {
	/**
	* Streaming RPC helpers.
	* @namespace
	*/
	var rpc = exports;
	/**
	* RPC implementation passed to {@link Service#create} performing a service request on network level, i.e. by utilizing http requests or websockets.
	* @typedef RPCImpl
	* @type {function}
	* @param {Method|rpc.ServiceMethod<Message<{}>,Message<{}>>} method Reflected or static method being called
	* @param {Uint8Array} requestData Request data
	* @param {RPCImplCallback} callback Callback function
	* @returns {undefined}
	* @example
	* function rpcImpl(method, requestData, callback) {
	*     if (protobuf.util.lcFirst(method.name) !== "myMethod") // compatible with static code
	*         throw Error("no such method");
	*     asynchronouslyObtainAResponse(requestData, function(err, responseData) {
	*         callback(err, responseData);
	*     });
	* }
	*/
	/**
	* Node-style callback as used by {@link RPCImpl}.
	* @typedef RPCImplCallback
	* @type {function}
	* @param {Error|null} error Error, if any, otherwise `null`
	* @param {Uint8Array|null} [response] Response data or `null` to signal end of stream, if there hasn't been an error
	* @returns {undefined}
	*/
	rpc.Service = require_service();
}));
//#endregion
//#region crystal-cascade/node_modules/protobufjs/src/roots.js
var require_roots = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = {};
}));
/**
* Named roots.
* This is where pbjs stores generated structures (the option `-r, --root` specifies a name).
* Can also be used manually to make roots available across modules.
* @name roots
* @type {Object.<string,Root>}
* @example
* // pbjs -r myroot -o compiled.js ...
*
* // in another module:
* require("./compiled.js");
*
* // in any subsequent module:
* var root = protobuf.roots["myroot"];
*/
//#endregion
//#region crystal-cascade/node_modules/protobufjs/src/index-minimal.js
var require_index_minimal = /* @__PURE__ */ __commonJSMin(((exports) => {
	var protobuf = exports;
	/**
	* Build type, one of `"full"`, `"light"` or `"minimal"`.
	* @name build
	* @type {string}
	* @const
	*/
	protobuf.build = "minimal";
	protobuf.Writer = require_writer();
	protobuf.BufferWriter = require_writer_buffer();
	protobuf.Reader = require_reader();
	protobuf.BufferReader = require_reader_buffer();
	protobuf.util = require_minimal$1();
	protobuf.rpc = require_rpc();
	protobuf.roots = require_roots();
	protobuf.configure = configure;
	/* istanbul ignore next */
	/**
	* Reconfigures the library according to the environment.
	* @returns {undefined}
	*/
	function configure() {
		protobuf.util._configure();
		protobuf.Writer._configure(protobuf.BufferWriter);
		protobuf.Reader._configure(protobuf.BufferReader);
	}
	configure();
}));
//#endregion
//#region crystal-cascade/node_modules/@devvit/protos/types/typeRegistry.js
var import_minimal = /* @__PURE__ */ __toESM((/* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_index_minimal();
})))(), 1);
/**
* #typeRegistry.ts
*
* Code generated by ts-proto. DO NOT EDIT.
* @packageDocumentation
*/
var messageTypeRegistry = /* @__PURE__ */ new Map();
//#endregion
//#region crystal-cascade/node_modules/@devvit/protos/types/google/protobuf/struct.js
/**
* #struct.ts
*
* Code generated by ts-proto. DO NOT EDIT.
* @packageDocumentation
*/
/**
* `NullValue` is a singleton enumeration to represent the null value for the
* `Value` type union.
*
*  The JSON representation for `NullValue` is JSON `null`.
*/
var NullValue;
(function(NullValue) {
	/** NULL_VALUE - Null value. */
	NullValue[NullValue["NULL_VALUE"] = 0] = "NULL_VALUE";
	NullValue[NullValue["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(NullValue || (NullValue = {}));
function nullValueFromJSON(object) {
	switch (object) {
		case 0:
		case "NULL_VALUE": return NullValue.NULL_VALUE;
		default: return NullValue.UNRECOGNIZED;
	}
}
function nullValueToJSON(object) {
	switch (object) {
		case NullValue.NULL_VALUE: return 0;
		case NullValue.UNRECOGNIZED:
		default: return -1;
	}
}
function createBaseStruct() {
	return { fields: {} };
}
var Struct = {
	$type: "google.protobuf.Struct",
	encode(message, writer = import_minimal.default.Writer.create()) {
		Object.entries(message.fields).forEach(([key, value]) => {
			if (value !== void 0) Struct_FieldsEntry.encode({
				key,
				value
			}, writer.uint32(10).fork()).ldelim();
		});
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseStruct();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {
				case 1:
					if (tag !== 10) break;
					const entry1 = Struct_FieldsEntry.decode(reader, reader.uint32());
					if (entry1.value !== void 0) message.fields[entry1.key] = entry1.value;
					continue;
			}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(object) {
		return { fields: isObject$3(object.fields) ? Object.entries(object.fields).reduce((acc, [key, value]) => {
			acc[key] = value;
			return acc;
		}, {}) : {} };
	},
	toJSON(message) {
		const obj = {};
		if (message.fields) {
			const entries = Object.entries(message.fields);
			if (entries.length > 0) {
				obj.fields = {};
				entries.forEach(([k, v]) => {
					obj.fields[k] = v;
				});
			}
		}
		return obj;
	},
	create(base) {
		return Struct.fromPartial(base ?? {});
	},
	fromPartial(object) {
		const message = createBaseStruct();
		message.fields = Object.entries(object.fields ?? {}).reduce((acc, [key, value]) => {
			if (value !== void 0) acc[key] = value;
			return acc;
		}, {});
		return message;
	},
	wrap(object) {
		const struct = createBaseStruct();
		if (object !== void 0) for (const key of Object.keys(object)) struct.fields[key] = object[key];
		return struct;
	},
	unwrap(message) {
		const object = {};
		if (message.fields) for (const key of Object.keys(message.fields)) object[key] = message.fields[key];
		return object;
	}
};
messageTypeRegistry.set(Struct.$type, Struct);
function createBaseStruct_FieldsEntry() {
	return {
		key: "",
		value: void 0
	};
}
var Struct_FieldsEntry = {
	$type: "google.protobuf.Struct.FieldsEntry",
	encode(message, writer = import_minimal.default.Writer.create()) {
		if (message.key !== "") writer.uint32(10).string(message.key);
		if (message.value !== void 0) Value.encode(Value.wrap(message.value), writer.uint32(18).fork()).ldelim();
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseStruct_FieldsEntry();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {
				case 1:
					if (tag !== 10) break;
					message.key = reader.string();
					continue;
				case 2:
					if (tag !== 18) break;
					message.value = Value.unwrap(Value.decode(reader, reader.uint32()));
					continue;
			}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(object) {
		return {
			key: isSet$14(object.key) ? globalThis.String(object.key) : "",
			value: isSet$14(object?.value) ? object.value : void 0
		};
	},
	toJSON(message) {
		const obj = {};
		if (message.key !== "") obj.key = message.key;
		if (message.value !== void 0) obj.value = message.value;
		return obj;
	},
	create(base) {
		return Struct_FieldsEntry.fromPartial(base ?? {});
	},
	fromPartial(object) {
		const message = createBaseStruct_FieldsEntry();
		message.key = object.key ?? "";
		message.value = object.value ?? void 0;
		return message;
	}
};
messageTypeRegistry.set(Struct_FieldsEntry.$type, Struct_FieldsEntry);
function createBaseValue() {
	return {
		nullValue: void 0,
		numberValue: void 0,
		stringValue: void 0,
		boolValue: void 0,
		structValue: void 0,
		listValue: void 0
	};
}
var Value = {
	$type: "google.protobuf.Value",
	encode(message, writer = import_minimal.default.Writer.create()) {
		if (message.nullValue !== void 0) writer.uint32(8).int32(message.nullValue);
		if (message.numberValue !== void 0) writer.uint32(17).double(message.numberValue);
		if (message.stringValue !== void 0) writer.uint32(26).string(message.stringValue);
		if (message.boolValue !== void 0) writer.uint32(32).bool(message.boolValue);
		if (message.structValue !== void 0) Struct.encode(Struct.wrap(message.structValue), writer.uint32(42).fork()).ldelim();
		if (message.listValue !== void 0) ListValue.encode(ListValue.wrap(message.listValue), writer.uint32(50).fork()).ldelim();
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseValue();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {
				case 1:
					if (tag !== 8) break;
					message.nullValue = reader.int32();
					continue;
				case 2:
					if (tag !== 17) break;
					message.numberValue = reader.double();
					continue;
				case 3:
					if (tag !== 26) break;
					message.stringValue = reader.string();
					continue;
				case 4:
					if (tag !== 32) break;
					message.boolValue = reader.bool();
					continue;
				case 5:
					if (tag !== 42) break;
					message.structValue = Struct.unwrap(Struct.decode(reader, reader.uint32()));
					continue;
				case 6:
					if (tag !== 50) break;
					message.listValue = ListValue.unwrap(ListValue.decode(reader, reader.uint32()));
					continue;
			}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(object) {
		return {
			nullValue: isSet$14(object.nullValue) ? nullValueFromJSON(object.nullValue) : void 0,
			numberValue: isSet$14(object.numberValue) ? globalThis.Number(object.numberValue) : void 0,
			stringValue: isSet$14(object.stringValue) ? globalThis.String(object.stringValue) : void 0,
			boolValue: isSet$14(object.boolValue) ? globalThis.Boolean(object.boolValue) : void 0,
			structValue: isObject$3(object.structValue) ? object.structValue : void 0,
			listValue: globalThis.Array.isArray(object.listValue) ? [...object.listValue] : void 0
		};
	},
	toJSON(message) {
		const obj = {};
		if (message.nullValue !== void 0) obj.nullValue = nullValueToJSON(message.nullValue);
		if (message.numberValue !== void 0) obj.numberValue = message.numberValue;
		if (message.stringValue !== void 0) obj.stringValue = message.stringValue;
		if (message.boolValue !== void 0) obj.boolValue = message.boolValue;
		if (message.structValue !== void 0) obj.structValue = message.structValue;
		if (message.listValue !== void 0) obj.listValue = message.listValue;
		return obj;
	},
	create(base) {
		return Value.fromPartial(base ?? {});
	},
	fromPartial(object) {
		const message = createBaseValue();
		message.nullValue = object.nullValue ?? void 0;
		message.numberValue = object.numberValue ?? void 0;
		message.stringValue = object.stringValue ?? void 0;
		message.boolValue = object.boolValue ?? void 0;
		message.structValue = object.structValue ?? void 0;
		message.listValue = object.listValue ?? void 0;
		return message;
	},
	wrap(value) {
		const result = createBaseValue();
		if (value === null) result.nullValue = NullValue.NULL_VALUE;
		else if (typeof value === "boolean") result.boolValue = value;
		else if (typeof value === "number") result.numberValue = value;
		else if (typeof value === "string") result.stringValue = value;
		else if (globalThis.Array.isArray(value)) result.listValue = value;
		else if (typeof value === "object") result.structValue = value;
		else if (typeof value !== "undefined") throw new globalThis.Error("Unsupported any value type: " + typeof value);
		return result;
	},
	unwrap(message) {
		if (message.stringValue !== void 0) return message.stringValue;
		else if (message?.numberValue !== void 0) return message.numberValue;
		else if (message?.boolValue !== void 0) return message.boolValue;
		else if (message?.structValue !== void 0) return message.structValue;
		else if (message?.listValue !== void 0) return message.listValue;
		else if (message?.nullValue !== void 0) return null;
	}
};
messageTypeRegistry.set(Value.$type, Value);
function createBaseListValue() {
	return { values: [] };
}
var ListValue = {
	$type: "google.protobuf.ListValue",
	encode(message, writer = import_minimal.default.Writer.create()) {
		for (const v of message.values) Value.encode(Value.wrap(v), writer.uint32(10).fork()).ldelim();
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseListValue();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {
				case 1:
					if (tag !== 10) break;
					message.values.push(Value.unwrap(Value.decode(reader, reader.uint32())));
					continue;
			}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(object) {
		return { values: globalThis.Array.isArray(object?.values) ? [...object.values] : [] };
	},
	toJSON(message) {
		const obj = {};
		if (message.values?.length) obj.values = message.values;
		return obj;
	},
	create(base) {
		return ListValue.fromPartial(base ?? {});
	},
	fromPartial(object) {
		const message = createBaseListValue();
		message.values = object.values?.map((e) => e) || [];
		return message;
	},
	wrap(array) {
		const result = createBaseListValue();
		result.values = array ?? [];
		return result;
	},
	unwrap(message) {
		if (message?.hasOwnProperty("values") && globalThis.Array.isArray(message.values)) return message.values;
		else return message;
	}
};
messageTypeRegistry.set(ListValue.$type, ListValue);
function isObject$3(value) {
	return typeof value === "object" && value !== null;
}
function isSet$14(value) {
	return value !== null && value !== void 0;
}
//#endregion
//#region crystal-cascade/node_modules/long/index.js
/**
* @license
* Copyright 2009 The Closure Library Authors
* Copyright 2020 Daniel Wirtz / The long.js Authors.
*
* Licensed under the Apache License, Version 2.0 (the "License");
* you may not use this file except in compliance with the License.
* You may obtain a copy of the License at
*
*     http://www.apache.org/licenses/LICENSE-2.0
*
* Unless required by applicable law or agreed to in writing, software
* distributed under the License is distributed on an "AS IS" BASIS,
* WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
* See the License for the specific language governing permissions and
* limitations under the License.
*
* SPDX-License-Identifier: Apache-2.0
*/
var wasm = null;
try {
	wasm = new WebAssembly.Instance(new WebAssembly.Module(new Uint8Array([
		0,
		97,
		115,
		109,
		1,
		0,
		0,
		0,
		1,
		13,
		2,
		96,
		0,
		1,
		127,
		96,
		4,
		127,
		127,
		127,
		127,
		1,
		127,
		3,
		7,
		6,
		0,
		1,
		1,
		1,
		1,
		1,
		6,
		6,
		1,
		127,
		1,
		65,
		0,
		11,
		7,
		50,
		6,
		3,
		109,
		117,
		108,
		0,
		1,
		5,
		100,
		105,
		118,
		95,
		115,
		0,
		2,
		5,
		100,
		105,
		118,
		95,
		117,
		0,
		3,
		5,
		114,
		101,
		109,
		95,
		115,
		0,
		4,
		5,
		114,
		101,
		109,
		95,
		117,
		0,
		5,
		8,
		103,
		101,
		116,
		95,
		104,
		105,
		103,
		104,
		0,
		0,
		10,
		191,
		1,
		6,
		4,
		0,
		35,
		0,
		11,
		36,
		1,
		1,
		126,
		32,
		0,
		173,
		32,
		1,
		173,
		66,
		32,
		134,
		132,
		32,
		2,
		173,
		32,
		3,
		173,
		66,
		32,
		134,
		132,
		126,
		34,
		4,
		66,
		32,
		135,
		167,
		36,
		0,
		32,
		4,
		167,
		11,
		36,
		1,
		1,
		126,
		32,
		0,
		173,
		32,
		1,
		173,
		66,
		32,
		134,
		132,
		32,
		2,
		173,
		32,
		3,
		173,
		66,
		32,
		134,
		132,
		127,
		34,
		4,
		66,
		32,
		135,
		167,
		36,
		0,
		32,
		4,
		167,
		11,
		36,
		1,
		1,
		126,
		32,
		0,
		173,
		32,
		1,
		173,
		66,
		32,
		134,
		132,
		32,
		2,
		173,
		32,
		3,
		173,
		66,
		32,
		134,
		132,
		128,
		34,
		4,
		66,
		32,
		135,
		167,
		36,
		0,
		32,
		4,
		167,
		11,
		36,
		1,
		1,
		126,
		32,
		0,
		173,
		32,
		1,
		173,
		66,
		32,
		134,
		132,
		32,
		2,
		173,
		32,
		3,
		173,
		66,
		32,
		134,
		132,
		129,
		34,
		4,
		66,
		32,
		135,
		167,
		36,
		0,
		32,
		4,
		167,
		11,
		36,
		1,
		1,
		126,
		32,
		0,
		173,
		32,
		1,
		173,
		66,
		32,
		134,
		132,
		32,
		2,
		173,
		32,
		3,
		173,
		66,
		32,
		134,
		132,
		130,
		34,
		4,
		66,
		32,
		135,
		167,
		36,
		0,
		32,
		4,
		167,
		11
	])), {}).exports;
} catch {}
/**
* Constructs a 64 bit two's-complement integer, given its low and high 32 bit values as *signed* integers.
*  See the from* functions below for more convenient ways of constructing Longs.
* @exports Long
* @class A Long class for representing a 64 bit two's-complement integer value.
* @param {number} low The low (signed) 32 bits of the long
* @param {number} high The high (signed) 32 bits of the long
* @param {boolean=} unsigned Whether unsigned or not, defaults to signed
* @constructor
*/
function Long(low, high, unsigned) {
	/**
	* The low 32 bits as a signed value.
	* @type {number}
	*/
	this.low = low | 0;
	/**
	* The high 32 bits as a signed value.
	* @type {number}
	*/
	this.high = high | 0;
	/**
	* Whether unsigned or not.
	* @type {boolean}
	*/
	this.unsigned = !!unsigned;
}
/**
* An indicator used to reliably determine if an object is a Long or not.
* @type {boolean}
* @const
* @private
*/
Long.prototype.__isLong__;
Object.defineProperty(Long.prototype, "__isLong__", { value: true });
/**
* @function
* @param {*} obj Object
* @returns {boolean}
* @inner
*/
function isLong(obj) {
	return (obj && obj["__isLong__"]) === true;
}
/**
* @function
* @param {*} value number
* @returns {number}
* @inner
*/
function ctz32(value) {
	var c = Math.clz32(value & -value);
	return value ? 31 - c : c;
}
/**
* Tests if the specified object is a Long.
* @function
* @param {*} obj Object
* @returns {boolean}
*/
Long.isLong = isLong;
/**
* A cache of the Long representations of small integer values.
* @type {!Object}
* @inner
*/
var INT_CACHE = {};
/**
* A cache of the Long representations of small unsigned integer values.
* @type {!Object}
* @inner
*/
var UINT_CACHE = {};
/**
* @param {number} value
* @param {boolean=} unsigned
* @returns {!Long}
* @inner
*/
function fromInt(value, unsigned) {
	var obj, cachedObj, cache;
	if (unsigned) {
		value >>>= 0;
		if (cache = 0 <= value && value < 256) {
			cachedObj = UINT_CACHE[value];
			if (cachedObj) return cachedObj;
		}
		obj = fromBits(value, 0, true);
		if (cache) UINT_CACHE[value] = obj;
		return obj;
	} else {
		value |= 0;
		if (cache = -128 <= value && value < 128) {
			cachedObj = INT_CACHE[value];
			if (cachedObj) return cachedObj;
		}
		obj = fromBits(value, value < 0 ? -1 : 0, false);
		if (cache) INT_CACHE[value] = obj;
		return obj;
	}
}
/**
* Returns a Long representing the given 32 bit integer value.
* @function
* @param {number} value The 32 bit integer in question
* @param {boolean=} unsigned Whether unsigned or not, defaults to signed
* @returns {!Long} The corresponding Long value
*/
Long.fromInt = fromInt;
/**
* @param {number} value
* @param {boolean=} unsigned
* @returns {!Long}
* @inner
*/
function fromNumber(value, unsigned) {
	if (isNaN(value)) return unsigned ? UZERO : ZERO;
	if (unsigned) {
		if (value < 0) return UZERO;
		if (value >= TWO_PWR_64_DBL) return MAX_UNSIGNED_VALUE;
	} else {
		if (value <= -TWO_PWR_63_DBL) return MIN_VALUE;
		if (value + 1 >= TWO_PWR_63_DBL) return MAX_VALUE;
	}
	if (value < 0) return fromNumber(-value, unsigned).neg();
	return fromBits(value % TWO_PWR_32_DBL | 0, value / TWO_PWR_32_DBL | 0, unsigned);
}
/**
* Returns a Long representing the given value, provided that it is a finite number. Otherwise, zero is returned.
* @function
* @param {number} value The number in question
* @param {boolean=} unsigned Whether unsigned or not, defaults to signed
* @returns {!Long} The corresponding Long value
*/
Long.fromNumber = fromNumber;
/**
* @param {number} lowBits
* @param {number} highBits
* @param {boolean=} unsigned
* @returns {!Long}
* @inner
*/
function fromBits(lowBits, highBits, unsigned) {
	return new Long(lowBits, highBits, unsigned);
}
/**
* Returns a Long representing the 64 bit integer that comes by concatenating the given low and high bits. Each is
*  assumed to use 32 bits.
* @function
* @param {number} lowBits The low 32 bits
* @param {number} highBits The high 32 bits
* @param {boolean=} unsigned Whether unsigned or not, defaults to signed
* @returns {!Long} The corresponding Long value
*/
Long.fromBits = fromBits;
/**
* @function
* @param {number} base
* @param {number} exponent
* @returns {number}
* @inner
*/
var pow_dbl = Math.pow;
/**
* @param {string} str
* @param {(boolean|number)=} unsigned
* @param {number=} radix
* @returns {!Long}
* @inner
*/
function fromString(str, unsigned, radix) {
	if (str.length === 0) throw Error("empty string");
	if (typeof unsigned === "number") {
		radix = unsigned;
		unsigned = false;
	} else unsigned = !!unsigned;
	if (str === "NaN" || str === "Infinity" || str === "+Infinity" || str === "-Infinity") return unsigned ? UZERO : ZERO;
	radix = radix || 10;
	if (radix < 2 || 36 < radix) throw RangeError("radix");
	var p;
	if ((p = str.indexOf("-")) > 0) throw Error("interior hyphen");
	else if (p === 0) return fromString(str.substring(1), unsigned, radix).neg();
	var radixToPower = fromNumber(pow_dbl(radix, 8));
	var result = ZERO;
	for (var i = 0; i < str.length; i += 8) {
		var size = Math.min(8, str.length - i), value = parseInt(str.substring(i, i + size), radix);
		if (size < 8) {
			var power = fromNumber(pow_dbl(radix, size));
			result = result.mul(power).add(fromNumber(value));
		} else {
			result = result.mul(radixToPower);
			result = result.add(fromNumber(value));
		}
	}
	result.unsigned = unsigned;
	return result;
}
/**
* Returns a Long representation of the given string, written using the specified radix.
* @function
* @param {string} str The textual representation of the Long
* @param {(boolean|number)=} unsigned Whether unsigned or not, defaults to signed
* @param {number=} radix The radix in which the text is written (2-36), defaults to 10
* @returns {!Long} The corresponding Long value
*/
Long.fromString = fromString;
/**
* @function
* @param {!Long|number|string|!{low: number, high: number, unsigned: boolean}} val
* @param {boolean=} unsigned
* @returns {!Long}
* @inner
*/
function fromValue(val, unsigned) {
	if (typeof val === "number") return fromNumber(val, unsigned);
	if (typeof val === "string") return fromString(val, unsigned);
	return fromBits(val.low, val.high, typeof unsigned === "boolean" ? unsigned : val.unsigned);
}
/**
* Converts the specified value to a Long using the appropriate from* function for its type.
* @function
* @param {!Long|number|bigint|string|!{low: number, high: number, unsigned: boolean}} val Value
* @param {boolean=} unsigned Whether unsigned or not, defaults to signed
* @returns {!Long}
*/
Long.fromValue = fromValue;
/**
* @type {number}
* @const
* @inner
*/
var TWO_PWR_16_DBL = 65536;
/**
* @type {number}
* @const
* @inner
*/
var TWO_PWR_24_DBL = 1 << 24;
/**
* @type {number}
* @const
* @inner
*/
var TWO_PWR_32_DBL = TWO_PWR_16_DBL * TWO_PWR_16_DBL;
/**
* @type {number}
* @const
* @inner
*/
var TWO_PWR_64_DBL = TWO_PWR_32_DBL * TWO_PWR_32_DBL;
/**
* @type {number}
* @const
* @inner
*/
var TWO_PWR_63_DBL = TWO_PWR_64_DBL / 2;
/**
* @type {!Long}
* @const
* @inner
*/
var TWO_PWR_24 = fromInt(TWO_PWR_24_DBL);
/**
* @type {!Long}
* @inner
*/
var ZERO = fromInt(0);
/**
* Signed zero.
* @type {!Long}
*/
Long.ZERO = ZERO;
/**
* @type {!Long}
* @inner
*/
var UZERO = fromInt(0, true);
/**
* Unsigned zero.
* @type {!Long}
*/
Long.UZERO = UZERO;
/**
* @type {!Long}
* @inner
*/
var ONE = fromInt(1);
/**
* Signed one.
* @type {!Long}
*/
Long.ONE = ONE;
/**
* @type {!Long}
* @inner
*/
var UONE = fromInt(1, true);
/**
* Unsigned one.
* @type {!Long}
*/
Long.UONE = UONE;
/**
* @type {!Long}
* @inner
*/
var NEG_ONE = fromInt(-1);
/**
* Signed negative one.
* @type {!Long}
*/
Long.NEG_ONE = NEG_ONE;
/**
* @type {!Long}
* @inner
*/
var MAX_VALUE = fromBits(-1, 2147483647, false);
/**
* Maximum signed value.
* @type {!Long}
*/
Long.MAX_VALUE = MAX_VALUE;
/**
* @type {!Long}
* @inner
*/
var MAX_UNSIGNED_VALUE = fromBits(-1, -1, true);
/**
* Maximum unsigned value.
* @type {!Long}
*/
Long.MAX_UNSIGNED_VALUE = MAX_UNSIGNED_VALUE;
/**
* @type {!Long}
* @inner
*/
var MIN_VALUE = fromBits(0, -2147483648, false);
/**
* Minimum signed value.
* @type {!Long}
*/
Long.MIN_VALUE = MIN_VALUE;
/**
* @alias Long.prototype
* @inner
*/
var LongPrototype = Long.prototype;
/**
* Converts the Long to a 32 bit integer, assuming it is a 32 bit integer.
* @this {!Long}
* @returns {number}
*/
LongPrototype.toInt = function toInt() {
	return this.unsigned ? this.low >>> 0 : this.low;
};
/**
* Converts the Long to a the nearest floating-point representation of this value (double, 53 bit mantissa).
* @this {!Long}
* @returns {number}
*/
LongPrototype.toNumber = function toNumber() {
	if (this.unsigned) return (this.high >>> 0) * TWO_PWR_32_DBL + (this.low >>> 0);
	return this.high * TWO_PWR_32_DBL + (this.low >>> 0);
};
/**
* Converts the Long to a string written in the specified radix.
* @this {!Long}
* @param {number=} radix Radix (2-36), defaults to 10
* @returns {string}
* @override
* @throws {RangeError} If `radix` is out of range
*/
LongPrototype.toString = function toString(radix) {
	radix = radix || 10;
	if (radix < 2 || 36 < radix) throw RangeError("radix");
	if (this.isZero()) return "0";
	if (this.isNegative()) if (this.eq(MIN_VALUE)) {
		var radixLong = fromNumber(radix), div = this.div(radixLong), rem1 = div.mul(radixLong).sub(this);
		return div.toString(radix) + rem1.toInt().toString(radix);
	} else return "-" + this.neg().toString(radix);
	var radixToPower = fromNumber(pow_dbl(radix, 6), this.unsigned), rem = this;
	var result = "";
	while (true) {
		var remDiv = rem.div(radixToPower), digits = (rem.sub(remDiv.mul(radixToPower)).toInt() >>> 0).toString(radix);
		rem = remDiv;
		if (rem.isZero()) return digits + result;
		else {
			while (digits.length < 6) digits = "0" + digits;
			result = "" + digits + result;
		}
	}
};
/**
* Gets the high 32 bits as a signed integer.
* @this {!Long}
* @returns {number} Signed high bits
*/
LongPrototype.getHighBits = function getHighBits() {
	return this.high;
};
/**
* Gets the high 32 bits as an unsigned integer.
* @this {!Long}
* @returns {number} Unsigned high bits
*/
LongPrototype.getHighBitsUnsigned = function getHighBitsUnsigned() {
	return this.high >>> 0;
};
/**
* Gets the low 32 bits as a signed integer.
* @this {!Long}
* @returns {number} Signed low bits
*/
LongPrototype.getLowBits = function getLowBits() {
	return this.low;
};
/**
* Gets the low 32 bits as an unsigned integer.
* @this {!Long}
* @returns {number} Unsigned low bits
*/
LongPrototype.getLowBitsUnsigned = function getLowBitsUnsigned() {
	return this.low >>> 0;
};
/**
* Gets the number of bits needed to represent the absolute value of this Long.
* @this {!Long}
* @returns {number}
*/
LongPrototype.getNumBitsAbs = function getNumBitsAbs() {
	if (this.isNegative()) return this.eq(MIN_VALUE) ? 64 : this.neg().getNumBitsAbs();
	var val = this.high != 0 ? this.high : this.low;
	for (var bit = 31; bit > 0; bit--) if ((val & 1 << bit) != 0) break;
	return this.high != 0 ? bit + 33 : bit + 1;
};
/**
* Tests if this Long can be safely represented as a JavaScript number.
* @this {!Long}
* @returns {boolean}
*/
LongPrototype.isSafeInteger = function isSafeInteger() {
	var top11Bits = this.high >> 21;
	if (!top11Bits) return true;
	if (this.unsigned) return false;
	return top11Bits === -1 && !(this.low === 0 && this.high === -2097152);
};
/**
* Tests if this Long's value equals zero.
* @this {!Long}
* @returns {boolean}
*/
LongPrototype.isZero = function isZero() {
	return this.high === 0 && this.low === 0;
};
/**
* Tests if this Long's value equals zero. This is an alias of {@link Long#isZero}.
* @returns {boolean}
*/
LongPrototype.eqz = LongPrototype.isZero;
/**
* Tests if this Long's value is negative.
* @this {!Long}
* @returns {boolean}
*/
LongPrototype.isNegative = function isNegative() {
	return !this.unsigned && this.high < 0;
};
/**
* Tests if this Long's value is positive or zero.
* @this {!Long}
* @returns {boolean}
*/
LongPrototype.isPositive = function isPositive() {
	return this.unsigned || this.high >= 0;
};
/**
* Tests if this Long's value is odd.
* @this {!Long}
* @returns {boolean}
*/
LongPrototype.isOdd = function isOdd() {
	return (this.low & 1) === 1;
};
/**
* Tests if this Long's value is even.
* @this {!Long}
* @returns {boolean}
*/
LongPrototype.isEven = function isEven() {
	return (this.low & 1) === 0;
};
/**
* Tests if this Long's value equals the specified's.
* @this {!Long}
* @param {!Long|number|bigint|string} other Other value
* @returns {boolean}
*/
LongPrototype.equals = function equals(other) {
	if (!isLong(other)) other = fromValue(other);
	if (this.unsigned !== other.unsigned && this.high >>> 31 === 1 && other.high >>> 31 === 1) return false;
	return this.high === other.high && this.low === other.low;
};
/**
* Tests if this Long's value equals the specified's. This is an alias of {@link Long#equals}.
* @function
* @param {!Long|number|bigint|string} other Other value
* @returns {boolean}
*/
LongPrototype.eq = LongPrototype.equals;
/**
* Tests if this Long's value differs from the specified's.
* @this {!Long}
* @param {!Long|number|bigint|string} other Other value
* @returns {boolean}
*/
LongPrototype.notEquals = function notEquals(other) {
	return !this.eq(other);
};
/**
* Tests if this Long's value differs from the specified's. This is an alias of {@link Long#notEquals}.
* @function
* @param {!Long|number|bigint|string} other Other value
* @returns {boolean}
*/
LongPrototype.neq = LongPrototype.notEquals;
/**
* Tests if this Long's value differs from the specified's. This is an alias of {@link Long#notEquals}.
* @function
* @param {!Long|number|bigint|string} other Other value
* @returns {boolean}
*/
LongPrototype.ne = LongPrototype.notEquals;
/**
* Tests if this Long's value is less than the specified's.
* @this {!Long}
* @param {!Long|number|bigint|string} other Other value
* @returns {boolean}
*/
LongPrototype.lessThan = function lessThan(other) {
	return this.comp(other) < 0;
};
/**
* Tests if this Long's value is less than the specified's. This is an alias of {@link Long#lessThan}.
* @function
* @param {!Long|number|bigint|string} other Other value
* @returns {boolean}
*/
LongPrototype.lt = LongPrototype.lessThan;
/**
* Tests if this Long's value is less than or equal the specified's.
* @this {!Long}
* @param {!Long|number|bigint|string} other Other value
* @returns {boolean}
*/
LongPrototype.lessThanOrEqual = function lessThanOrEqual(other) {
	return this.comp(other) <= 0;
};
/**
* Tests if this Long's value is less than or equal the specified's. This is an alias of {@link Long#lessThanOrEqual}.
* @function
* @param {!Long|number|bigint|string} other Other value
* @returns {boolean}
*/
LongPrototype.lte = LongPrototype.lessThanOrEqual;
/**
* Tests if this Long's value is less than or equal the specified's. This is an alias of {@link Long#lessThanOrEqual}.
* @function
* @param {!Long|number|bigint|string} other Other value
* @returns {boolean}
*/
LongPrototype.le = LongPrototype.lessThanOrEqual;
/**
* Tests if this Long's value is greater than the specified's.
* @this {!Long}
* @param {!Long|number|bigint|string} other Other value
* @returns {boolean}
*/
LongPrototype.greaterThan = function greaterThan(other) {
	return this.comp(other) > 0;
};
/**
* Tests if this Long's value is greater than the specified's. This is an alias of {@link Long#greaterThan}.
* @function
* @param {!Long|number|bigint|string} other Other value
* @returns {boolean}
*/
LongPrototype.gt = LongPrototype.greaterThan;
/**
* Tests if this Long's value is greater than or equal the specified's.
* @this {!Long}
* @param {!Long|number|bigint|string} other Other value
* @returns {boolean}
*/
LongPrototype.greaterThanOrEqual = function greaterThanOrEqual(other) {
	return this.comp(other) >= 0;
};
/**
* Tests if this Long's value is greater than or equal the specified's. This is an alias of {@link Long#greaterThanOrEqual}.
* @function
* @param {!Long|number|bigint|string} other Other value
* @returns {boolean}
*/
LongPrototype.gte = LongPrototype.greaterThanOrEqual;
/**
* Tests if this Long's value is greater than or equal the specified's. This is an alias of {@link Long#greaterThanOrEqual}.
* @function
* @param {!Long|number|bigint|string} other Other value
* @returns {boolean}
*/
LongPrototype.ge = LongPrototype.greaterThanOrEqual;
/**
* Compares this Long's value with the specified's.
* @this {!Long}
* @param {!Long|number|bigint|string} other Other value
* @returns {number} 0 if they are the same, 1 if the this is greater and -1
*  if the given one is greater
*/
LongPrototype.compare = function compare(other) {
	if (!isLong(other)) other = fromValue(other);
	if (this.eq(other)) return 0;
	var thisNeg = this.isNegative(), otherNeg = other.isNegative();
	if (thisNeg && !otherNeg) return -1;
	if (!thisNeg && otherNeg) return 1;
	if (!this.unsigned) return this.sub(other).isNegative() ? -1 : 1;
	return other.high >>> 0 > this.high >>> 0 || other.high === this.high && other.low >>> 0 > this.low >>> 0 ? -1 : 1;
};
/**
* Compares this Long's value with the specified's. This is an alias of {@link Long#compare}.
* @function
* @param {!Long|number|bigint|string} other Other value
* @returns {number} 0 if they are the same, 1 if the this is greater and -1
*  if the given one is greater
*/
LongPrototype.comp = LongPrototype.compare;
/**
* Negates this Long's value.
* @this {!Long}
* @returns {!Long} Negated Long
*/
LongPrototype.negate = function negate() {
	if (!this.unsigned && this.eq(MIN_VALUE)) return MIN_VALUE;
	return this.not().add(ONE);
};
/**
* Negates this Long's value. This is an alias of {@link Long#negate}.
* @function
* @returns {!Long} Negated Long
*/
LongPrototype.neg = LongPrototype.negate;
/**
* Returns the sum of this and the specified Long.
* @this {!Long}
* @param {!Long|number|bigint|string} addend Addend
* @returns {!Long} Sum
*/
LongPrototype.add = function add(addend) {
	if (!isLong(addend)) addend = fromValue(addend);
	var a48 = this.high >>> 16;
	var a32 = this.high & 65535;
	var a16 = this.low >>> 16;
	var a00 = this.low & 65535;
	var b48 = addend.high >>> 16;
	var b32 = addend.high & 65535;
	var b16 = addend.low >>> 16;
	var b00 = addend.low & 65535;
	var c48 = 0, c32 = 0, c16 = 0, c00 = 0;
	c00 += a00 + b00;
	c16 += c00 >>> 16;
	c00 &= 65535;
	c16 += a16 + b16;
	c32 += c16 >>> 16;
	c16 &= 65535;
	c32 += a32 + b32;
	c48 += c32 >>> 16;
	c32 &= 65535;
	c48 += a48 + b48;
	c48 &= 65535;
	return fromBits(c16 << 16 | c00, c48 << 16 | c32, this.unsigned);
};
/**
* Returns the difference of this and the specified Long.
* @this {!Long}
* @param {!Long|number|bigint|string} subtrahend Subtrahend
* @returns {!Long} Difference
*/
LongPrototype.subtract = function subtract(subtrahend) {
	if (!isLong(subtrahend)) subtrahend = fromValue(subtrahend);
	return this.add(subtrahend.neg());
};
/**
* Returns the difference of this and the specified Long. This is an alias of {@link Long#subtract}.
* @function
* @param {!Long|number|bigint|string} subtrahend Subtrahend
* @returns {!Long} Difference
*/
LongPrototype.sub = LongPrototype.subtract;
/**
* Returns the product of this and the specified Long.
* @this {!Long}
* @param {!Long|number|bigint|string} multiplier Multiplier
* @returns {!Long} Product
*/
LongPrototype.multiply = function multiply(multiplier) {
	if (this.isZero()) return this;
	if (!isLong(multiplier)) multiplier = fromValue(multiplier);
	if (wasm) return fromBits(wasm["mul"](this.low, this.high, multiplier.low, multiplier.high), wasm["get_high"](), this.unsigned);
	if (multiplier.isZero()) return this.unsigned ? UZERO : ZERO;
	if (this.eq(MIN_VALUE)) return multiplier.isOdd() ? MIN_VALUE : ZERO;
	if (multiplier.eq(MIN_VALUE)) return this.isOdd() ? MIN_VALUE : ZERO;
	if (this.isNegative()) if (multiplier.isNegative()) return this.neg().mul(multiplier.neg());
	else return this.neg().mul(multiplier).neg();
	else if (multiplier.isNegative()) return this.mul(multiplier.neg()).neg();
	if (this.lt(TWO_PWR_24) && multiplier.lt(TWO_PWR_24)) return fromNumber(this.toNumber() * multiplier.toNumber(), this.unsigned);
	var a48 = this.high >>> 16;
	var a32 = this.high & 65535;
	var a16 = this.low >>> 16;
	var a00 = this.low & 65535;
	var b48 = multiplier.high >>> 16;
	var b32 = multiplier.high & 65535;
	var b16 = multiplier.low >>> 16;
	var b00 = multiplier.low & 65535;
	var c48 = 0, c32 = 0, c16 = 0, c00 = 0;
	c00 += a00 * b00;
	c16 += c00 >>> 16;
	c00 &= 65535;
	c16 += a16 * b00;
	c32 += c16 >>> 16;
	c16 &= 65535;
	c16 += a00 * b16;
	c32 += c16 >>> 16;
	c16 &= 65535;
	c32 += a32 * b00;
	c48 += c32 >>> 16;
	c32 &= 65535;
	c32 += a16 * b16;
	c48 += c32 >>> 16;
	c32 &= 65535;
	c32 += a00 * b32;
	c48 += c32 >>> 16;
	c32 &= 65535;
	c48 += a48 * b00 + a32 * b16 + a16 * b32 + a00 * b48;
	c48 &= 65535;
	return fromBits(c16 << 16 | c00, c48 << 16 | c32, this.unsigned);
};
/**
* Returns the product of this and the specified Long. This is an alias of {@link Long#multiply}.
* @function
* @param {!Long|number|bigint|string} multiplier Multiplier
* @returns {!Long} Product
*/
LongPrototype.mul = LongPrototype.multiply;
/**
* Returns this Long divided by the specified. The result is signed if this Long is signed or
*  unsigned if this Long is unsigned.
* @this {!Long}
* @param {!Long|number|bigint|string} divisor Divisor
* @returns {!Long} Quotient
*/
LongPrototype.divide = function divide(divisor) {
	if (!isLong(divisor)) divisor = fromValue(divisor);
	if (divisor.isZero()) throw Error("division by zero");
	if (wasm) {
		if (!this.unsigned && this.high === -2147483648 && divisor.low === -1 && divisor.high === -1) return this;
		return fromBits((this.unsigned ? wasm["div_u"] : wasm["div_s"])(this.low, this.high, divisor.low, divisor.high), wasm["get_high"](), this.unsigned);
	}
	if (this.isZero()) return this.unsigned ? UZERO : ZERO;
	var approx, rem, res;
	if (!this.unsigned) {
		if (this.eq(MIN_VALUE)) if (divisor.eq(ONE) || divisor.eq(NEG_ONE)) return MIN_VALUE;
		else if (divisor.eq(MIN_VALUE)) return ONE;
		else {
			approx = this.shr(1).div(divisor).shl(1);
			if (approx.eq(ZERO)) return divisor.isNegative() ? ONE : NEG_ONE;
			else {
				rem = this.sub(divisor.mul(approx));
				res = approx.add(rem.div(divisor));
				return res;
			}
		}
		else if (divisor.eq(MIN_VALUE)) return this.unsigned ? UZERO : ZERO;
		if (this.isNegative()) {
			if (divisor.isNegative()) return this.neg().div(divisor.neg());
			return this.neg().div(divisor).neg();
		} else if (divisor.isNegative()) return this.div(divisor.neg()).neg();
		res = ZERO;
	} else {
		if (!divisor.unsigned) divisor = divisor.toUnsigned();
		if (divisor.gt(this)) return UZERO;
		if (divisor.gt(this.shru(1))) return UONE;
		res = UZERO;
	}
	rem = this;
	while (rem.gte(divisor)) {
		approx = Math.max(1, Math.floor(rem.toNumber() / divisor.toNumber()));
		var log2 = Math.ceil(Math.log(approx) / Math.LN2), delta = log2 <= 48 ? 1 : pow_dbl(2, log2 - 48), approxRes = fromNumber(approx), approxRem = approxRes.mul(divisor);
		while (approxRem.isNegative() || approxRem.gt(rem)) {
			approx -= delta;
			approxRes = fromNumber(approx, this.unsigned);
			approxRem = approxRes.mul(divisor);
		}
		if (approxRes.isZero()) approxRes = ONE;
		res = res.add(approxRes);
		rem = rem.sub(approxRem);
	}
	return res;
};
/**
* Returns this Long divided by the specified. This is an alias of {@link Long#divide}.
* @function
* @param {!Long|number|bigint|string} divisor Divisor
* @returns {!Long} Quotient
*/
LongPrototype.div = LongPrototype.divide;
/**
* Returns this Long modulo the specified.
* @this {!Long}
* @param {!Long|number|bigint|string} divisor Divisor
* @returns {!Long} Remainder
*/
LongPrototype.modulo = function modulo(divisor) {
	if (!isLong(divisor)) divisor = fromValue(divisor);
	if (wasm) return fromBits((this.unsigned ? wasm["rem_u"] : wasm["rem_s"])(this.low, this.high, divisor.low, divisor.high), wasm["get_high"](), this.unsigned);
	return this.sub(this.div(divisor).mul(divisor));
};
/**
* Returns this Long modulo the specified. This is an alias of {@link Long#modulo}.
* @function
* @param {!Long|number|bigint|string} divisor Divisor
* @returns {!Long} Remainder
*/
LongPrototype.mod = LongPrototype.modulo;
/**
* Returns this Long modulo the specified. This is an alias of {@link Long#modulo}.
* @function
* @param {!Long|number|bigint|string} divisor Divisor
* @returns {!Long} Remainder
*/
LongPrototype.rem = LongPrototype.modulo;
/**
* Returns the bitwise NOT of this Long.
* @this {!Long}
* @returns {!Long}
*/
LongPrototype.not = function not() {
	return fromBits(~this.low, ~this.high, this.unsigned);
};
/**
* Returns count leading zeros of this Long.
* @this {!Long}
* @returns {!number}
*/
LongPrototype.countLeadingZeros = function countLeadingZeros() {
	return this.high ? Math.clz32(this.high) : Math.clz32(this.low) + 32;
};
/**
* Returns count leading zeros. This is an alias of {@link Long#countLeadingZeros}.
* @function
* @param {!Long}
* @returns {!number}
*/
LongPrototype.clz = LongPrototype.countLeadingZeros;
/**
* Returns count trailing zeros of this Long.
* @this {!Long}
* @returns {!number}
*/
LongPrototype.countTrailingZeros = function countTrailingZeros() {
	return this.low ? ctz32(this.low) : ctz32(this.high) + 32;
};
/**
* Returns count trailing zeros. This is an alias of {@link Long#countTrailingZeros}.
* @function
* @param {!Long}
* @returns {!number}
*/
LongPrototype.ctz = LongPrototype.countTrailingZeros;
/**
* Returns the bitwise AND of this Long and the specified.
* @this {!Long}
* @param {!Long|number|bigint|string} other Other Long
* @returns {!Long}
*/
LongPrototype.and = function and(other) {
	if (!isLong(other)) other = fromValue(other);
	return fromBits(this.low & other.low, this.high & other.high, this.unsigned);
};
/**
* Returns the bitwise OR of this Long and the specified.
* @this {!Long}
* @param {!Long|number|bigint|string} other Other Long
* @returns {!Long}
*/
LongPrototype.or = function or(other) {
	if (!isLong(other)) other = fromValue(other);
	return fromBits(this.low | other.low, this.high | other.high, this.unsigned);
};
/**
* Returns the bitwise XOR of this Long and the given one.
* @this {!Long}
* @param {!Long|number|bigint|string} other Other Long
* @returns {!Long}
*/
LongPrototype.xor = function xor(other) {
	if (!isLong(other)) other = fromValue(other);
	return fromBits(this.low ^ other.low, this.high ^ other.high, this.unsigned);
};
/**
* Returns this Long with bits shifted to the left by the given amount.
* @this {!Long}
* @param {number|!Long} numBits Number of bits
* @returns {!Long} Shifted Long
*/
LongPrototype.shiftLeft = function shiftLeft(numBits) {
	if (isLong(numBits)) numBits = numBits.toInt();
	if ((numBits &= 63) === 0) return this;
	else if (numBits < 32) return fromBits(this.low << numBits, this.high << numBits | this.low >>> 32 - numBits, this.unsigned);
	else return fromBits(0, this.low << numBits - 32, this.unsigned);
};
/**
* Returns this Long with bits shifted to the left by the given amount. This is an alias of {@link Long#shiftLeft}.
* @function
* @param {number|!Long} numBits Number of bits
* @returns {!Long} Shifted Long
*/
LongPrototype.shl = LongPrototype.shiftLeft;
/**
* Returns this Long with bits arithmetically shifted to the right by the given amount.
* @this {!Long}
* @param {number|!Long} numBits Number of bits
* @returns {!Long} Shifted Long
*/
LongPrototype.shiftRight = function shiftRight(numBits) {
	if (isLong(numBits)) numBits = numBits.toInt();
	if ((numBits &= 63) === 0) return this;
	else if (numBits < 32) return fromBits(this.low >>> numBits | this.high << 32 - numBits, this.high >> numBits, this.unsigned);
	else return fromBits(this.high >> numBits - 32, this.high >= 0 ? 0 : -1, this.unsigned);
};
/**
* Returns this Long with bits arithmetically shifted to the right by the given amount. This is an alias of {@link Long#shiftRight}.
* @function
* @param {number|!Long} numBits Number of bits
* @returns {!Long} Shifted Long
*/
LongPrototype.shr = LongPrototype.shiftRight;
/**
* Returns this Long with bits logically shifted to the right by the given amount.
* @this {!Long}
* @param {number|!Long} numBits Number of bits
* @returns {!Long} Shifted Long
*/
LongPrototype.shiftRightUnsigned = function shiftRightUnsigned(numBits) {
	if (isLong(numBits)) numBits = numBits.toInt();
	if ((numBits &= 63) === 0) return this;
	if (numBits < 32) return fromBits(this.low >>> numBits | this.high << 32 - numBits, this.high >>> numBits, this.unsigned);
	if (numBits === 32) return fromBits(this.high, 0, this.unsigned);
	return fromBits(this.high >>> numBits - 32, 0, this.unsigned);
};
/**
* Returns this Long with bits logically shifted to the right by the given amount. This is an alias of {@link Long#shiftRightUnsigned}.
* @function
* @param {number|!Long} numBits Number of bits
* @returns {!Long} Shifted Long
*/
LongPrototype.shru = LongPrototype.shiftRightUnsigned;
/**
* Returns this Long with bits logically shifted to the right by the given amount. This is an alias of {@link Long#shiftRightUnsigned}.
* @function
* @param {number|!Long} numBits Number of bits
* @returns {!Long} Shifted Long
*/
LongPrototype.shr_u = LongPrototype.shiftRightUnsigned;
/**
* Returns this Long with bits rotated to the left by the given amount.
* @this {!Long}
* @param {number|!Long} numBits Number of bits
* @returns {!Long} Rotated Long
*/
LongPrototype.rotateLeft = function rotateLeft(numBits) {
	var b;
	if (isLong(numBits)) numBits = numBits.toInt();
	if ((numBits &= 63) === 0) return this;
	if (numBits === 32) return fromBits(this.high, this.low, this.unsigned);
	if (numBits < 32) {
		b = 32 - numBits;
		return fromBits(this.low << numBits | this.high >>> b, this.high << numBits | this.low >>> b, this.unsigned);
	}
	numBits -= 32;
	b = 32 - numBits;
	return fromBits(this.high << numBits | this.low >>> b, this.low << numBits | this.high >>> b, this.unsigned);
};
/**
* Returns this Long with bits rotated to the left by the given amount. This is an alias of {@link Long#rotateLeft}.
* @function
* @param {number|!Long} numBits Number of bits
* @returns {!Long} Rotated Long
*/
LongPrototype.rotl = LongPrototype.rotateLeft;
/**
* Returns this Long with bits rotated to the right by the given amount.
* @this {!Long}
* @param {number|!Long} numBits Number of bits
* @returns {!Long} Rotated Long
*/
LongPrototype.rotateRight = function rotateRight(numBits) {
	var b;
	if (isLong(numBits)) numBits = numBits.toInt();
	if ((numBits &= 63) === 0) return this;
	if (numBits === 32) return fromBits(this.high, this.low, this.unsigned);
	if (numBits < 32) {
		b = 32 - numBits;
		return fromBits(this.high << b | this.low >>> numBits, this.low << b | this.high >>> numBits, this.unsigned);
	}
	numBits -= 32;
	b = 32 - numBits;
	return fromBits(this.low << b | this.high >>> numBits, this.high << b | this.low >>> numBits, this.unsigned);
};
/**
* Returns this Long with bits rotated to the right by the given amount. This is an alias of {@link Long#rotateRight}.
* @function
* @param {number|!Long} numBits Number of bits
* @returns {!Long} Rotated Long
*/
LongPrototype.rotr = LongPrototype.rotateRight;
/**
* Converts this Long to signed.
* @this {!Long}
* @returns {!Long} Signed long
*/
LongPrototype.toSigned = function toSigned() {
	if (!this.unsigned) return this;
	return fromBits(this.low, this.high, false);
};
/**
* Converts this Long to unsigned.
* @this {!Long}
* @returns {!Long} Unsigned long
*/
LongPrototype.toUnsigned = function toUnsigned() {
	if (this.unsigned) return this;
	return fromBits(this.low, this.high, true);
};
/**
* Converts this Long to its byte representation.
* @param {boolean=} le Whether little or big endian, defaults to big endian
* @this {!Long}
* @returns {!Array.<number>} Byte representation
*/
LongPrototype.toBytes = function toBytes(le) {
	return le ? this.toBytesLE() : this.toBytesBE();
};
/**
* Converts this Long to its little endian byte representation.
* @this {!Long}
* @returns {!Array.<number>} Little endian byte representation
*/
LongPrototype.toBytesLE = function toBytesLE() {
	var hi = this.high, lo = this.low;
	return [
		lo & 255,
		lo >>> 8 & 255,
		lo >>> 16 & 255,
		lo >>> 24,
		hi & 255,
		hi >>> 8 & 255,
		hi >>> 16 & 255,
		hi >>> 24
	];
};
/**
* Converts this Long to its big endian byte representation.
* @this {!Long}
* @returns {!Array.<number>} Big endian byte representation
*/
LongPrototype.toBytesBE = function toBytesBE() {
	var hi = this.high, lo = this.low;
	return [
		hi >>> 24,
		hi >>> 16 & 255,
		hi >>> 8 & 255,
		hi & 255,
		lo >>> 24,
		lo >>> 16 & 255,
		lo >>> 8 & 255,
		lo & 255
	];
};
/**
* Creates a Long from its byte representation.
* @param {!Array.<number>} bytes Byte representation
* @param {boolean=} unsigned Whether unsigned or not, defaults to signed
* @param {boolean=} le Whether little or big endian, defaults to big endian
* @returns {Long} The corresponding Long value
*/
Long.fromBytes = function fromBytes(bytes, unsigned, le) {
	return le ? Long.fromBytesLE(bytes, unsigned) : Long.fromBytesBE(bytes, unsigned);
};
/**
* Creates a Long from its little endian byte representation.
* @param {!Array.<number>} bytes Little endian byte representation
* @param {boolean=} unsigned Whether unsigned or not, defaults to signed
* @returns {Long} The corresponding Long value
*/
Long.fromBytesLE = function fromBytesLE(bytes, unsigned) {
	return new Long(bytes[0] | bytes[1] << 8 | bytes[2] << 16 | bytes[3] << 24, bytes[4] | bytes[5] << 8 | bytes[6] << 16 | bytes[7] << 24, unsigned);
};
/**
* Creates a Long from its big endian byte representation.
* @param {!Array.<number>} bytes Big endian byte representation
* @param {boolean=} unsigned Whether unsigned or not, defaults to signed
* @returns {Long} The corresponding Long value
*/
Long.fromBytesBE = function fromBytesBE(bytes, unsigned) {
	return new Long(bytes[4] << 24 | bytes[5] << 16 | bytes[6] << 8 | bytes[7], bytes[0] << 24 | bytes[1] << 16 | bytes[2] << 8 | bytes[3], unsigned);
};
if (typeof BigInt === "function") {
	/**
	* Returns a Long representing the given big integer.
	* @function
	* @param {number} value The big integer value
	* @param {boolean=} unsigned Whether unsigned or not, defaults to signed
	* @returns {!Long} The corresponding Long value
	*/
	Long.fromBigInt = function fromBigInt(value, unsigned) {
		return fromBits(Number(BigInt.asIntN(32, value)), Number(BigInt.asIntN(32, value >> BigInt(32))), unsigned);
	};
	Long.fromValue = function fromValueWithBigInt(value, unsigned) {
		if (typeof value === "bigint") return Long.fromBigInt(value, unsigned);
		return fromValue(value, unsigned);
	};
	/**
	* Converts the Long to its big integer representation.
	* @this {!Long}
	* @returns {bigint}
	*/
	LongPrototype.toBigInt = function toBigInt() {
		var lowBigInt = BigInt(this.low >>> 0);
		return BigInt(this.unsigned ? this.high >>> 0 : this.high) << BigInt(32) | lowBigInt;
	};
}
//#endregion
//#region crystal-cascade/node_modules/@devvit/protos/types/google/protobuf/timestamp.js
/**
* #timestamp.ts
*
* Code generated by ts-proto. DO NOT EDIT.
* @packageDocumentation
*/
function createBaseTimestamp() {
	return {
		seconds: 0,
		nanos: 0
	};
}
var Timestamp = {
	$type: "google.protobuf.Timestamp",
	encode(message, writer = import_minimal.default.Writer.create()) {
		if (message.seconds !== 0) writer.uint32(8).int64(message.seconds);
		if (message.nanos !== 0) writer.uint32(16).int32(message.nanos);
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseTimestamp();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {
				case 1:
					if (tag !== 8) break;
					message.seconds = longToNumber(reader.int64());
					continue;
				case 2:
					if (tag !== 16) break;
					message.nanos = reader.int32();
					continue;
			}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(object) {
		return {
			seconds: isSet$13(object.seconds) ? globalThis.Number(object.seconds) : 0,
			nanos: isSet$13(object.nanos) ? globalThis.Number(object.nanos) : 0
		};
	},
	toJSON(message) {
		const obj = {};
		if (message.seconds !== 0) obj.seconds = Math.round(message.seconds);
		if (message.nanos !== 0) obj.nanos = Math.round(message.nanos);
		return obj;
	},
	create(base) {
		return Timestamp.fromPartial(base ?? {});
	},
	fromPartial(object) {
		const message = createBaseTimestamp();
		message.seconds = object.seconds ?? 0;
		message.nanos = object.nanos ?? 0;
		return message;
	}
};
messageTypeRegistry.set(Timestamp.$type, Timestamp);
function longToNumber(long) {
	if (long.gt(globalThis.Number.MAX_SAFE_INTEGER)) throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
	return long.toNumber();
}
if (import_minimal.default.util.Long !== Long) {
	import_minimal.default.util.Long = Long;
	import_minimal.default.configure();
}
function isSet$13(value) {
	return value !== null && value !== void 0;
}
//#endregion
//#region crystal-cascade/node_modules/@devvit/protos/types/reddit/devvit/app_permission/v1/app_permission.js
/**
* #app_permission.ts
*
* Code generated by ts-proto. DO NOT EDIT.
* @packageDocumentation
*/
/** This enum is used to represent the consent status of an app permission. */
var ConsentStatus$1;
(function(ConsentStatus) {
	/** CONSENT_STATUS_UNKNOWN - The consent status is unknown, which means it has not been set or is not applicable. */
	ConsentStatus[ConsentStatus["CONSENT_STATUS_UNKNOWN"] = 0] = "CONSENT_STATUS_UNKNOWN";
	/** REVOKED - The user has explicitly denied consent for the app permissions. */
	ConsentStatus[ConsentStatus["REVOKED"] = 1] = "REVOKED";
	/** GRANTED - The user has granted consent for the app permissions. */
	ConsentStatus[ConsentStatus["GRANTED"] = 2] = "GRANTED";
	ConsentStatus[ConsentStatus["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(ConsentStatus$1 || (ConsentStatus$1 = {}));
function consentStatusFromJSON(object) {
	switch (object) {
		case 0:
		case "CONSENT_STATUS_UNKNOWN": return ConsentStatus$1.CONSENT_STATUS_UNKNOWN;
		case 1:
		case "REVOKED": return ConsentStatus$1.REVOKED;
		case 2:
		case "GRANTED": return ConsentStatus$1.GRANTED;
		default: return ConsentStatus$1.UNRECOGNIZED;
	}
}
function consentStatusToJSON(object) {
	switch (object) {
		case ConsentStatus$1.CONSENT_STATUS_UNKNOWN: return 0;
		case ConsentStatus$1.REVOKED: return 1;
		case ConsentStatus$1.GRANTED: return 2;
		case ConsentStatus$1.UNRECOGNIZED:
		default: return -1;
	}
}
/** This enum is used to represent the scopes of permissions that can be granted to an app. */
var Scope$1;
(function(Scope) {
	/** SCOPE_UNKNOWN - The scope is unknown, which means it has not been set or is not applicable. */
	Scope[Scope["SCOPE_UNKNOWN"] = 0] = "SCOPE_UNKNOWN";
	/** SUBMIT_POST - Allows the app to submit posts on behalf of the user. */
	Scope[Scope["SUBMIT_POST"] = 1] = "SUBMIT_POST";
	/** SUBMIT_COMMENT - Allows the app to submit comments on behalf of the user. */
	Scope[Scope["SUBMIT_COMMENT"] = 2] = "SUBMIT_COMMENT";
	/** SUBSCRIBE_TO_SUBREDDIT - Allows the app to subscribe the user to a subreddit. */
	Scope[Scope["SUBSCRIBE_TO_SUBREDDIT"] = 3] = "SUBSCRIBE_TO_SUBREDDIT";
	Scope[Scope["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(Scope$1 || (Scope$1 = {}));
function scopeFromJSON(object) {
	switch (object) {
		case 0:
		case "SCOPE_UNKNOWN": return Scope$1.SCOPE_UNKNOWN;
		case 1:
		case "SUBMIT_POST": return Scope$1.SUBMIT_POST;
		case 2:
		case "SUBMIT_COMMENT": return Scope$1.SUBMIT_COMMENT;
		case 3:
		case "SUBSCRIBE_TO_SUBREDDIT": return Scope$1.SUBSCRIBE_TO_SUBREDDIT;
		default: return Scope$1.UNRECOGNIZED;
	}
}
function scopeToJSON(object) {
	switch (object) {
		case Scope$1.SCOPE_UNKNOWN: return 0;
		case Scope$1.SUBMIT_POST: return 1;
		case Scope$1.SUBMIT_COMMENT: return 2;
		case Scope$1.SUBSCRIBE_TO_SUBREDDIT: return 3;
		case Scope$1.UNRECOGNIZED:
		default: return -1;
	}
}
function createBaseAppPermission() {
	return {
		appSlug: "",
		subredditId: "",
		scopes: [],
		consentStatus: 0,
		updatedAt: void 0
	};
}
var AppPermission = {
	$type: "reddit.devvit.app_permission.v1.AppPermission",
	encode(message, writer = import_minimal.default.Writer.create()) {
		if (message.appSlug !== "") writer.uint32(10).string(message.appSlug);
		if (message.subredditId !== "") writer.uint32(18).string(message.subredditId);
		writer.uint32(26).fork();
		for (const v of message.scopes) writer.int32(v);
		writer.ldelim();
		if (message.consentStatus !== 0) writer.uint32(32).int32(message.consentStatus);
		if (message.updatedAt !== void 0) Timestamp.encode(toTimestamp(message.updatedAt), writer.uint32(42).fork()).ldelim();
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseAppPermission();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {
				case 1:
					if (tag !== 10) break;
					message.appSlug = reader.string();
					continue;
				case 2:
					if (tag !== 18) break;
					message.subredditId = reader.string();
					continue;
				case 3:
					if (tag === 24) {
						message.scopes.push(reader.int32());
						continue;
					}
					if (tag === 26) {
						const end2 = reader.uint32() + reader.pos;
						while (reader.pos < end2) message.scopes.push(reader.int32());
						continue;
					}
					break;
				case 4:
					if (tag !== 32) break;
					message.consentStatus = reader.int32();
					continue;
				case 5:
					if (tag !== 42) break;
					message.updatedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
					continue;
			}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(object) {
		return {
			appSlug: isSet$12(object.appSlug) ? globalThis.String(object.appSlug) : "",
			subredditId: isSet$12(object.subredditId) ? globalThis.String(object.subredditId) : "",
			scopes: globalThis.Array.isArray(object?.scopes) ? object.scopes.map((e) => scopeFromJSON(e)) : [],
			consentStatus: isSet$12(object.consentStatus) ? consentStatusFromJSON(object.consentStatus) : 0,
			updatedAt: isSet$12(object.updatedAt) ? fromJsonTimestamp(object.updatedAt) : void 0
		};
	},
	toJSON(message) {
		const obj = {};
		if (message.appSlug !== "") obj.appSlug = message.appSlug;
		if (message.subredditId !== "") obj.subredditId = message.subredditId;
		if (message.scopes?.length) obj.scopes = message.scopes.map((e) => scopeToJSON(e));
		if (message.consentStatus !== 0) obj.consentStatus = consentStatusToJSON(message.consentStatus);
		if (message.updatedAt !== void 0) obj.updatedAt = message.updatedAt.toISOString();
		return obj;
	},
	create(base) {
		return AppPermission.fromPartial(base ?? {});
	},
	fromPartial(object) {
		const message = createBaseAppPermission();
		message.appSlug = object.appSlug ?? "";
		message.subredditId = object.subredditId ?? "";
		message.scopes = object.scopes?.map((e) => e) || [];
		message.consentStatus = object.consentStatus ?? 0;
		message.updatedAt = object.updatedAt ?? void 0;
		return message;
	}
};
messageTypeRegistry.set(AppPermission.$type, AppPermission);
function createBaseDevvitApp() {
	return {
		appSlug: "",
		appName: "",
		termsAndConditions: void 0,
		privacyPolicy: void 0
	};
}
var DevvitApp = {
	$type: "reddit.devvit.app_permission.v1.DevvitApp",
	encode(message, writer = import_minimal.default.Writer.create()) {
		if (message.appSlug !== "") writer.uint32(10).string(message.appSlug);
		if (message.appName !== "") writer.uint32(18).string(message.appName);
		if (message.termsAndConditions !== void 0) writer.uint32(26).string(message.termsAndConditions);
		if (message.privacyPolicy !== void 0) writer.uint32(34).string(message.privacyPolicy);
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseDevvitApp();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {
				case 1:
					if (tag !== 10) break;
					message.appSlug = reader.string();
					continue;
				case 2:
					if (tag !== 18) break;
					message.appName = reader.string();
					continue;
				case 3:
					if (tag !== 26) break;
					message.termsAndConditions = reader.string();
					continue;
				case 4:
					if (tag !== 34) break;
					message.privacyPolicy = reader.string();
					continue;
			}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(object) {
		return {
			appSlug: isSet$12(object.appSlug) ? globalThis.String(object.appSlug) : "",
			appName: isSet$12(object.appName) ? globalThis.String(object.appName) : "",
			termsAndConditions: isSet$12(object.termsAndConditions) ? globalThis.String(object.termsAndConditions) : void 0,
			privacyPolicy: isSet$12(object.privacyPolicy) ? globalThis.String(object.privacyPolicy) : void 0
		};
	},
	toJSON(message) {
		const obj = {};
		if (message.appSlug !== "") obj.appSlug = message.appSlug;
		if (message.appName !== "") obj.appName = message.appName;
		if (message.termsAndConditions !== void 0) obj.termsAndConditions = message.termsAndConditions;
		if (message.privacyPolicy !== void 0) obj.privacyPolicy = message.privacyPolicy;
		return obj;
	},
	create(base) {
		return DevvitApp.fromPartial(base ?? {});
	},
	fromPartial(object) {
		const message = createBaseDevvitApp();
		message.appSlug = object.appSlug ?? "";
		message.appName = object.appName ?? "";
		message.termsAndConditions = object.termsAndConditions ?? void 0;
		message.privacyPolicy = object.privacyPolicy ?? void 0;
		return message;
	}
};
messageTypeRegistry.set(DevvitApp.$type, DevvitApp);
function createBaseGetAppPermissionsByUserIdRequest() {
	return { userId: "" };
}
var GetAppPermissionsByUserIdRequest = {
	$type: "reddit.devvit.app_permission.v1.GetAppPermissionsByUserIdRequest",
	encode(message, writer = import_minimal.default.Writer.create()) {
		if (message.userId !== "") writer.uint32(10).string(message.userId);
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseGetAppPermissionsByUserIdRequest();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {
				case 1:
					if (tag !== 10) break;
					message.userId = reader.string();
					continue;
			}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(object) {
		return { userId: isSet$12(object.userId) ? globalThis.String(object.userId) : "" };
	},
	toJSON(message) {
		const obj = {};
		if (message.userId !== "") obj.userId = message.userId;
		return obj;
	},
	create(base) {
		return GetAppPermissionsByUserIdRequest.fromPartial(base ?? {});
	},
	fromPartial(object) {
		const message = createBaseGetAppPermissionsByUserIdRequest();
		message.userId = object.userId ?? "";
		return message;
	}
};
messageTypeRegistry.set(GetAppPermissionsByUserIdRequest.$type, GetAppPermissionsByUserIdRequest);
function createBaseGetAppPermissionsByUserIdResponse() {
	return {
		appPermissions: [],
		devvitApps: []
	};
}
var GetAppPermissionsByUserIdResponse = {
	$type: "reddit.devvit.app_permission.v1.GetAppPermissionsByUserIdResponse",
	encode(message, writer = import_minimal.default.Writer.create()) {
		for (const v of message.appPermissions) AppPermission.encode(v, writer.uint32(10).fork()).ldelim();
		for (const v of message.devvitApps) DevvitApp.encode(v, writer.uint32(18).fork()).ldelim();
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseGetAppPermissionsByUserIdResponse();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {
				case 1:
					if (tag !== 10) break;
					message.appPermissions.push(AppPermission.decode(reader, reader.uint32()));
					continue;
				case 2:
					if (tag !== 18) break;
					message.devvitApps.push(DevvitApp.decode(reader, reader.uint32()));
					continue;
			}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(object) {
		return {
			appPermissions: globalThis.Array.isArray(object?.appPermissions) ? object.appPermissions.map((e) => AppPermission.fromJSON(e)) : [],
			devvitApps: globalThis.Array.isArray(object?.devvitApps) ? object.devvitApps.map((e) => DevvitApp.fromJSON(e)) : []
		};
	},
	toJSON(message) {
		const obj = {};
		if (message.appPermissions?.length) obj.appPermissions = message.appPermissions.map((e) => AppPermission.toJSON(e));
		if (message.devvitApps?.length) obj.devvitApps = message.devvitApps.map((e) => DevvitApp.toJSON(e));
		return obj;
	},
	create(base) {
		return GetAppPermissionsByUserIdResponse.fromPartial(base ?? {});
	},
	fromPartial(object) {
		const message = createBaseGetAppPermissionsByUserIdResponse();
		message.appPermissions = object.appPermissions?.map((e) => AppPermission.fromPartial(e)) || [];
		message.devvitApps = object.devvitApps?.map((e) => DevvitApp.fromPartial(e)) || [];
		return message;
	}
};
messageTypeRegistry.set(GetAppPermissionsByUserIdResponse.$type, GetAppPermissionsByUserIdResponse);
function createBaseGrantAppPermissionRequest() {
	return {
		userId: "",
		appSlug: "",
		subredditId: "",
		scopes: []
	};
}
var GrantAppPermissionRequest = {
	$type: "reddit.devvit.app_permission.v1.GrantAppPermissionRequest",
	encode(message, writer = import_minimal.default.Writer.create()) {
		if (message.userId !== "") writer.uint32(10).string(message.userId);
		if (message.appSlug !== "") writer.uint32(18).string(message.appSlug);
		if (message.subredditId !== "") writer.uint32(26).string(message.subredditId);
		writer.uint32(34).fork();
		for (const v of message.scopes) writer.int32(v);
		writer.ldelim();
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseGrantAppPermissionRequest();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {
				case 1:
					if (tag !== 10) break;
					message.userId = reader.string();
					continue;
				case 2:
					if (tag !== 18) break;
					message.appSlug = reader.string();
					continue;
				case 3:
					if (tag !== 26) break;
					message.subredditId = reader.string();
					continue;
				case 4:
					if (tag === 32) {
						message.scopes.push(reader.int32());
						continue;
					}
					if (tag === 34) {
						const end2 = reader.uint32() + reader.pos;
						while (reader.pos < end2) message.scopes.push(reader.int32());
						continue;
					}
					break;
			}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(object) {
		return {
			userId: isSet$12(object.userId) ? globalThis.String(object.userId) : "",
			appSlug: isSet$12(object.appSlug) ? globalThis.String(object.appSlug) : "",
			subredditId: isSet$12(object.subredditId) ? globalThis.String(object.subredditId) : "",
			scopes: globalThis.Array.isArray(object?.scopes) ? object.scopes.map((e) => scopeFromJSON(e)) : []
		};
	},
	toJSON(message) {
		const obj = {};
		if (message.userId !== "") obj.userId = message.userId;
		if (message.appSlug !== "") obj.appSlug = message.appSlug;
		if (message.subredditId !== "") obj.subredditId = message.subredditId;
		if (message.scopes?.length) obj.scopes = message.scopes.map((e) => scopeToJSON(e));
		return obj;
	},
	create(base) {
		return GrantAppPermissionRequest.fromPartial(base ?? {});
	},
	fromPartial(object) {
		const message = createBaseGrantAppPermissionRequest();
		message.userId = object.userId ?? "";
		message.appSlug = object.appSlug ?? "";
		message.subredditId = object.subredditId ?? "";
		message.scopes = object.scopes?.map((e) => e) || [];
		return message;
	}
};
messageTypeRegistry.set(GrantAppPermissionRequest.$type, GrantAppPermissionRequest);
function createBaseGrantAppPermissionResponse() {
	return { error: void 0 };
}
var GrantAppPermissionResponse = {
	$type: "reddit.devvit.app_permission.v1.GrantAppPermissionResponse",
	encode(message, writer = import_minimal.default.Writer.create()) {
		if (message.error !== void 0) ErrorMessage.encode(message.error, writer.uint32(10).fork()).ldelim();
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseGrantAppPermissionResponse();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {
				case 1:
					if (tag !== 10) break;
					message.error = ErrorMessage.decode(reader, reader.uint32());
					continue;
			}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(object) {
		return { error: isSet$12(object.error) ? ErrorMessage.fromJSON(object.error) : void 0 };
	},
	toJSON(message) {
		const obj = {};
		if (message.error !== void 0) obj.error = ErrorMessage.toJSON(message.error);
		return obj;
	},
	create(base) {
		return GrantAppPermissionResponse.fromPartial(base ?? {});
	},
	fromPartial(object) {
		const message = createBaseGrantAppPermissionResponse();
		message.error = object.error !== void 0 && object.error !== null ? ErrorMessage.fromPartial(object.error) : void 0;
		return message;
	}
};
messageTypeRegistry.set(GrantAppPermissionResponse.$type, GrantAppPermissionResponse);
function createBaseRevokeAppPermissionRequest() {
	return {
		appSlug: "",
		userId: "",
		subredditId: ""
	};
}
var RevokeAppPermissionRequest = {
	$type: "reddit.devvit.app_permission.v1.RevokeAppPermissionRequest",
	encode(message, writer = import_minimal.default.Writer.create()) {
		if (message.appSlug !== "") writer.uint32(10).string(message.appSlug);
		if (message.userId !== "") writer.uint32(18).string(message.userId);
		if (message.subredditId !== "") writer.uint32(26).string(message.subredditId);
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseRevokeAppPermissionRequest();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {
				case 1:
					if (tag !== 10) break;
					message.appSlug = reader.string();
					continue;
				case 2:
					if (tag !== 18) break;
					message.userId = reader.string();
					continue;
				case 3:
					if (tag !== 26) break;
					message.subredditId = reader.string();
					continue;
			}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(object) {
		return {
			appSlug: isSet$12(object.appSlug) ? globalThis.String(object.appSlug) : "",
			userId: isSet$12(object.userId) ? globalThis.String(object.userId) : "",
			subredditId: isSet$12(object.subredditId) ? globalThis.String(object.subredditId) : ""
		};
	},
	toJSON(message) {
		const obj = {};
		if (message.appSlug !== "") obj.appSlug = message.appSlug;
		if (message.userId !== "") obj.userId = message.userId;
		if (message.subredditId !== "") obj.subredditId = message.subredditId;
		return obj;
	},
	create(base) {
		return RevokeAppPermissionRequest.fromPartial(base ?? {});
	},
	fromPartial(object) {
		const message = createBaseRevokeAppPermissionRequest();
		message.appSlug = object.appSlug ?? "";
		message.userId = object.userId ?? "";
		message.subredditId = object.subredditId ?? "";
		return message;
	}
};
messageTypeRegistry.set(RevokeAppPermissionRequest.$type, RevokeAppPermissionRequest);
function createBaseRevokeAppPermissionResponse() {
	return { error: void 0 };
}
var RevokeAppPermissionResponse = {
	$type: "reddit.devvit.app_permission.v1.RevokeAppPermissionResponse",
	encode(message, writer = import_minimal.default.Writer.create()) {
		if (message.error !== void 0) ErrorMessage.encode(message.error, writer.uint32(10).fork()).ldelim();
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseRevokeAppPermissionResponse();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {
				case 1:
					if (tag !== 10) break;
					message.error = ErrorMessage.decode(reader, reader.uint32());
					continue;
			}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(object) {
		return { error: isSet$12(object.error) ? ErrorMessage.fromJSON(object.error) : void 0 };
	},
	toJSON(message) {
		const obj = {};
		if (message.error !== void 0) obj.error = ErrorMessage.toJSON(message.error);
		return obj;
	},
	create(base) {
		return RevokeAppPermissionResponse.fromPartial(base ?? {});
	},
	fromPartial(object) {
		const message = createBaseRevokeAppPermissionResponse();
		message.error = object.error !== void 0 && object.error !== null ? ErrorMessage.fromPartial(object.error) : void 0;
		return message;
	}
};
messageTypeRegistry.set(RevokeAppPermissionResponse.$type, RevokeAppPermissionResponse);
function createBaseErrorMessage() {
	return { message: "" };
}
var ErrorMessage = {
	$type: "reddit.devvit.app_permission.v1.ErrorMessage",
	encode(message, writer = import_minimal.default.Writer.create()) {
		if (message.message !== "") writer.uint32(10).string(message.message);
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseErrorMessage();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {
				case 1:
					if (tag !== 10) break;
					message.message = reader.string();
					continue;
			}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(object) {
		return { message: isSet$12(object.message) ? globalThis.String(object.message) : "" };
	},
	toJSON(message) {
		const obj = {};
		if (message.message !== "") obj.message = message.message;
		return obj;
	},
	create(base) {
		return ErrorMessage.fromPartial(base ?? {});
	},
	fromPartial(object) {
		const message = createBaseErrorMessage();
		message.message = object.message ?? "";
		return message;
	}
};
messageTypeRegistry.set(ErrorMessage.$type, ErrorMessage);
function toTimestamp(date) {
	return {
		seconds: Math.trunc(date.getTime() / 1e3),
		nanos: date.getTime() % 1e3 * 1e6
	};
}
function fromTimestamp(t) {
	let millis = (t.seconds || 0) * 1e3;
	millis += (t.nanos || 0) / 1e6;
	return new globalThis.Date(millis);
}
function fromJsonTimestamp(o) {
	if (o instanceof globalThis.Date) return o;
	else if (typeof o === "string") return new globalThis.Date(o);
	else return fromTimestamp(Timestamp.fromJSON(o));
}
function isSet$12(value) {
	return value !== null && value !== void 0;
}
//#endregion
//#region crystal-cascade/node_modules/@devvit/protos/types/devvit/ui/effect_types/v1alpha/app_permission.js
/**
* #app_permission.ts
*
* Code generated by ts-proto. DO NOT EDIT.
* @packageDocumentation
*/
function createBaseCanRunAsUserEffect() {
	return {
		postId: void 0,
		appSlug: void 0,
		subredditId: void 0
	};
}
var CanRunAsUserEffect = {
	$type: "devvit.ui.effect_types.v1alpha.CanRunAsUserEffect",
	encode(message, writer = import_minimal.default.Writer.create()) {
		if (message.postId !== void 0) writer.uint32(10).string(message.postId);
		if (message.appSlug !== void 0) writer.uint32(18).string(message.appSlug);
		if (message.subredditId !== void 0) writer.uint32(26).string(message.subredditId);
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseCanRunAsUserEffect();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {
				case 1:
					if (tag !== 10) break;
					message.postId = reader.string();
					continue;
				case 2:
					if (tag !== 18) break;
					message.appSlug = reader.string();
					continue;
				case 3:
					if (tag !== 26) break;
					message.subredditId = reader.string();
					continue;
			}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(object) {
		return {
			postId: isSet$11(object.postId) ? globalThis.String(object.postId) : void 0,
			appSlug: isSet$11(object.appSlug) ? globalThis.String(object.appSlug) : void 0,
			subredditId: isSet$11(object.subredditId) ? globalThis.String(object.subredditId) : void 0
		};
	},
	toJSON(message) {
		const obj = {};
		if (message.postId !== void 0) obj.postId = message.postId;
		if (message.appSlug !== void 0) obj.appSlug = message.appSlug;
		if (message.subredditId !== void 0) obj.subredditId = message.subredditId;
		return obj;
	},
	create(base) {
		return CanRunAsUserEffect.fromPartial(base ?? {});
	},
	fromPartial(object) {
		const message = createBaseCanRunAsUserEffect();
		message.postId = object.postId ?? void 0;
		message.appSlug = object.appSlug ?? void 0;
		message.subredditId = object.subredditId ?? void 0;
		return message;
	}
};
messageTypeRegistry.set(CanRunAsUserEffect.$type, CanRunAsUserEffect);
function createBaseConsentStatusEvent() {
	return { consentStatus: 0 };
}
var ConsentStatusEvent = {
	$type: "devvit.ui.effect_types.v1alpha.ConsentStatusEvent",
	encode(message, writer = import_minimal.default.Writer.create()) {
		if (message.consentStatus !== 0) writer.uint32(8).int32(message.consentStatus);
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseConsentStatusEvent();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {
				case 1:
					if (tag !== 8) break;
					message.consentStatus = reader.int32();
					continue;
			}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(object) {
		return { consentStatus: isSet$11(object.consentStatus) ? consentStatusFromJSON(object.consentStatus) : 0 };
	},
	toJSON(message) {
		const obj = {};
		if (message.consentStatus !== 0) obj.consentStatus = consentStatusToJSON(message.consentStatus);
		return obj;
	},
	create(base) {
		return ConsentStatusEvent.fromPartial(base ?? {});
	},
	fromPartial(object) {
		const message = createBaseConsentStatusEvent();
		message.consentStatus = object.consentStatus ?? 0;
		return message;
	}
};
messageTypeRegistry.set(ConsentStatusEvent.$type, ConsentStatusEvent);
function isSet$11(value) {
	return value !== null && value !== void 0;
}
//#endregion
//#region crystal-cascade/node_modules/@devvit/protos/types/devvit/ui/effect_types/v1alpha/create_order.js
/**
* #create_order.ts
*
* Code generated by ts-proto. DO NOT EDIT.
* @packageDocumentation
*/
var OrderResultStatus$1;
(function(OrderResultStatus) {
	OrderResultStatus[OrderResultStatus["STATUS_CANCELLED"] = 0] = "STATUS_CANCELLED";
	OrderResultStatus[OrderResultStatus["STATUS_SUCCESS"] = 1] = "STATUS_SUCCESS";
	OrderResultStatus[OrderResultStatus["STATUS_ERROR"] = 2] = "STATUS_ERROR";
	OrderResultStatus[OrderResultStatus["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(OrderResultStatus$1 || (OrderResultStatus$1 = {}));
function orderResultStatusFromJSON(object) {
	switch (object) {
		case 0:
		case "STATUS_CANCELLED": return OrderResultStatus$1.STATUS_CANCELLED;
		case 1:
		case "STATUS_SUCCESS": return OrderResultStatus$1.STATUS_SUCCESS;
		case 2:
		case "STATUS_ERROR": return OrderResultStatus$1.STATUS_ERROR;
		default: return OrderResultStatus$1.UNRECOGNIZED;
	}
}
function orderResultStatusToJSON(object) {
	switch (object) {
		case OrderResultStatus$1.STATUS_CANCELLED: return 0;
		case OrderResultStatus$1.STATUS_SUCCESS: return 1;
		case OrderResultStatus$1.STATUS_ERROR: return 2;
		case OrderResultStatus$1.UNRECOGNIZED:
		default: return -1;
	}
}
function createBaseCreateOrderEffect() {
	return {
		id: "",
		skus: [],
		metadata: {}
	};
}
var CreateOrderEffect = {
	$type: "devvit.ui.effect_types.v1alpha.CreateOrderEffect",
	encode(message, writer = import_minimal.default.Writer.create()) {
		if (message.id !== "") writer.uint32(10).string(message.id);
		for (const v of message.skus) writer.uint32(18).string(v);
		Object.entries(message.metadata).forEach(([key, value]) => {
			CreateOrderEffect_MetadataEntry.encode({
				key,
				value
			}, writer.uint32(26).fork()).ldelim();
		});
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseCreateOrderEffect();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {
				case 1:
					if (tag !== 10) break;
					message.id = reader.string();
					continue;
				case 2:
					if (tag !== 18) break;
					message.skus.push(reader.string());
					continue;
				case 3:
					if (tag !== 26) break;
					const entry3 = CreateOrderEffect_MetadataEntry.decode(reader, reader.uint32());
					if (entry3.value !== void 0) message.metadata[entry3.key] = entry3.value;
					continue;
			}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(object) {
		return {
			id: isSet$10(object.id) ? globalThis.String(object.id) : "",
			skus: globalThis.Array.isArray(object?.skus) ? object.skus.map((e) => globalThis.String(e)) : [],
			metadata: isObject$2(object.metadata) ? Object.entries(object.metadata).reduce((acc, [key, value]) => {
				acc[key] = String(value);
				return acc;
			}, {}) : {}
		};
	},
	toJSON(message) {
		const obj = {};
		if (message.id !== "") obj.id = message.id;
		if (message.skus?.length) obj.skus = message.skus;
		if (message.metadata) {
			const entries = Object.entries(message.metadata);
			if (entries.length > 0) {
				obj.metadata = {};
				entries.forEach(([k, v]) => {
					obj.metadata[k] = v;
				});
			}
		}
		return obj;
	},
	create(base) {
		return CreateOrderEffect.fromPartial(base ?? {});
	},
	fromPartial(object) {
		const message = createBaseCreateOrderEffect();
		message.id = object.id ?? "";
		message.skus = object.skus?.map((e) => e) || [];
		message.metadata = Object.entries(object.metadata ?? {}).reduce((acc, [key, value]) => {
			if (value !== void 0) acc[key] = globalThis.String(value);
			return acc;
		}, {});
		return message;
	}
};
messageTypeRegistry.set(CreateOrderEffect.$type, CreateOrderEffect);
function createBaseCreateOrderEffect_MetadataEntry() {
	return {
		key: "",
		value: ""
	};
}
var CreateOrderEffect_MetadataEntry = {
	$type: "devvit.ui.effect_types.v1alpha.CreateOrderEffect.MetadataEntry",
	encode(message, writer = import_minimal.default.Writer.create()) {
		if (message.key !== "") writer.uint32(10).string(message.key);
		if (message.value !== "") writer.uint32(18).string(message.value);
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseCreateOrderEffect_MetadataEntry();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {
				case 1:
					if (tag !== 10) break;
					message.key = reader.string();
					continue;
				case 2:
					if (tag !== 18) break;
					message.value = reader.string();
					continue;
			}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(object) {
		return {
			key: isSet$10(object.key) ? globalThis.String(object.key) : "",
			value: isSet$10(object.value) ? globalThis.String(object.value) : ""
		};
	},
	toJSON(message) {
		const obj = {};
		if (message.key !== "") obj.key = message.key;
		if (message.value !== "") obj.value = message.value;
		return obj;
	},
	create(base) {
		return CreateOrderEffect_MetadataEntry.fromPartial(base ?? {});
	},
	fromPartial(object) {
		const message = createBaseCreateOrderEffect_MetadataEntry();
		message.key = object.key ?? "";
		message.value = object.value ?? "";
		return message;
	}
};
messageTypeRegistry.set(CreateOrderEffect_MetadataEntry.$type, CreateOrderEffect_MetadataEntry);
function createBaseOrderResultEvent() {
	return {
		errorMessage: void 0,
		orderId: void 0,
		order: void 0,
		status: 0,
		errorCode: void 0
	};
}
var OrderResultEvent = {
	$type: "devvit.ui.effect_types.v1alpha.OrderResultEvent",
	encode(message, writer = import_minimal.default.Writer.create()) {
		if (message.errorMessage !== void 0) writer.uint32(18).string(message.errorMessage);
		if (message.orderId !== void 0) writer.uint32(26).string(message.orderId);
		if (message.order !== void 0) CreateOrderEffect.encode(message.order, writer.uint32(34).fork()).ldelim();
		if (message.status !== 0) writer.uint32(40).int32(message.status);
		if (message.errorCode !== void 0) writer.uint32(48).int32(message.errorCode);
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseOrderResultEvent();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {
				case 2:
					if (tag !== 18) break;
					message.errorMessage = reader.string();
					continue;
				case 3:
					if (tag !== 26) break;
					message.orderId = reader.string();
					continue;
				case 4:
					if (tag !== 34) break;
					message.order = CreateOrderEffect.decode(reader, reader.uint32());
					continue;
				case 5:
					if (tag !== 40) break;
					message.status = reader.int32();
					continue;
				case 6:
					if (tag !== 48) break;
					message.errorCode = reader.int32();
					continue;
			}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(object) {
		return {
			errorMessage: isSet$10(object.errorMessage) ? globalThis.String(object.errorMessage) : void 0,
			orderId: isSet$10(object.orderId) ? globalThis.String(object.orderId) : void 0,
			order: isSet$10(object.order) ? CreateOrderEffect.fromJSON(object.order) : void 0,
			status: isSet$10(object.status) ? orderResultStatusFromJSON(object.status) : 0,
			errorCode: isSet$10(object.errorCode) ? globalThis.Number(object.errorCode) : void 0
		};
	},
	toJSON(message) {
		const obj = {};
		if (message.errorMessage !== void 0) obj.errorMessage = message.errorMessage;
		if (message.orderId !== void 0) obj.orderId = message.orderId;
		if (message.order !== void 0) obj.order = CreateOrderEffect.toJSON(message.order);
		if (message.status !== 0) obj.status = orderResultStatusToJSON(message.status);
		if (message.errorCode !== void 0) obj.errorCode = Math.round(message.errorCode);
		return obj;
	},
	create(base) {
		return OrderResultEvent.fromPartial(base ?? {});
	},
	fromPartial(object) {
		const message = createBaseOrderResultEvent();
		message.errorMessage = object.errorMessage ?? void 0;
		message.orderId = object.orderId ?? void 0;
		message.order = object.order !== void 0 && object.order !== null ? CreateOrderEffect.fromPartial(object.order) : void 0;
		message.status = object.status ?? 0;
		message.errorCode = object.errorCode ?? void 0;
		return message;
	}
};
messageTypeRegistry.set(OrderResultEvent.$type, OrderResultEvent);
function isObject$2(value) {
	return typeof value === "object" && value !== null;
}
function isSet$10(value) {
	return value !== null && value !== void 0;
}
//#endregion
//#region crystal-cascade/node_modules/@devvit/protos/types/devvit/ui/form_builder/v1alpha/type.js
/**
* #type.ts
*
* Code generated by ts-proto. DO NOT EDIT.
* @packageDocumentation
*/
var FormFieldType$1;
(function(FormFieldType) {
	/** STRING - Single-line string */
	FormFieldType[FormFieldType["STRING"] = 0] = "STRING";
	/** PARAGRAPH - Multi-line string */
	FormFieldType[FormFieldType["PARAGRAPH"] = 1] = "PARAGRAPH";
	/** NUMBER - Number input */
	FormFieldType[FormFieldType["NUMBER"] = 2] = "NUMBER";
	/** BOOLEAN - Checkbox, switch */
	FormFieldType[FormFieldType["BOOLEAN"] = 3] = "BOOLEAN";
	/** LIST - Collect multiple entries of a single type */
	FormFieldType[FormFieldType["LIST"] = 4] = "LIST";
	/** SELECTION - Provide a list of options for the user to choose from */
	FormFieldType[FormFieldType["SELECTION"] = 5] = "SELECTION";
	/** GROUP - Create a labeled subsection of the form with nested fields */
	FormFieldType[FormFieldType["GROUP"] = 6] = "GROUP";
	/** IMAGE - Image upload - value is a URL for the uploaded image */
	FormFieldType[FormFieldType["IMAGE"] = 7] = "IMAGE";
	FormFieldType[FormFieldType["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(FormFieldType$1 || (FormFieldType$1 = {}));
function formFieldTypeFromJSON(object) {
	switch (object) {
		case 0:
		case "STRING": return FormFieldType$1.STRING;
		case 1:
		case "PARAGRAPH": return FormFieldType$1.PARAGRAPH;
		case 2:
		case "NUMBER": return FormFieldType$1.NUMBER;
		case 3:
		case "BOOLEAN": return FormFieldType$1.BOOLEAN;
		case 4:
		case "LIST": return FormFieldType$1.LIST;
		case 5:
		case "SELECTION": return FormFieldType$1.SELECTION;
		case 6:
		case "GROUP": return FormFieldType$1.GROUP;
		case 7:
		case "IMAGE": return FormFieldType$1.IMAGE;
		default: return FormFieldType$1.UNRECOGNIZED;
	}
}
function formFieldTypeToJSON(object) {
	switch (object) {
		case FormFieldType$1.STRING: return 0;
		case FormFieldType$1.PARAGRAPH: return 1;
		case FormFieldType$1.NUMBER: return 2;
		case FormFieldType$1.BOOLEAN: return 3;
		case FormFieldType$1.LIST: return 4;
		case FormFieldType$1.SELECTION: return 5;
		case FormFieldType$1.GROUP: return 6;
		case FormFieldType$1.IMAGE: return 7;
		case FormFieldType$1.UNRECOGNIZED:
		default: return -1;
	}
}
//#endregion
//#region crystal-cascade/node_modules/@devvit/protos/types/devvit/ui/form_builder/v1alpha/value.js
/**
* #value.ts
*
* Code generated by ts-proto. DO NOT EDIT.
* @packageDocumentation
*/
function createBaseFormFieldValue() {
	return {
		fieldType: 0,
		isSecret: void 0,
		stringValue: void 0,
		numberValue: void 0,
		boolValue: void 0,
		listValue: void 0,
		selectionValue: void 0,
		groupValue: void 0
	};
}
var FormFieldValue = {
	$type: "devvit.ui.form_builder.v1alpha.FormFieldValue",
	encode(message, writer = import_minimal.default.Writer.create()) {
		if (message.fieldType !== 0) writer.uint32(8).int32(message.fieldType);
		if (message.isSecret !== void 0) writer.uint32(800).bool(message.isSecret);
		if (message.stringValue !== void 0) writer.uint32(18).string(message.stringValue);
		if (message.numberValue !== void 0) writer.uint32(25).double(message.numberValue);
		if (message.boolValue !== void 0) writer.uint32(32).bool(message.boolValue);
		if (message.listValue !== void 0) FormFieldValue_ListValue.encode(message.listValue, writer.uint32(42).fork()).ldelim();
		if (message.selectionValue !== void 0) FormFieldValue_SelectionValue.encode(message.selectionValue, writer.uint32(50).fork()).ldelim();
		if (message.groupValue !== void 0) FormFieldValue_GroupValue.encode(message.groupValue, writer.uint32(58).fork()).ldelim();
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseFormFieldValue();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {
				case 1:
					if (tag !== 8) break;
					message.fieldType = reader.int32();
					continue;
				case 100:
					if (tag !== 800) break;
					message.isSecret = reader.bool();
					continue;
				case 2:
					if (tag !== 18) break;
					message.stringValue = reader.string();
					continue;
				case 3:
					if (tag !== 25) break;
					message.numberValue = reader.double();
					continue;
				case 4:
					if (tag !== 32) break;
					message.boolValue = reader.bool();
					continue;
				case 5:
					if (tag !== 42) break;
					message.listValue = FormFieldValue_ListValue.decode(reader, reader.uint32());
					continue;
				case 6:
					if (tag !== 50) break;
					message.selectionValue = FormFieldValue_SelectionValue.decode(reader, reader.uint32());
					continue;
				case 7:
					if (tag !== 58) break;
					message.groupValue = FormFieldValue_GroupValue.decode(reader, reader.uint32());
					continue;
			}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(object) {
		return {
			fieldType: isSet$9(object.fieldType) ? formFieldTypeFromJSON(object.fieldType) : 0,
			isSecret: isSet$9(object.isSecret) ? globalThis.Boolean(object.isSecret) : void 0,
			stringValue: isSet$9(object.stringValue) ? globalThis.String(object.stringValue) : void 0,
			numberValue: isSet$9(object.numberValue) ? globalThis.Number(object.numberValue) : void 0,
			boolValue: isSet$9(object.boolValue) ? globalThis.Boolean(object.boolValue) : void 0,
			listValue: isSet$9(object.listValue) ? FormFieldValue_ListValue.fromJSON(object.listValue) : void 0,
			selectionValue: isSet$9(object.selectionValue) ? FormFieldValue_SelectionValue.fromJSON(object.selectionValue) : void 0,
			groupValue: isSet$9(object.groupValue) ? FormFieldValue_GroupValue.fromJSON(object.groupValue) : void 0
		};
	},
	toJSON(message) {
		const obj = {};
		if (message.fieldType !== 0) obj.fieldType = formFieldTypeToJSON(message.fieldType);
		if (message.isSecret !== void 0) obj.isSecret = message.isSecret;
		if (message.stringValue !== void 0) obj.stringValue = message.stringValue;
		if (message.numberValue !== void 0) obj.numberValue = message.numberValue;
		if (message.boolValue !== void 0) obj.boolValue = message.boolValue;
		if (message.listValue !== void 0) obj.listValue = FormFieldValue_ListValue.toJSON(message.listValue);
		if (message.selectionValue !== void 0) obj.selectionValue = FormFieldValue_SelectionValue.toJSON(message.selectionValue);
		if (message.groupValue !== void 0) obj.groupValue = FormFieldValue_GroupValue.toJSON(message.groupValue);
		return obj;
	},
	create(base) {
		return FormFieldValue.fromPartial(base ?? {});
	},
	fromPartial(object) {
		const message = createBaseFormFieldValue();
		message.fieldType = object.fieldType ?? 0;
		message.isSecret = object.isSecret ?? void 0;
		message.stringValue = object.stringValue ?? void 0;
		message.numberValue = object.numberValue ?? void 0;
		message.boolValue = object.boolValue ?? void 0;
		message.listValue = object.listValue !== void 0 && object.listValue !== null ? FormFieldValue_ListValue.fromPartial(object.listValue) : void 0;
		message.selectionValue = object.selectionValue !== void 0 && object.selectionValue !== null ? FormFieldValue_SelectionValue.fromPartial(object.selectionValue) : void 0;
		message.groupValue = object.groupValue !== void 0 && object.groupValue !== null ? FormFieldValue_GroupValue.fromPartial(object.groupValue) : void 0;
		return message;
	}
};
messageTypeRegistry.set(FormFieldValue.$type, FormFieldValue);
function createBaseFormFieldValue_ListValue() {
	return {
		itemType: 0,
		items: []
	};
}
var FormFieldValue_ListValue = {
	$type: "devvit.ui.form_builder.v1alpha.FormFieldValue.ListValue",
	encode(message, writer = import_minimal.default.Writer.create()) {
		if (message.itemType !== 0) writer.uint32(8).int32(message.itemType);
		for (const v of message.items) FormFieldValue.encode(v, writer.uint32(18).fork()).ldelim();
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseFormFieldValue_ListValue();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {
				case 1:
					if (tag !== 8) break;
					message.itemType = reader.int32();
					continue;
				case 2:
					if (tag !== 18) break;
					message.items.push(FormFieldValue.decode(reader, reader.uint32()));
					continue;
			}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(object) {
		return {
			itemType: isSet$9(object.itemType) ? formFieldTypeFromJSON(object.itemType) : 0,
			items: globalThis.Array.isArray(object?.items) ? object.items.map((e) => FormFieldValue.fromJSON(e)) : []
		};
	},
	toJSON(message) {
		const obj = {};
		if (message.itemType !== 0) obj.itemType = formFieldTypeToJSON(message.itemType);
		if (message.items?.length) obj.items = message.items.map((e) => FormFieldValue.toJSON(e));
		return obj;
	},
	create(base) {
		return FormFieldValue_ListValue.fromPartial(base ?? {});
	},
	fromPartial(object) {
		const message = createBaseFormFieldValue_ListValue();
		message.itemType = object.itemType ?? 0;
		message.items = object.items?.map((e) => FormFieldValue.fromPartial(e)) || [];
		return message;
	}
};
messageTypeRegistry.set(FormFieldValue_ListValue.$type, FormFieldValue_ListValue);
function createBaseFormFieldValue_SelectionValue() {
	return { values: [] };
}
var FormFieldValue_SelectionValue = {
	$type: "devvit.ui.form_builder.v1alpha.FormFieldValue.SelectionValue",
	encode(message, writer = import_minimal.default.Writer.create()) {
		for (const v of message.values) writer.uint32(10).string(v);
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseFormFieldValue_SelectionValue();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {
				case 1:
					if (tag !== 10) break;
					message.values.push(reader.string());
					continue;
			}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(object) {
		return { values: globalThis.Array.isArray(object?.values) ? object.values.map((e) => globalThis.String(e)) : [] };
	},
	toJSON(message) {
		const obj = {};
		if (message.values?.length) obj.values = message.values;
		return obj;
	},
	create(base) {
		return FormFieldValue_SelectionValue.fromPartial(base ?? {});
	},
	fromPartial(object) {
		const message = createBaseFormFieldValue_SelectionValue();
		message.values = object.values?.map((e) => e) || [];
		return message;
	}
};
messageTypeRegistry.set(FormFieldValue_SelectionValue.$type, FormFieldValue_SelectionValue);
function createBaseFormFieldValue_GroupValue() {
	return {};
}
var FormFieldValue_GroupValue = {
	$type: "devvit.ui.form_builder.v1alpha.FormFieldValue.GroupValue",
	encode(_, writer = import_minimal.default.Writer.create()) {
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseFormFieldValue_GroupValue();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(_) {
		return {};
	},
	toJSON(_) {
		return {};
	},
	create(base) {
		return FormFieldValue_GroupValue.fromPartial(base ?? {});
	},
	fromPartial(_) {
		return createBaseFormFieldValue_GroupValue();
	}
};
messageTypeRegistry.set(FormFieldValue_GroupValue.$type, FormFieldValue_GroupValue);
function isSet$9(value) {
	return value !== null && value !== void 0;
}
//#endregion
//#region crystal-cascade/node_modules/@devvit/protos/types/devvit/ui/form_builder/v1alpha/field.js
/**
* #field.ts
*
* Code generated by ts-proto. DO NOT EDIT.
* @packageDocumentation
*/
function createBaseFormField() {
	return {
		fieldId: "",
		fieldType: 0,
		label: "",
		helpText: void 0,
		defaultValue: void 0,
		required: void 0,
		disabled: void 0,
		fieldConfig: void 0,
		isSecret: void 0
	};
}
var FormField = {
	$type: "devvit.ui.form_builder.v1alpha.FormField",
	encode(message, writer = import_minimal.default.Writer.create()) {
		if (message.fieldId !== "") writer.uint32(10).string(message.fieldId);
		if (message.fieldType !== 0) writer.uint32(16).int32(message.fieldType);
		if (message.label !== "") writer.uint32(26).string(message.label);
		if (message.helpText !== void 0) writer.uint32(34).string(message.helpText);
		if (message.defaultValue !== void 0) FormFieldValue.encode(message.defaultValue, writer.uint32(42).fork()).ldelim();
		if (message.required !== void 0) writer.uint32(48).bool(message.required);
		if (message.disabled !== void 0) writer.uint32(56).bool(message.disabled);
		if (message.fieldConfig !== void 0) FieldConfig.encode(message.fieldConfig, writer.uint32(66).fork()).ldelim();
		if (message.isSecret !== void 0) writer.uint32(72).bool(message.isSecret);
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseFormField();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {
				case 1:
					if (tag !== 10) break;
					message.fieldId = reader.string();
					continue;
				case 2:
					if (tag !== 16) break;
					message.fieldType = reader.int32();
					continue;
				case 3:
					if (tag !== 26) break;
					message.label = reader.string();
					continue;
				case 4:
					if (tag !== 34) break;
					message.helpText = reader.string();
					continue;
				case 5:
					if (tag !== 42) break;
					message.defaultValue = FormFieldValue.decode(reader, reader.uint32());
					continue;
				case 6:
					if (tag !== 48) break;
					message.required = reader.bool();
					continue;
				case 7:
					if (tag !== 56) break;
					message.disabled = reader.bool();
					continue;
				case 8:
					if (tag !== 66) break;
					message.fieldConfig = FieldConfig.decode(reader, reader.uint32());
					continue;
				case 9:
					if (tag !== 72) break;
					message.isSecret = reader.bool();
					continue;
			}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(object) {
		return {
			fieldId: isSet$8(object.fieldId) ? globalThis.String(object.fieldId) : "",
			fieldType: isSet$8(object.fieldType) ? formFieldTypeFromJSON(object.fieldType) : 0,
			label: isSet$8(object.label) ? globalThis.String(object.label) : "",
			helpText: isSet$8(object.helpText) ? globalThis.String(object.helpText) : void 0,
			defaultValue: isSet$8(object.defaultValue) ? FormFieldValue.fromJSON(object.defaultValue) : void 0,
			required: isSet$8(object.required) ? globalThis.Boolean(object.required) : void 0,
			disabled: isSet$8(object.disabled) ? globalThis.Boolean(object.disabled) : void 0,
			fieldConfig: isSet$8(object.fieldConfig) ? FieldConfig.fromJSON(object.fieldConfig) : void 0,
			isSecret: isSet$8(object.isSecret) ? globalThis.Boolean(object.isSecret) : void 0
		};
	},
	toJSON(message) {
		const obj = {};
		if (message.fieldId !== "") obj.fieldId = message.fieldId;
		if (message.fieldType !== 0) obj.fieldType = formFieldTypeToJSON(message.fieldType);
		if (message.label !== "") obj.label = message.label;
		if (message.helpText !== void 0) obj.helpText = message.helpText;
		if (message.defaultValue !== void 0) obj.defaultValue = FormFieldValue.toJSON(message.defaultValue);
		if (message.required !== void 0) obj.required = message.required;
		if (message.disabled !== void 0) obj.disabled = message.disabled;
		if (message.fieldConfig !== void 0) obj.fieldConfig = FieldConfig.toJSON(message.fieldConfig);
		if (message.isSecret !== void 0) obj.isSecret = message.isSecret;
		return obj;
	},
	create(base) {
		return FormField.fromPartial(base ?? {});
	},
	fromPartial(object) {
		const message = createBaseFormField();
		message.fieldId = object.fieldId ?? "";
		message.fieldType = object.fieldType ?? 0;
		message.label = object.label ?? "";
		message.helpText = object.helpText ?? void 0;
		message.defaultValue = object.defaultValue !== void 0 && object.defaultValue !== null ? FormFieldValue.fromPartial(object.defaultValue) : void 0;
		message.required = object.required ?? void 0;
		message.disabled = object.disabled ?? void 0;
		message.fieldConfig = object.fieldConfig !== void 0 && object.fieldConfig !== null ? FieldConfig.fromPartial(object.fieldConfig) : void 0;
		message.isSecret = object.isSecret ?? void 0;
		return message;
	}
};
messageTypeRegistry.set(FormField.$type, FormField);
function createBaseFieldConfig() {
	return {
		stringConfig: void 0,
		paragraphConfig: void 0,
		numberConfig: void 0,
		booleanConfig: void 0,
		listConfig: void 0,
		selectionConfig: void 0,
		groupConfig: void 0
	};
}
var FieldConfig = {
	$type: "devvit.ui.form_builder.v1alpha.FieldConfig",
	encode(message, writer = import_minimal.default.Writer.create()) {
		if (message.stringConfig !== void 0) FieldConfig_String.encode(message.stringConfig, writer.uint32(10).fork()).ldelim();
		if (message.paragraphConfig !== void 0) FieldConfig_Paragraph.encode(message.paragraphConfig, writer.uint32(18).fork()).ldelim();
		if (message.numberConfig !== void 0) FieldConfig_Number.encode(message.numberConfig, writer.uint32(26).fork()).ldelim();
		if (message.booleanConfig !== void 0) FieldConfig_Boolean.encode(message.booleanConfig, writer.uint32(34).fork()).ldelim();
		if (message.listConfig !== void 0) FieldConfig_List.encode(message.listConfig, writer.uint32(42).fork()).ldelim();
		if (message.selectionConfig !== void 0) FieldConfig_Selection.encode(message.selectionConfig, writer.uint32(50).fork()).ldelim();
		if (message.groupConfig !== void 0) FieldConfig_Group.encode(message.groupConfig, writer.uint32(58).fork()).ldelim();
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseFieldConfig();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {
				case 1:
					if (tag !== 10) break;
					message.stringConfig = FieldConfig_String.decode(reader, reader.uint32());
					continue;
				case 2:
					if (tag !== 18) break;
					message.paragraphConfig = FieldConfig_Paragraph.decode(reader, reader.uint32());
					continue;
				case 3:
					if (tag !== 26) break;
					message.numberConfig = FieldConfig_Number.decode(reader, reader.uint32());
					continue;
				case 4:
					if (tag !== 34) break;
					message.booleanConfig = FieldConfig_Boolean.decode(reader, reader.uint32());
					continue;
				case 5:
					if (tag !== 42) break;
					message.listConfig = FieldConfig_List.decode(reader, reader.uint32());
					continue;
				case 6:
					if (tag !== 50) break;
					message.selectionConfig = FieldConfig_Selection.decode(reader, reader.uint32());
					continue;
				case 7:
					if (tag !== 58) break;
					message.groupConfig = FieldConfig_Group.decode(reader, reader.uint32());
					continue;
			}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(object) {
		return {
			stringConfig: isSet$8(object.stringConfig) ? FieldConfig_String.fromJSON(object.stringConfig) : void 0,
			paragraphConfig: isSet$8(object.paragraphConfig) ? FieldConfig_Paragraph.fromJSON(object.paragraphConfig) : void 0,
			numberConfig: isSet$8(object.numberConfig) ? FieldConfig_Number.fromJSON(object.numberConfig) : void 0,
			booleanConfig: isSet$8(object.booleanConfig) ? FieldConfig_Boolean.fromJSON(object.booleanConfig) : void 0,
			listConfig: isSet$8(object.listConfig) ? FieldConfig_List.fromJSON(object.listConfig) : void 0,
			selectionConfig: isSet$8(object.selectionConfig) ? FieldConfig_Selection.fromJSON(object.selectionConfig) : void 0,
			groupConfig: isSet$8(object.groupConfig) ? FieldConfig_Group.fromJSON(object.groupConfig) : void 0
		};
	},
	toJSON(message) {
		const obj = {};
		if (message.stringConfig !== void 0) obj.stringConfig = FieldConfig_String.toJSON(message.stringConfig);
		if (message.paragraphConfig !== void 0) obj.paragraphConfig = FieldConfig_Paragraph.toJSON(message.paragraphConfig);
		if (message.numberConfig !== void 0) obj.numberConfig = FieldConfig_Number.toJSON(message.numberConfig);
		if (message.booleanConfig !== void 0) obj.booleanConfig = FieldConfig_Boolean.toJSON(message.booleanConfig);
		if (message.listConfig !== void 0) obj.listConfig = FieldConfig_List.toJSON(message.listConfig);
		if (message.selectionConfig !== void 0) obj.selectionConfig = FieldConfig_Selection.toJSON(message.selectionConfig);
		if (message.groupConfig !== void 0) obj.groupConfig = FieldConfig_Group.toJSON(message.groupConfig);
		return obj;
	},
	create(base) {
		return FieldConfig.fromPartial(base ?? {});
	},
	fromPartial(object) {
		const message = createBaseFieldConfig();
		message.stringConfig = object.stringConfig !== void 0 && object.stringConfig !== null ? FieldConfig_String.fromPartial(object.stringConfig) : void 0;
		message.paragraphConfig = object.paragraphConfig !== void 0 && object.paragraphConfig !== null ? FieldConfig_Paragraph.fromPartial(object.paragraphConfig) : void 0;
		message.numberConfig = object.numberConfig !== void 0 && object.numberConfig !== null ? FieldConfig_Number.fromPartial(object.numberConfig) : void 0;
		message.booleanConfig = object.booleanConfig !== void 0 && object.booleanConfig !== null ? FieldConfig_Boolean.fromPartial(object.booleanConfig) : void 0;
		message.listConfig = object.listConfig !== void 0 && object.listConfig !== null ? FieldConfig_List.fromPartial(object.listConfig) : void 0;
		message.selectionConfig = object.selectionConfig !== void 0 && object.selectionConfig !== null ? FieldConfig_Selection.fromPartial(object.selectionConfig) : void 0;
		message.groupConfig = object.groupConfig !== void 0 && object.groupConfig !== null ? FieldConfig_Group.fromPartial(object.groupConfig) : void 0;
		return message;
	}
};
messageTypeRegistry.set(FieldConfig.$type, FieldConfig);
function createBaseFieldConfig_String() {
	return {
		minLength: void 0,
		maxLength: void 0,
		placeholder: void 0
	};
}
var FieldConfig_String = {
	$type: "devvit.ui.form_builder.v1alpha.FieldConfig.String",
	encode(message, writer = import_minimal.default.Writer.create()) {
		if (message.minLength !== void 0) writer.uint32(8).int32(message.minLength);
		if (message.maxLength !== void 0) writer.uint32(16).int32(message.maxLength);
		if (message.placeholder !== void 0) writer.uint32(26).string(message.placeholder);
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseFieldConfig_String();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {
				case 1:
					if (tag !== 8) break;
					message.minLength = reader.int32();
					continue;
				case 2:
					if (tag !== 16) break;
					message.maxLength = reader.int32();
					continue;
				case 3:
					if (tag !== 26) break;
					message.placeholder = reader.string();
					continue;
			}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(object) {
		return {
			minLength: isSet$8(object.minLength) ? globalThis.Number(object.minLength) : void 0,
			maxLength: isSet$8(object.maxLength) ? globalThis.Number(object.maxLength) : void 0,
			placeholder: isSet$8(object.placeholder) ? globalThis.String(object.placeholder) : void 0
		};
	},
	toJSON(message) {
		const obj = {};
		if (message.minLength !== void 0) obj.minLength = Math.round(message.minLength);
		if (message.maxLength !== void 0) obj.maxLength = Math.round(message.maxLength);
		if (message.placeholder !== void 0) obj.placeholder = message.placeholder;
		return obj;
	},
	create(base) {
		return FieldConfig_String.fromPartial(base ?? {});
	},
	fromPartial(object) {
		const message = createBaseFieldConfig_String();
		message.minLength = object.minLength ?? void 0;
		message.maxLength = object.maxLength ?? void 0;
		message.placeholder = object.placeholder ?? void 0;
		return message;
	}
};
messageTypeRegistry.set(FieldConfig_String.$type, FieldConfig_String);
function createBaseFieldConfig_Paragraph() {
	return {
		maxCharacters: void 0,
		lineHeight: void 0,
		placeholder: void 0
	};
}
var FieldConfig_Paragraph = {
	$type: "devvit.ui.form_builder.v1alpha.FieldConfig.Paragraph",
	encode(message, writer = import_minimal.default.Writer.create()) {
		if (message.maxCharacters !== void 0) writer.uint32(8).int32(message.maxCharacters);
		if (message.lineHeight !== void 0) writer.uint32(16).int32(message.lineHeight);
		if (message.placeholder !== void 0) writer.uint32(26).string(message.placeholder);
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseFieldConfig_Paragraph();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {
				case 1:
					if (tag !== 8) break;
					message.maxCharacters = reader.int32();
					continue;
				case 2:
					if (tag !== 16) break;
					message.lineHeight = reader.int32();
					continue;
				case 3:
					if (tag !== 26) break;
					message.placeholder = reader.string();
					continue;
			}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(object) {
		return {
			maxCharacters: isSet$8(object.maxCharacters) ? globalThis.Number(object.maxCharacters) : void 0,
			lineHeight: isSet$8(object.lineHeight) ? globalThis.Number(object.lineHeight) : void 0,
			placeholder: isSet$8(object.placeholder) ? globalThis.String(object.placeholder) : void 0
		};
	},
	toJSON(message) {
		const obj = {};
		if (message.maxCharacters !== void 0) obj.maxCharacters = Math.round(message.maxCharacters);
		if (message.lineHeight !== void 0) obj.lineHeight = Math.round(message.lineHeight);
		if (message.placeholder !== void 0) obj.placeholder = message.placeholder;
		return obj;
	},
	create(base) {
		return FieldConfig_Paragraph.fromPartial(base ?? {});
	},
	fromPartial(object) {
		const message = createBaseFieldConfig_Paragraph();
		message.maxCharacters = object.maxCharacters ?? void 0;
		message.lineHeight = object.lineHeight ?? void 0;
		message.placeholder = object.placeholder ?? void 0;
		return message;
	}
};
messageTypeRegistry.set(FieldConfig_Paragraph.$type, FieldConfig_Paragraph);
function createBaseFieldConfig_Number() {
	return {
		step: void 0,
		min: void 0,
		max: void 0
	};
}
var FieldConfig_Number = {
	$type: "devvit.ui.form_builder.v1alpha.FieldConfig.Number",
	encode(message, writer = import_minimal.default.Writer.create()) {
		if (message.step !== void 0) writer.uint32(9).double(message.step);
		if (message.min !== void 0) writer.uint32(17).double(message.min);
		if (message.max !== void 0) writer.uint32(25).double(message.max);
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseFieldConfig_Number();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {
				case 1:
					if (tag !== 9) break;
					message.step = reader.double();
					continue;
				case 2:
					if (tag !== 17) break;
					message.min = reader.double();
					continue;
				case 3:
					if (tag !== 25) break;
					message.max = reader.double();
					continue;
			}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(object) {
		return {
			step: isSet$8(object.step) ? globalThis.Number(object.step) : void 0,
			min: isSet$8(object.min) ? globalThis.Number(object.min) : void 0,
			max: isSet$8(object.max) ? globalThis.Number(object.max) : void 0
		};
	},
	toJSON(message) {
		const obj = {};
		if (message.step !== void 0) obj.step = message.step;
		if (message.min !== void 0) obj.min = message.min;
		if (message.max !== void 0) obj.max = message.max;
		return obj;
	},
	create(base) {
		return FieldConfig_Number.fromPartial(base ?? {});
	},
	fromPartial(object) {
		const message = createBaseFieldConfig_Number();
		message.step = object.step ?? void 0;
		message.min = object.min ?? void 0;
		message.max = object.max ?? void 0;
		return message;
	}
};
messageTypeRegistry.set(FieldConfig_Number.$type, FieldConfig_Number);
function createBaseFieldConfig_Boolean() {
	return {};
}
var FieldConfig_Boolean = {
	$type: "devvit.ui.form_builder.v1alpha.FieldConfig.Boolean",
	encode(_, writer = import_minimal.default.Writer.create()) {
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseFieldConfig_Boolean();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(_) {
		return {};
	},
	toJSON(_) {
		return {};
	},
	create(base) {
		return FieldConfig_Boolean.fromPartial(base ?? {});
	},
	fromPartial(_) {
		return createBaseFieldConfig_Boolean();
	}
};
messageTypeRegistry.set(FieldConfig_Boolean.$type, FieldConfig_Boolean);
function createBaseFieldConfig_List() {
	return {
		itemType: 0,
		itemConfig: void 0,
		minEntries: void 0,
		maxEntries: void 0,
		entryLabel: void 0
	};
}
var FieldConfig_List = {
	$type: "devvit.ui.form_builder.v1alpha.FieldConfig.List",
	encode(message, writer = import_minimal.default.Writer.create()) {
		if (message.itemType !== 0) writer.uint32(8).int32(message.itemType);
		if (message.itemConfig !== void 0) FieldConfig.encode(message.itemConfig, writer.uint32(18).fork()).ldelim();
		if (message.minEntries !== void 0) writer.uint32(24).int32(message.minEntries);
		if (message.maxEntries !== void 0) writer.uint32(32).int32(message.maxEntries);
		if (message.entryLabel !== void 0) writer.uint32(42).string(message.entryLabel);
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseFieldConfig_List();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {
				case 1:
					if (tag !== 8) break;
					message.itemType = reader.int32();
					continue;
				case 2:
					if (tag !== 18) break;
					message.itemConfig = FieldConfig.decode(reader, reader.uint32());
					continue;
				case 3:
					if (tag !== 24) break;
					message.minEntries = reader.int32();
					continue;
				case 4:
					if (tag !== 32) break;
					message.maxEntries = reader.int32();
					continue;
				case 5:
					if (tag !== 42) break;
					message.entryLabel = reader.string();
					continue;
			}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(object) {
		return {
			itemType: isSet$8(object.itemType) ? formFieldTypeFromJSON(object.itemType) : 0,
			itemConfig: isSet$8(object.itemConfig) ? FieldConfig.fromJSON(object.itemConfig) : void 0,
			minEntries: isSet$8(object.minEntries) ? globalThis.Number(object.minEntries) : void 0,
			maxEntries: isSet$8(object.maxEntries) ? globalThis.Number(object.maxEntries) : void 0,
			entryLabel: isSet$8(object.entryLabel) ? globalThis.String(object.entryLabel) : void 0
		};
	},
	toJSON(message) {
		const obj = {};
		if (message.itemType !== 0) obj.itemType = formFieldTypeToJSON(message.itemType);
		if (message.itemConfig !== void 0) obj.itemConfig = FieldConfig.toJSON(message.itemConfig);
		if (message.minEntries !== void 0) obj.minEntries = Math.round(message.minEntries);
		if (message.maxEntries !== void 0) obj.maxEntries = Math.round(message.maxEntries);
		if (message.entryLabel !== void 0) obj.entryLabel = message.entryLabel;
		return obj;
	},
	create(base) {
		return FieldConfig_List.fromPartial(base ?? {});
	},
	fromPartial(object) {
		const message = createBaseFieldConfig_List();
		message.itemType = object.itemType ?? 0;
		message.itemConfig = object.itemConfig !== void 0 && object.itemConfig !== null ? FieldConfig.fromPartial(object.itemConfig) : void 0;
		message.minEntries = object.minEntries ?? void 0;
		message.maxEntries = object.maxEntries ?? void 0;
		message.entryLabel = object.entryLabel ?? void 0;
		return message;
	}
};
messageTypeRegistry.set(FieldConfig_List.$type, FieldConfig_List);
function createBaseFieldConfig_Selection() {
	return {
		choices: [],
		multiSelect: void 0,
		minSelections: void 0,
		maxSelections: void 0,
		renderAsList: void 0
	};
}
var FieldConfig_Selection = {
	$type: "devvit.ui.form_builder.v1alpha.FieldConfig.Selection",
	encode(message, writer = import_minimal.default.Writer.create()) {
		for (const v of message.choices) FieldConfig_Selection_Item.encode(v, writer.uint32(10).fork()).ldelim();
		if (message.multiSelect !== void 0) writer.uint32(16).bool(message.multiSelect);
		if (message.minSelections !== void 0) writer.uint32(24).int32(message.minSelections);
		if (message.maxSelections !== void 0) writer.uint32(32).int32(message.maxSelections);
		if (message.renderAsList !== void 0) writer.uint32(40).bool(message.renderAsList);
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseFieldConfig_Selection();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {
				case 1:
					if (tag !== 10) break;
					message.choices.push(FieldConfig_Selection_Item.decode(reader, reader.uint32()));
					continue;
				case 2:
					if (tag !== 16) break;
					message.multiSelect = reader.bool();
					continue;
				case 3:
					if (tag !== 24) break;
					message.minSelections = reader.int32();
					continue;
				case 4:
					if (tag !== 32) break;
					message.maxSelections = reader.int32();
					continue;
				case 5:
					if (tag !== 40) break;
					message.renderAsList = reader.bool();
					continue;
			}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(object) {
		return {
			choices: globalThis.Array.isArray(object?.choices) ? object.choices.map((e) => FieldConfig_Selection_Item.fromJSON(e)) : [],
			multiSelect: isSet$8(object.multiSelect) ? globalThis.Boolean(object.multiSelect) : void 0,
			minSelections: isSet$8(object.minSelections) ? globalThis.Number(object.minSelections) : void 0,
			maxSelections: isSet$8(object.maxSelections) ? globalThis.Number(object.maxSelections) : void 0,
			renderAsList: isSet$8(object.renderAsList) ? globalThis.Boolean(object.renderAsList) : void 0
		};
	},
	toJSON(message) {
		const obj = {};
		if (message.choices?.length) obj.choices = message.choices.map((e) => FieldConfig_Selection_Item.toJSON(e));
		if (message.multiSelect !== void 0) obj.multiSelect = message.multiSelect;
		if (message.minSelections !== void 0) obj.minSelections = Math.round(message.minSelections);
		if (message.maxSelections !== void 0) obj.maxSelections = Math.round(message.maxSelections);
		if (message.renderAsList !== void 0) obj.renderAsList = message.renderAsList;
		return obj;
	},
	create(base) {
		return FieldConfig_Selection.fromPartial(base ?? {});
	},
	fromPartial(object) {
		const message = createBaseFieldConfig_Selection();
		message.choices = object.choices?.map((e) => FieldConfig_Selection_Item.fromPartial(e)) || [];
		message.multiSelect = object.multiSelect ?? void 0;
		message.minSelections = object.minSelections ?? void 0;
		message.maxSelections = object.maxSelections ?? void 0;
		message.renderAsList = object.renderAsList ?? void 0;
		return message;
	}
};
messageTypeRegistry.set(FieldConfig_Selection.$type, FieldConfig_Selection);
function createBaseFieldConfig_Selection_Item() {
	return {
		label: "",
		value: ""
	};
}
var FieldConfig_Selection_Item = {
	$type: "devvit.ui.form_builder.v1alpha.FieldConfig.Selection.Item",
	encode(message, writer = import_minimal.default.Writer.create()) {
		if (message.label !== "") writer.uint32(10).string(message.label);
		if (message.value !== "") writer.uint32(18).string(message.value);
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseFieldConfig_Selection_Item();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {
				case 1:
					if (tag !== 10) break;
					message.label = reader.string();
					continue;
				case 2:
					if (tag !== 18) break;
					message.value = reader.string();
					continue;
			}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(object) {
		return {
			label: isSet$8(object.label) ? globalThis.String(object.label) : "",
			value: isSet$8(object.value) ? globalThis.String(object.value) : ""
		};
	},
	toJSON(message) {
		const obj = {};
		if (message.label !== "") obj.label = message.label;
		if (message.value !== "") obj.value = message.value;
		return obj;
	},
	create(base) {
		return FieldConfig_Selection_Item.fromPartial(base ?? {});
	},
	fromPartial(object) {
		const message = createBaseFieldConfig_Selection_Item();
		message.label = object.label ?? "";
		message.value = object.value ?? "";
		return message;
	}
};
messageTypeRegistry.set(FieldConfig_Selection_Item.$type, FieldConfig_Selection_Item);
function createBaseFieldConfig_Group() {
	return { fields: [] };
}
var FieldConfig_Group = {
	$type: "devvit.ui.form_builder.v1alpha.FieldConfig.Group",
	encode(message, writer = import_minimal.default.Writer.create()) {
		for (const v of message.fields) FormField.encode(v, writer.uint32(10).fork()).ldelim();
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseFieldConfig_Group();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {
				case 1:
					if (tag !== 10) break;
					message.fields.push(FormField.decode(reader, reader.uint32()));
					continue;
			}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(object) {
		return { fields: globalThis.Array.isArray(object?.fields) ? object.fields.map((e) => FormField.fromJSON(e)) : [] };
	},
	toJSON(message) {
		const obj = {};
		if (message.fields?.length) obj.fields = message.fields.map((e) => FormField.toJSON(e));
		return obj;
	},
	create(base) {
		return FieldConfig_Group.fromPartial(base ?? {});
	},
	fromPartial(object) {
		const message = createBaseFieldConfig_Group();
		message.fields = object.fields?.map((e) => FormField.fromPartial(e)) || [];
		return message;
	}
};
messageTypeRegistry.set(FieldConfig_Group.$type, FieldConfig_Group);
function isSet$8(value) {
	return value !== null && value !== void 0;
}
//#endregion
//#region crystal-cascade/node_modules/@devvit/protos/types/devvit/ui/form_builder/v1alpha/form.js
/**
* #form.ts
*
* Code generated by ts-proto. DO NOT EDIT.
* @packageDocumentation
*/
function createBaseForm() {
	return {
		fields: [],
		title: void 0,
		shortDescription: void 0,
		acceptLabel: void 0,
		cancelLabel: void 0,
		id: void 0
	};
}
var Form = {
	$type: "devvit.ui.form_builder.v1alpha.Form",
	encode(message, writer = import_minimal.default.Writer.create()) {
		for (const v of message.fields) FormField.encode(v, writer.uint32(10).fork()).ldelim();
		if (message.title !== void 0) writer.uint32(18).string(message.title);
		if (message.shortDescription !== void 0) writer.uint32(26).string(message.shortDescription);
		if (message.acceptLabel !== void 0) writer.uint32(34).string(message.acceptLabel);
		if (message.cancelLabel !== void 0) writer.uint32(42).string(message.cancelLabel);
		if (message.id !== void 0) writer.uint32(50).string(message.id);
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseForm();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {
				case 1:
					if (tag !== 10) break;
					message.fields.push(FormField.decode(reader, reader.uint32()));
					continue;
				case 2:
					if (tag !== 18) break;
					message.title = reader.string();
					continue;
				case 3:
					if (tag !== 26) break;
					message.shortDescription = reader.string();
					continue;
				case 4:
					if (tag !== 34) break;
					message.acceptLabel = reader.string();
					continue;
				case 5:
					if (tag !== 42) break;
					message.cancelLabel = reader.string();
					continue;
				case 6:
					if (tag !== 50) break;
					message.id = reader.string();
					continue;
			}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(object) {
		return {
			fields: globalThis.Array.isArray(object?.fields) ? object.fields.map((e) => FormField.fromJSON(e)) : [],
			title: isSet$7(object.title) ? globalThis.String(object.title) : void 0,
			shortDescription: isSet$7(object.shortDescription) ? globalThis.String(object.shortDescription) : void 0,
			acceptLabel: isSet$7(object.acceptLabel) ? globalThis.String(object.acceptLabel) : void 0,
			cancelLabel: isSet$7(object.cancelLabel) ? globalThis.String(object.cancelLabel) : void 0,
			id: isSet$7(object.id) ? globalThis.String(object.id) : void 0
		};
	},
	toJSON(message) {
		const obj = {};
		if (message.fields?.length) obj.fields = message.fields.map((e) => FormField.toJSON(e));
		if (message.title !== void 0) obj.title = message.title;
		if (message.shortDescription !== void 0) obj.shortDescription = message.shortDescription;
		if (message.acceptLabel !== void 0) obj.acceptLabel = message.acceptLabel;
		if (message.cancelLabel !== void 0) obj.cancelLabel = message.cancelLabel;
		if (message.id !== void 0) obj.id = message.id;
		return obj;
	},
	create(base) {
		return Form.fromPartial(base ?? {});
	},
	fromPartial(object) {
		const message = createBaseForm();
		message.fields = object.fields?.map((e) => FormField.fromPartial(e)) || [];
		message.title = object.title ?? void 0;
		message.shortDescription = object.shortDescription ?? void 0;
		message.acceptLabel = object.acceptLabel ?? void 0;
		message.cancelLabel = object.cancelLabel ?? void 0;
		message.id = object.id ?? void 0;
		return message;
	}
};
messageTypeRegistry.set(Form.$type, Form);
function isSet$7(value) {
	return value !== null && value !== void 0;
}
//#endregion
//#region crystal-cascade/node_modules/@devvit/protos/types/devvit/ui/effect_types/v1alpha/show_form.js
/**
* #show_form.ts
*
* Code generated by ts-proto. DO NOT EDIT.
* @packageDocumentation
*/
function createBaseShowFormEffect() {
	return { form: void 0 };
}
var ShowFormEffect = {
	$type: "devvit.ui.effect_types.v1alpha.ShowFormEffect",
	encode(message, writer = import_minimal.default.Writer.create()) {
		if (message.form !== void 0) Form.encode(message.form, writer.uint32(10).fork()).ldelim();
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseShowFormEffect();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {
				case 1:
					if (tag !== 10) break;
					message.form = Form.decode(reader, reader.uint32());
					continue;
			}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(object) {
		return { form: isSet$6(object.form) ? Form.fromJSON(object.form) : void 0 };
	},
	toJSON(message) {
		const obj = {};
		if (message.form !== void 0) obj.form = Form.toJSON(message.form);
		return obj;
	},
	create(base) {
		return ShowFormEffect.fromPartial(base ?? {});
	},
	fromPartial(object) {
		const message = createBaseShowFormEffect();
		message.form = object.form !== void 0 && object.form !== null ? Form.fromPartial(object.form) : void 0;
		return message;
	}
};
messageTypeRegistry.set(ShowFormEffect.$type, ShowFormEffect);
function createBaseFormSubmittedEvent() {
	return {
		results: {},
		formId: void 0
	};
}
var FormSubmittedEvent = {
	$type: "devvit.ui.effect_types.v1alpha.FormSubmittedEvent",
	encode(message, writer = import_minimal.default.Writer.create()) {
		Object.entries(message.results).forEach(([key, value]) => {
			FormSubmittedEvent_ResultsEntry.encode({
				key,
				value
			}, writer.uint32(10).fork()).ldelim();
		});
		if (message.formId !== void 0) writer.uint32(18).string(message.formId);
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseFormSubmittedEvent();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {
				case 1:
					if (tag !== 10) break;
					const entry1 = FormSubmittedEvent_ResultsEntry.decode(reader, reader.uint32());
					if (entry1.value !== void 0) message.results[entry1.key] = entry1.value;
					continue;
				case 2:
					if (tag !== 18) break;
					message.formId = reader.string();
					continue;
			}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(object) {
		return {
			results: isObject$1(object.results) ? Object.entries(object.results).reduce((acc, [key, value]) => {
				acc[key] = FormFieldValue.fromJSON(value);
				return acc;
			}, {}) : {},
			formId: isSet$6(object.formId) ? globalThis.String(object.formId) : void 0
		};
	},
	toJSON(message) {
		const obj = {};
		if (message.results) {
			const entries = Object.entries(message.results);
			if (entries.length > 0) {
				obj.results = {};
				entries.forEach(([k, v]) => {
					obj.results[k] = FormFieldValue.toJSON(v);
				});
			}
		}
		if (message.formId !== void 0) obj.formId = message.formId;
		return obj;
	},
	create(base) {
		return FormSubmittedEvent.fromPartial(base ?? {});
	},
	fromPartial(object) {
		const message = createBaseFormSubmittedEvent();
		message.results = Object.entries(object.results ?? {}).reduce((acc, [key, value]) => {
			if (value !== void 0) acc[key] = FormFieldValue.fromPartial(value);
			return acc;
		}, {});
		message.formId = object.formId ?? void 0;
		return message;
	}
};
messageTypeRegistry.set(FormSubmittedEvent.$type, FormSubmittedEvent);
function createBaseFormSubmittedEvent_ResultsEntry() {
	return {
		key: "",
		value: void 0
	};
}
var FormSubmittedEvent_ResultsEntry = {
	$type: "devvit.ui.effect_types.v1alpha.FormSubmittedEvent.ResultsEntry",
	encode(message, writer = import_minimal.default.Writer.create()) {
		if (message.key !== "") writer.uint32(10).string(message.key);
		if (message.value !== void 0) FormFieldValue.encode(message.value, writer.uint32(18).fork()).ldelim();
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseFormSubmittedEvent_ResultsEntry();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {
				case 1:
					if (tag !== 10) break;
					message.key = reader.string();
					continue;
				case 2:
					if (tag !== 18) break;
					message.value = FormFieldValue.decode(reader, reader.uint32());
					continue;
			}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(object) {
		return {
			key: isSet$6(object.key) ? globalThis.String(object.key) : "",
			value: isSet$6(object.value) ? FormFieldValue.fromJSON(object.value) : void 0
		};
	},
	toJSON(message) {
		const obj = {};
		if (message.key !== "") obj.key = message.key;
		if (message.value !== void 0) obj.value = FormFieldValue.toJSON(message.value);
		return obj;
	},
	create(base) {
		return FormSubmittedEvent_ResultsEntry.fromPartial(base ?? {});
	},
	fromPartial(object) {
		const message = createBaseFormSubmittedEvent_ResultsEntry();
		message.key = object.key ?? "";
		message.value = object.value !== void 0 && object.value !== null ? FormFieldValue.fromPartial(object.value) : void 0;
		return message;
	}
};
messageTypeRegistry.set(FormSubmittedEvent_ResultsEntry.$type, FormSubmittedEvent_ResultsEntry);
function createBaseFormCanceledEvent() {
	return { formId: void 0 };
}
var FormCanceledEvent = {
	$type: "devvit.ui.effect_types.v1alpha.FormCanceledEvent",
	encode(message, writer = import_minimal.default.Writer.create()) {
		if (message.formId !== void 0) writer.uint32(10).string(message.formId);
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseFormCanceledEvent();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {
				case 1:
					if (tag !== 10) break;
					message.formId = reader.string();
					continue;
			}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(object) {
		return { formId: isSet$6(object.formId) ? globalThis.String(object.formId) : void 0 };
	},
	toJSON(message) {
		const obj = {};
		if (message.formId !== void 0) obj.formId = message.formId;
		return obj;
	},
	create(base) {
		return FormCanceledEvent.fromPartial(base ?? {});
	},
	fromPartial(object) {
		const message = createBaseFormCanceledEvent();
		message.formId = object.formId ?? void 0;
		return message;
	}
};
messageTypeRegistry.set(FormCanceledEvent.$type, FormCanceledEvent);
function isObject$1(value) {
	return typeof value === "object" && value !== null;
}
function isSet$6(value) {
	return value !== null && value !== void 0;
}
//#endregion
//#region crystal-cascade/node_modules/@devvit/protos/types/devvit/ui/effect_types/v1alpha/update_request_context.js
/**
* #update_request_context.ts
*
* Code generated by ts-proto. DO NOT EDIT.
* @packageDocumentation
*/
function createBaseUpdateRequestContextEffect() {
	return {};
}
var UpdateRequestContextEffect = {
	$type: "devvit.ui.effect_types.v1alpha.UpdateRequestContextEffect",
	encode(_, writer = import_minimal.default.Writer.create()) {
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseUpdateRequestContextEffect();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(_) {
		return {};
	},
	toJSON(_) {
		return {};
	},
	create(base) {
		return UpdateRequestContextEffect.fromPartial(base ?? {});
	},
	fromPartial(_) {
		return createBaseUpdateRequestContextEffect();
	}
};
messageTypeRegistry.set(UpdateRequestContextEffect.$type, UpdateRequestContextEffect);
function createBaseUpdateRequestContextEvent() {
	return { signedRequestContext: "" };
}
var UpdateRequestContextEvent = {
	$type: "devvit.ui.effect_types.v1alpha.UpdateRequestContextEvent",
	encode(message, writer = import_minimal.default.Writer.create()) {
		if (message.signedRequestContext !== "") writer.uint32(10).string(message.signedRequestContext);
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseUpdateRequestContextEvent();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {
				case 1:
					if (tag !== 10) break;
					message.signedRequestContext = reader.string();
					continue;
			}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(object) {
		return { signedRequestContext: isSet$5(object.signedRequestContext) ? globalThis.String(object.signedRequestContext) : "" };
	},
	toJSON(message) {
		const obj = {};
		if (message.signedRequestContext !== "") obj.signedRequestContext = message.signedRequestContext;
		return obj;
	},
	create(base) {
		return UpdateRequestContextEvent.fromPartial(base ?? {});
	},
	fromPartial(object) {
		const message = createBaseUpdateRequestContextEvent();
		message.signedRequestContext = object.signedRequestContext ?? "";
		return message;
	}
};
messageTypeRegistry.set(UpdateRequestContextEvent.$type, UpdateRequestContextEvent);
function isSet$5(value) {
	return value !== null && value !== void 0;
}
//#endregion
//#region crystal-cascade/node_modules/@devvit/protos/types/google/protobuf/empty.js
/**
* #empty.ts
*
* Code generated by ts-proto. DO NOT EDIT.
* @packageDocumentation
*/
function createBaseEmpty() {
	return {};
}
var Empty = {
	$type: "google.protobuf.Empty",
	encode(_, writer = import_minimal.default.Writer.create()) {
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseEmpty();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(_) {
		return {};
	},
	toJSON(_) {
		return {};
	},
	create(base) {
		return Empty.fromPartial(base ?? {});
	},
	fromPartial(_) {
		return createBaseEmpty();
	}
};
messageTypeRegistry.set(Empty.$type, Empty);
//#endregion
//#region crystal-cascade/node_modules/@devvit/protos/types/devvit/events/v1alpha/realtime.js
/**
* #realtime.ts
*
* Code generated by ts-proto. DO NOT EDIT.
* @packageDocumentation
*/
function createBaseRealtimeRequest() {
	return { channels: [] };
}
var RealtimeRequest = {
	$type: "devvit.events.v1alpha.RealtimeRequest",
	encode(message, writer = import_minimal.default.Writer.create()) {
		for (const v of message.channels) writer.uint32(10).string(v);
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseRealtimeRequest();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {
				case 1:
					if (tag !== 10) break;
					message.channels.push(reader.string());
					continue;
			}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(object) {
		return { channels: globalThis.Array.isArray(object?.channels) ? object.channels.map((e) => globalThis.String(e)) : [] };
	},
	toJSON(message) {
		const obj = {};
		if (message.channels?.length) obj.channels = message.channels;
		return obj;
	},
	create(base) {
		return RealtimeRequest.fromPartial(base ?? {});
	},
	fromPartial(object) {
		const message = createBaseRealtimeRequest();
		message.channels = object.channels?.map((e) => e) || [];
		return message;
	}
};
messageTypeRegistry.set(RealtimeRequest.$type, RealtimeRequest);
function createBaseRealtimeEvent() {
	return {
		channel: "",
		data: void 0
	};
}
var RealtimeEvent = {
	$type: "devvit.events.v1alpha.RealtimeEvent",
	encode(message, writer = import_minimal.default.Writer.create()) {
		if (message.channel !== "") writer.uint32(10).string(message.channel);
		if (message.data !== void 0) Struct.encode(Struct.wrap(message.data), writer.uint32(18).fork()).ldelim();
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseRealtimeEvent();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {
				case 1:
					if (tag !== 10) break;
					message.channel = reader.string();
					continue;
				case 2:
					if (tag !== 18) break;
					message.data = Struct.unwrap(Struct.decode(reader, reader.uint32()));
					continue;
			}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(object) {
		return {
			channel: isSet$4(object.channel) ? globalThis.String(object.channel) : "",
			data: isObject(object.data) ? object.data : void 0
		};
	},
	toJSON(message) {
		const obj = {};
		if (message.channel !== "") obj.channel = message.channel;
		if (message.data !== void 0) obj.data = message.data;
		return obj;
	},
	create(base) {
		return RealtimeEvent.fromPartial(base ?? {});
	},
	fromPartial(object) {
		const message = createBaseRealtimeEvent();
		message.channel = object.channel ?? "";
		message.data = object.data ?? void 0;
		return message;
	}
};
messageTypeRegistry.set(RealtimeEvent.$type, RealtimeEvent);
function isObject(value) {
	return typeof value === "object" && value !== null;
}
function isSet$4(value) {
	return value !== null && value !== void 0;
}
//#endregion
//#region crystal-cascade/node_modules/@devvit/protos/types/devvit/ui/effects/v1alpha/realtime_subscriptions.js
/**
* #realtime_subscriptions.ts
*
* Code generated by ts-proto. DO NOT EDIT.
* @packageDocumentation
*/
var RealtimeSubscriptionStatus$1;
(function(RealtimeSubscriptionStatus) {
	RealtimeSubscriptionStatus[RealtimeSubscriptionStatus["REALTIME_SUBSCRIBED"] = 0] = "REALTIME_SUBSCRIBED";
	RealtimeSubscriptionStatus[RealtimeSubscriptionStatus["REALTIME_UNSUBSCRIBED"] = 1] = "REALTIME_UNSUBSCRIBED";
	RealtimeSubscriptionStatus[RealtimeSubscriptionStatus["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(RealtimeSubscriptionStatus$1 || (RealtimeSubscriptionStatus$1 = {}));
function realtimeSubscriptionStatusFromJSON(object) {
	switch (object) {
		case 0:
		case "REALTIME_SUBSCRIBED": return RealtimeSubscriptionStatus$1.REALTIME_SUBSCRIBED;
		case 1:
		case "REALTIME_UNSUBSCRIBED": return RealtimeSubscriptionStatus$1.REALTIME_UNSUBSCRIBED;
		default: return RealtimeSubscriptionStatus$1.UNRECOGNIZED;
	}
}
function realtimeSubscriptionStatusToJSON(object) {
	switch (object) {
		case RealtimeSubscriptionStatus$1.REALTIME_SUBSCRIBED: return 0;
		case RealtimeSubscriptionStatus$1.REALTIME_UNSUBSCRIBED: return 1;
		case RealtimeSubscriptionStatus$1.UNRECOGNIZED:
		default: return -1;
	}
}
function createBaseRealtimeSubscriptionsEffect() {
	return { subscriptionIds: [] };
}
var RealtimeSubscriptionsEffect = {
	$type: "devvit.ui.effects.v1alpha.RealtimeSubscriptionsEffect",
	encode(message, writer = import_minimal.default.Writer.create()) {
		for (const v of message.subscriptionIds) writer.uint32(10).string(v);
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseRealtimeSubscriptionsEffect();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {
				case 1:
					if (tag !== 10) break;
					message.subscriptionIds.push(reader.string());
					continue;
			}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(object) {
		return { subscriptionIds: globalThis.Array.isArray(object?.subscriptionIds) ? object.subscriptionIds.map((e) => globalThis.String(e)) : [] };
	},
	toJSON(message) {
		const obj = {};
		if (message.subscriptionIds?.length) obj.subscriptionIds = message.subscriptionIds;
		return obj;
	},
	create(base) {
		return RealtimeSubscriptionsEffect.fromPartial(base ?? {});
	},
	fromPartial(object) {
		const message = createBaseRealtimeSubscriptionsEffect();
		message.subscriptionIds = object.subscriptionIds?.map((e) => e) || [];
		return message;
	}
};
messageTypeRegistry.set(RealtimeSubscriptionsEffect.$type, RealtimeSubscriptionsEffect);
function createBaseRealtimeSubscriptionEvent() {
	return {
		event: void 0,
		status: void 0
	};
}
var RealtimeSubscriptionEvent = {
	$type: "devvit.ui.effects.v1alpha.RealtimeSubscriptionEvent",
	encode(message, writer = import_minimal.default.Writer.create()) {
		if (message.event !== void 0) RealtimeEvent.encode(message.event, writer.uint32(10).fork()).ldelim();
		if (message.status !== void 0) writer.uint32(16).int32(message.status);
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseRealtimeSubscriptionEvent();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {
				case 1:
					if (tag !== 10) break;
					message.event = RealtimeEvent.decode(reader, reader.uint32());
					continue;
				case 2:
					if (tag !== 16) break;
					message.status = reader.int32();
					continue;
			}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(object) {
		return {
			event: isSet$3(object.event) ? RealtimeEvent.fromJSON(object.event) : void 0,
			status: isSet$3(object.status) ? realtimeSubscriptionStatusFromJSON(object.status) : void 0
		};
	},
	toJSON(message) {
		const obj = {};
		if (message.event !== void 0) obj.event = RealtimeEvent.toJSON(message.event);
		if (message.status !== void 0) obj.status = realtimeSubscriptionStatusToJSON(message.status);
		return obj;
	},
	create(base) {
		return RealtimeSubscriptionEvent.fromPartial(base ?? {});
	},
	fromPartial(object) {
		const message = createBaseRealtimeSubscriptionEvent();
		message.event = object.event !== void 0 && object.event !== null ? RealtimeEvent.fromPartial(object.event) : void 0;
		message.status = object.status ?? void 0;
		return message;
	}
};
messageTypeRegistry.set(RealtimeSubscriptionEvent.$type, RealtimeSubscriptionEvent);
function isSet$3(value) {
	return value !== null && value !== void 0;
}
//#endregion
//#region crystal-cascade/node_modules/@devvit/protos/types/devvit/ui/effects/web_view/v1alpha/immersive_mode.js
/**
* #immersive_mode.ts
*
* Code generated by ts-proto. DO NOT EDIT.
* @packageDocumentation
*/
/** How a Devvit app is presented. */
var WebViewImmersiveMode;
(function(WebViewImmersiveMode) {
	WebViewImmersiveMode[WebViewImmersiveMode["UNSPECIFIED"] = 0] = "UNSPECIFIED";
	/**
	* INLINE_MODE - Conventional post presentation. May appear adjacent other posts in a feed
	* or with comments.
	*/
	WebViewImmersiveMode[WebViewImmersiveMode["INLINE_MODE"] = 1] = "INLINE_MODE";
	/**
	* IMMERSIVE_MODE - Large modal expanded presentation. Appears popped out from the rest of the
	* Reddit experience which is disabled.
	*/
	WebViewImmersiveMode[WebViewImmersiveMode["IMMERSIVE_MODE"] = 2] = "IMMERSIVE_MODE";
	WebViewImmersiveMode[WebViewImmersiveMode["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(WebViewImmersiveMode || (WebViewImmersiveMode = {}));
function webViewImmersiveModeFromJSON(object) {
	switch (object) {
		case 0:
		case "UNSPECIFIED": return WebViewImmersiveMode.UNSPECIFIED;
		case 1:
		case "INLINE_MODE": return WebViewImmersiveMode.INLINE_MODE;
		case 2:
		case "IMMERSIVE_MODE": return WebViewImmersiveMode.IMMERSIVE_MODE;
		default: return WebViewImmersiveMode.UNRECOGNIZED;
	}
}
function webViewImmersiveModeToJSON(object) {
	switch (object) {
		case WebViewImmersiveMode.UNSPECIFIED: return 0;
		case WebViewImmersiveMode.INLINE_MODE: return 1;
		case WebViewImmersiveMode.IMMERSIVE_MODE: return 2;
		case WebViewImmersiveMode.UNRECOGNIZED:
		default: return -1;
	}
}
function createBaseWebViewImmersiveModeEffect() {
	return {
		immersiveMode: 0,
		entryUrl: void 0
	};
}
var WebViewImmersiveModeEffect = {
	$type: "devvit.ui.effects.web_view.v1alpha.WebViewImmersiveModeEffect",
	encode(message, writer = import_minimal.default.Writer.create()) {
		if (message.immersiveMode !== 0) writer.uint32(8).int32(message.immersiveMode);
		if (message.entryUrl !== void 0) writer.uint32(18).string(message.entryUrl);
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseWebViewImmersiveModeEffect();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {
				case 1:
					if (tag !== 8) break;
					message.immersiveMode = reader.int32();
					continue;
				case 2:
					if (tag !== 18) break;
					message.entryUrl = reader.string();
					continue;
			}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(object) {
		return {
			immersiveMode: isSet$2(object.immersiveMode) ? webViewImmersiveModeFromJSON(object.immersiveMode) : 0,
			entryUrl: isSet$2(object.entryUrl) ? globalThis.String(object.entryUrl) : void 0
		};
	},
	toJSON(message) {
		const obj = {};
		if (message.immersiveMode !== 0) obj.immersiveMode = webViewImmersiveModeToJSON(message.immersiveMode);
		if (message.entryUrl !== void 0) obj.entryUrl = message.entryUrl;
		return obj;
	},
	create(base) {
		return WebViewImmersiveModeEffect.fromPartial(base ?? {});
	},
	fromPartial(object) {
		const message = createBaseWebViewImmersiveModeEffect();
		message.immersiveMode = object.immersiveMode ?? 0;
		message.entryUrl = object.entryUrl ?? void 0;
		return message;
	}
};
messageTypeRegistry.set(WebViewImmersiveModeEffect.$type, WebViewImmersiveModeEffect);
function isSet$2(value) {
	return value !== null && value !== void 0;
}
//#endregion
//#region crystal-cascade/node_modules/@devvit/protos/types/devvit/ui/effects/web_view/v1alpha/screenshot.js
/**
* #screenshot.ts
*
* Code generated by ts-proto. DO NOT EDIT.
* @packageDocumentation
*/
function createBaseWebViewScreenshotRequestEvent() {
	return {};
}
var WebViewScreenshotRequestEvent = {
	$type: "devvit.ui.effects.web_view.v1alpha.WebViewScreenshotRequestEvent",
	encode(_, writer = import_minimal.default.Writer.create()) {
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseWebViewScreenshotRequestEvent();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(_) {
		return {};
	},
	toJSON(_) {
		return {};
	},
	create(base) {
		return WebViewScreenshotRequestEvent.fromPartial(base ?? {});
	},
	fromPartial(_) {
		return createBaseWebViewScreenshotRequestEvent();
	}
};
messageTypeRegistry.set(WebViewScreenshotRequestEvent.$type, WebViewScreenshotRequestEvent);
function createBaseWebViewScreenshotResponseEffect() {
	return {
		dataUrl: void 0,
		error: void 0
	};
}
var WebViewScreenshotResponseEffect = {
	$type: "devvit.ui.effects.web_view.v1alpha.WebViewScreenshotResponseEffect",
	encode(message, writer = import_minimal.default.Writer.create()) {
		if (message.dataUrl !== void 0) writer.uint32(10).string(message.dataUrl);
		if (message.error !== void 0) writer.uint32(18).string(message.error);
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseWebViewScreenshotResponseEffect();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {
				case 1:
					if (tag !== 10) break;
					message.dataUrl = reader.string();
					continue;
				case 2:
					if (tag !== 18) break;
					message.error = reader.string();
					continue;
			}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(object) {
		return {
			dataUrl: isSet$1(object.dataUrl) ? globalThis.String(object.dataUrl) : void 0,
			error: isSet$1(object.error) ? globalThis.String(object.error) : void 0
		};
	},
	toJSON(message) {
		const obj = {};
		if (message.dataUrl !== void 0) obj.dataUrl = message.dataUrl;
		if (message.error !== void 0) obj.error = message.error;
		return obj;
	},
	create(base) {
		return WebViewScreenshotResponseEffect.fromPartial(base ?? {});
	},
	fromPartial(object) {
		const message = createBaseWebViewScreenshotResponseEffect();
		message.dataUrl = object.dataUrl ?? void 0;
		message.error = object.error ?? void 0;
		return message;
	}
};
messageTypeRegistry.set(WebViewScreenshotResponseEffect.$type, WebViewScreenshotResponseEffect);
function isSet$1(value) {
	return value !== null && value !== void 0;
}
//#endregion
//#region crystal-cascade/node_modules/@devvit/protos/types/devvit/ui/events/v1alpha/web_view.js
/**
* #web_view.ts
*
* Code generated by ts-proto. DO NOT EDIT.
* @packageDocumentation
*/
/**
* Blocks only.
*
* @deprecated
*/
var WebViewVisibility;
(function(WebViewVisibility) {
	WebViewVisibility[WebViewVisibility["WEBVIEW_VISIBLE"] = 0] = "WEBVIEW_VISIBLE";
	WebViewVisibility[WebViewVisibility["WEBVIEW_HIDDEN"] = 1] = "WEBVIEW_HIDDEN";
	WebViewVisibility[WebViewVisibility["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(WebViewVisibility || (WebViewVisibility = {}));
function webViewVisibilityFromJSON(object) {
	switch (object) {
		case 0:
		case "WEBVIEW_VISIBLE": return WebViewVisibility.WEBVIEW_VISIBLE;
		case 1:
		case "WEBVIEW_HIDDEN": return WebViewVisibility.WEBVIEW_HIDDEN;
		default: return WebViewVisibility.UNRECOGNIZED;
	}
}
function webViewVisibilityToJSON(object) {
	switch (object) {
		case WebViewVisibility.WEBVIEW_VISIBLE: return 0;
		case WebViewVisibility.WEBVIEW_HIDDEN: return 1;
		case WebViewVisibility.UNRECOGNIZED:
		default: return -1;
	}
}
function createBaseWebViewPostMessageEvent() {
	return {
		message: void 0,
		jsonString: ""
	};
}
var WebViewPostMessageEvent = {
	$type: "devvit.ui.events.v1alpha.WebViewPostMessageEvent",
	encode(message, writer = import_minimal.default.Writer.create()) {
		if (message.message !== void 0) Value.encode(Value.wrap(message.message), writer.uint32(10).fork()).ldelim();
		if (message.jsonString !== "") writer.uint32(18).string(message.jsonString);
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseWebViewPostMessageEvent();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {
				case 1:
					if (tag !== 10) break;
					message.message = Value.unwrap(Value.decode(reader, reader.uint32()));
					continue;
				case 2:
					if (tag !== 18) break;
					message.jsonString = reader.string();
					continue;
			}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(object) {
		return {
			message: isSet(object?.message) ? object.message : void 0,
			jsonString: isSet(object.jsonString) ? globalThis.String(object.jsonString) : ""
		};
	},
	toJSON(message) {
		const obj = {};
		if (message.message !== void 0) obj.message = message.message;
		if (message.jsonString !== "") obj.jsonString = message.jsonString;
		return obj;
	},
	create(base) {
		return WebViewPostMessageEvent.fromPartial(base ?? {});
	},
	fromPartial(object) {
		const message = createBaseWebViewPostMessageEvent();
		message.message = object.message ?? void 0;
		message.jsonString = object.jsonString ?? "";
		return message;
	}
};
messageTypeRegistry.set(WebViewPostMessageEvent.$type, WebViewPostMessageEvent);
function createBaseWebViewFullScreenEvent() {
	return { visibility: 0 };
}
var WebViewFullScreenEvent = {
	$type: "devvit.ui.events.v1alpha.WebViewFullScreenEvent",
	encode(message, writer = import_minimal.default.Writer.create()) {
		if (message.visibility !== 0) writer.uint32(8).int32(message.visibility);
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseWebViewFullScreenEvent();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {
				case 1:
					if (tag !== 8) break;
					message.visibility = reader.int32();
					continue;
			}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(object) {
		return { visibility: isSet(object.visibility) ? webViewVisibilityFromJSON(object.visibility) : 0 };
	},
	toJSON(message) {
		const obj = {};
		if (message.visibility !== 0) obj.visibility = webViewVisibilityToJSON(message.visibility);
		return obj;
	},
	create(base) {
		return WebViewFullScreenEvent.fromPartial(base ?? {});
	},
	fromPartial(object) {
		const message = createBaseWebViewFullScreenEvent();
		message.visibility = object.visibility ?? 0;
		return message;
	}
};
messageTypeRegistry.set(WebViewFullScreenEvent.$type, WebViewFullScreenEvent);
function createBaseWebViewEvent() {
	return {
		postMessage: void 0,
		fullScreen: void 0
	};
}
var WebViewEvent = {
	$type: "devvit.ui.events.v1alpha.WebViewEvent",
	encode(message, writer = import_minimal.default.Writer.create()) {
		if (message.postMessage !== void 0) WebViewPostMessageEvent.encode(message.postMessage, writer.uint32(10).fork()).ldelim();
		if (message.fullScreen !== void 0) WebViewFullScreenEvent.encode(message.fullScreen, writer.uint32(18).fork()).ldelim();
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseWebViewEvent();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {
				case 1:
					if (tag !== 10) break;
					message.postMessage = WebViewPostMessageEvent.decode(reader, reader.uint32());
					continue;
				case 2:
					if (tag !== 18) break;
					message.fullScreen = WebViewFullScreenEvent.decode(reader, reader.uint32());
					continue;
			}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(object) {
		return {
			postMessage: isSet(object.postMessage) ? WebViewPostMessageEvent.fromJSON(object.postMessage) : void 0,
			fullScreen: isSet(object.fullScreen) ? WebViewFullScreenEvent.fromJSON(object.fullScreen) : void 0
		};
	},
	toJSON(message) {
		const obj = {};
		if (message.postMessage !== void 0) obj.postMessage = WebViewPostMessageEvent.toJSON(message.postMessage);
		if (message.fullScreen !== void 0) obj.fullScreen = WebViewFullScreenEvent.toJSON(message.fullScreen);
		return obj;
	},
	create(base) {
		return WebViewEvent.fromPartial(base ?? {});
	},
	fromPartial(object) {
		const message = createBaseWebViewEvent();
		message.postMessage = object.postMessage !== void 0 && object.postMessage !== null ? WebViewPostMessageEvent.fromPartial(object.postMessage) : void 0;
		message.fullScreen = object.fullScreen !== void 0 && object.fullScreen !== null ? WebViewFullScreenEvent.fromPartial(object.fullScreen) : void 0;
		return message;
	}
};
messageTypeRegistry.set(WebViewEvent.$type, WebViewEvent);
function createBaseWebViewImmersiveModeChangedEvent() {
	return { immersiveMode: 0 };
}
var WebViewImmersiveModeChangedEvent = {
	$type: "devvit.ui.events.v1alpha.WebViewImmersiveModeChangedEvent",
	encode(message, writer = import_minimal.default.Writer.create()) {
		if (message.immersiveMode !== 0) writer.uint32(8).int32(message.immersiveMode);
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseWebViewImmersiveModeChangedEvent();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {
				case 1:
					if (tag !== 8) break;
					message.immersiveMode = reader.int32();
					continue;
			}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(object) {
		return { immersiveMode: isSet(object.immersiveMode) ? webViewImmersiveModeFromJSON(object.immersiveMode) : 0 };
	},
	toJSON(message) {
		const obj = {};
		if (message.immersiveMode !== 0) obj.immersiveMode = webViewImmersiveModeToJSON(message.immersiveMode);
		return obj;
	},
	create(base) {
		return WebViewImmersiveModeChangedEvent.fromPartial(base ?? {});
	},
	fromPartial(object) {
		const message = createBaseWebViewImmersiveModeChangedEvent();
		message.immersiveMode = object.immersiveMode ?? 0;
		return message;
	}
};
messageTypeRegistry.set(WebViewImmersiveModeChangedEvent.$type, WebViewImmersiveModeChangedEvent);
function createBaseWebViewInternalEventMessage() {
	return {
		id: "",
		formCanceled: void 0,
		formSubmitted: void 0,
		realtimeEvent: void 0,
		immersiveModeEvent: void 0,
		consentStatus: void 0,
		orderResult: void 0,
		updateRequestContext: void 0,
		screenshotRequest: void 0
	};
}
var WebViewInternalEventMessage = {
	$type: "devvit.ui.events.v1alpha.WebViewInternalEventMessage",
	encode(message, writer = import_minimal.default.Writer.create()) {
		if (message.id !== "") writer.uint32(10).string(message.id);
		if (message.formCanceled !== void 0) FormCanceledEvent.encode(message.formCanceled, writer.uint32(18).fork()).ldelim();
		if (message.formSubmitted !== void 0) FormSubmittedEvent.encode(message.formSubmitted, writer.uint32(26).fork()).ldelim();
		if (message.realtimeEvent !== void 0) RealtimeSubscriptionEvent.encode(message.realtimeEvent, writer.uint32(34).fork()).ldelim();
		if (message.immersiveModeEvent !== void 0) WebViewImmersiveModeChangedEvent.encode(message.immersiveModeEvent, writer.uint32(42).fork()).ldelim();
		if (message.consentStatus !== void 0) ConsentStatusEvent.encode(message.consentStatus, writer.uint32(58).fork()).ldelim();
		if (message.orderResult !== void 0) OrderResultEvent.encode(message.orderResult, writer.uint32(66).fork()).ldelim();
		if (message.updateRequestContext !== void 0) UpdateRequestContextEvent.encode(message.updateRequestContext, writer.uint32(74).fork()).ldelim();
		if (message.screenshotRequest !== void 0) WebViewScreenshotRequestEvent.encode(message.screenshotRequest, writer.uint32(82).fork()).ldelim();
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseWebViewInternalEventMessage();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {
				case 1:
					if (tag !== 10) break;
					message.id = reader.string();
					continue;
				case 2:
					if (tag !== 18) break;
					message.formCanceled = FormCanceledEvent.decode(reader, reader.uint32());
					continue;
				case 3:
					if (tag !== 26) break;
					message.formSubmitted = FormSubmittedEvent.decode(reader, reader.uint32());
					continue;
				case 4:
					if (tag !== 34) break;
					message.realtimeEvent = RealtimeSubscriptionEvent.decode(reader, reader.uint32());
					continue;
				case 5:
					if (tag !== 42) break;
					message.immersiveModeEvent = WebViewImmersiveModeChangedEvent.decode(reader, reader.uint32());
					continue;
				case 7:
					if (tag !== 58) break;
					message.consentStatus = ConsentStatusEvent.decode(reader, reader.uint32());
					continue;
				case 8:
					if (tag !== 66) break;
					message.orderResult = OrderResultEvent.decode(reader, reader.uint32());
					continue;
				case 9:
					if (tag !== 74) break;
					message.updateRequestContext = UpdateRequestContextEvent.decode(reader, reader.uint32());
					continue;
				case 10:
					if (tag !== 82) break;
					message.screenshotRequest = WebViewScreenshotRequestEvent.decode(reader, reader.uint32());
					continue;
			}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(object) {
		return {
			id: isSet(object.id) ? globalThis.String(object.id) : "",
			formCanceled: isSet(object.formCanceled) ? FormCanceledEvent.fromJSON(object.formCanceled) : void 0,
			formSubmitted: isSet(object.formSubmitted) ? FormSubmittedEvent.fromJSON(object.formSubmitted) : void 0,
			realtimeEvent: isSet(object.realtimeEvent) ? RealtimeSubscriptionEvent.fromJSON(object.realtimeEvent) : void 0,
			immersiveModeEvent: isSet(object.immersiveModeEvent) ? WebViewImmersiveModeChangedEvent.fromJSON(object.immersiveModeEvent) : void 0,
			consentStatus: isSet(object.consentStatus) ? ConsentStatusEvent.fromJSON(object.consentStatus) : void 0,
			orderResult: isSet(object.orderResult) ? OrderResultEvent.fromJSON(object.orderResult) : void 0,
			updateRequestContext: isSet(object.updateRequestContext) ? UpdateRequestContextEvent.fromJSON(object.updateRequestContext) : void 0,
			screenshotRequest: isSet(object.screenshotRequest) ? WebViewScreenshotRequestEvent.fromJSON(object.screenshotRequest) : void 0
		};
	},
	toJSON(message) {
		const obj = {};
		if (message.id !== "") obj.id = message.id;
		if (message.formCanceled !== void 0) obj.formCanceled = FormCanceledEvent.toJSON(message.formCanceled);
		if (message.formSubmitted !== void 0) obj.formSubmitted = FormSubmittedEvent.toJSON(message.formSubmitted);
		if (message.realtimeEvent !== void 0) obj.realtimeEvent = RealtimeSubscriptionEvent.toJSON(message.realtimeEvent);
		if (message.immersiveModeEvent !== void 0) obj.immersiveModeEvent = WebViewImmersiveModeChangedEvent.toJSON(message.immersiveModeEvent);
		if (message.consentStatus !== void 0) obj.consentStatus = ConsentStatusEvent.toJSON(message.consentStatus);
		if (message.orderResult !== void 0) obj.orderResult = OrderResultEvent.toJSON(message.orderResult);
		if (message.updateRequestContext !== void 0) obj.updateRequestContext = UpdateRequestContextEvent.toJSON(message.updateRequestContext);
		if (message.screenshotRequest !== void 0) obj.screenshotRequest = WebViewScreenshotRequestEvent.toJSON(message.screenshotRequest);
		return obj;
	},
	create(base) {
		return WebViewInternalEventMessage.fromPartial(base ?? {});
	},
	fromPartial(object) {
		const message = createBaseWebViewInternalEventMessage();
		message.id = object.id ?? "";
		message.formCanceled = object.formCanceled !== void 0 && object.formCanceled !== null ? FormCanceledEvent.fromPartial(object.formCanceled) : void 0;
		message.formSubmitted = object.formSubmitted !== void 0 && object.formSubmitted !== null ? FormSubmittedEvent.fromPartial(object.formSubmitted) : void 0;
		message.realtimeEvent = object.realtimeEvent !== void 0 && object.realtimeEvent !== null ? RealtimeSubscriptionEvent.fromPartial(object.realtimeEvent) : void 0;
		message.immersiveModeEvent = object.immersiveModeEvent !== void 0 && object.immersiveModeEvent !== null ? WebViewImmersiveModeChangedEvent.fromPartial(object.immersiveModeEvent) : void 0;
		message.consentStatus = object.consentStatus !== void 0 && object.consentStatus !== null ? ConsentStatusEvent.fromPartial(object.consentStatus) : void 0;
		message.orderResult = object.orderResult !== void 0 && object.orderResult !== null ? OrderResultEvent.fromPartial(object.orderResult) : void 0;
		message.updateRequestContext = object.updateRequestContext !== void 0 && object.updateRequestContext !== null ? UpdateRequestContextEvent.fromPartial(object.updateRequestContext) : void 0;
		message.screenshotRequest = object.screenshotRequest !== void 0 && object.screenshotRequest !== null ? WebViewScreenshotRequestEvent.fromPartial(object.screenshotRequest) : void 0;
		return message;
	}
};
messageTypeRegistry.set(WebViewInternalEventMessage.$type, WebViewInternalEventMessage);
function createBaseWebViewMessageEvent() {
	return { data: void 0 };
}
var WebViewMessageEvent = {
	$type: "devvit.ui.events.v1alpha.WebViewMessageEvent",
	encode(message, writer = import_minimal.default.Writer.create()) {
		if (message.data !== void 0) WebViewMessageEvent_MessageData.encode(message.data, writer.uint32(10).fork()).ldelim();
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseWebViewMessageEvent();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {
				case 1:
					if (tag !== 10) break;
					message.data = WebViewMessageEvent_MessageData.decode(reader, reader.uint32());
					continue;
			}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(object) {
		return { data: isSet(object.data) ? WebViewMessageEvent_MessageData.fromJSON(object.data) : void 0 };
	},
	toJSON(message) {
		const obj = {};
		if (message.data !== void 0) obj.data = WebViewMessageEvent_MessageData.toJSON(message.data);
		return obj;
	},
	create(base) {
		return WebViewMessageEvent.fromPartial(base ?? {});
	},
	fromPartial(object) {
		const message = createBaseWebViewMessageEvent();
		message.data = object.data !== void 0 && object.data !== null ? WebViewMessageEvent_MessageData.fromPartial(object.data) : void 0;
		return message;
	}
};
messageTypeRegistry.set(WebViewMessageEvent.$type, WebViewMessageEvent);
function createBaseWebViewMessageEvent_MessageData() {
	return {
		type: "",
		data: void 0
	};
}
var WebViewMessageEvent_MessageData = {
	$type: "devvit.ui.events.v1alpha.WebViewMessageEvent.MessageData",
	encode(message, writer = import_minimal.default.Writer.create()) {
		if (message.type !== "") writer.uint32(10).string(message.type);
		if (message.data !== void 0) WebViewInternalEventMessage.encode(message.data, writer.uint32(18).fork()).ldelim();
		return writer;
	},
	decode(input, length) {
		const reader = input instanceof import_minimal.default.Reader ? input : import_minimal.default.Reader.create(input);
		let end = length === void 0 ? reader.len : reader.pos + length;
		const message = createBaseWebViewMessageEvent_MessageData();
		while (reader.pos < end) {
			const tag = reader.uint32();
			switch (tag >>> 3) {
				case 1:
					if (tag !== 10) break;
					message.type = reader.string();
					continue;
				case 2:
					if (tag !== 18) break;
					message.data = WebViewInternalEventMessage.decode(reader, reader.uint32());
					continue;
			}
			if ((tag & 7) === 4 || tag === 0) break;
			reader.skipType(tag & 7);
		}
		return message;
	},
	fromJSON(object) {
		return {
			type: isSet(object.type) ? globalThis.String(object.type) : "",
			data: isSet(object.data) ? WebViewInternalEventMessage.fromJSON(object.data) : void 0
		};
	},
	toJSON(message) {
		const obj = {};
		if (message.type !== "") obj.type = message.type;
		if (message.data !== void 0) obj.data = WebViewInternalEventMessage.toJSON(message.data);
		return obj;
	},
	create(base) {
		return WebViewMessageEvent_MessageData.fromPartial(base ?? {});
	},
	fromPartial(object) {
		const message = createBaseWebViewMessageEvent_MessageData();
		message.type = object.type ?? "";
		message.data = object.data !== void 0 && object.data !== null ? WebViewInternalEventMessage.fromPartial(object.data) : void 0;
		return message;
	}
};
messageTypeRegistry.set(WebViewMessageEvent_MessageData.$type, WebViewMessageEvent_MessageData);
function isSet(value) {
	return value !== null && value !== void 0;
}
//#endregion
//#region crystal-cascade/node_modules/@devvit/shared-types/client/emit-effect.js
/** `WebViewInternalMessage.type`. */
var webViewInternalMessageType = "devvit-internal";
/**
* Emits an effect to the parent window returns immediately.
*
* @param effect - The effect to be emitted to the parent window
* @param requestId - Optional request id used to correlate request/response messages
* @description
* This function handles effects that don't require a response, and can also
* set a request id when replying to client-originated requests.
*/
var emitEffect = (effect, requestId) => {
	const message = {
		...effect,
		realtimeEffect: effect.realtime,
		id: requestId,
		scope: WebViewInternalMessageScope.CLIENT,
		type: webViewInternalMessageType
	};
	if (effect.showToast || effect.navigateToUrl) message.effect = effect;
	parent.postMessage(message, "*");
};
/**
* Emits an effect to the parent window and returns a promise that resolves with the response message.
*
* @param effect - The effect to be emitted to the parent window
* @returns A promise that resolves with the response message
*
* @description
* This function handles effects that require a response.
*/
var emitEffectWithResponse = (effect) => {
	return new Promise((resolve) => {
		const message = {
			...effect,
			realtimeEffect: effect.realtime,
			scope: WebViewInternalMessageScope.CLIENT,
			type: webViewInternalMessageType
		};
		if (effect.showForm || effect.type === EffectType.EFFECT_CAN_RUN_AS_USER) message.effect = effect;
		const id = crypto.randomUUID();
		message.id = id;
		const handleEffect = (event) => {
			if (event.data?.type === "devvit-message" && event.data?.data?.id === id) {
				resolve(WebViewInternalEventMessage.fromJSON(event.data.data));
				removeEventListener("message", handleEffect);
			}
		};
		addEventListener("message", handleEffect);
		parent.postMessage(message, "*");
	});
};
//#endregion
//#region crystal-cascade/node_modules/@devvit/shared-types/client/telemetry.js
function emitTelemetryClickEffect(event) {
	const click = TelemetryClickPayload(event.target, event.isTrusted);
	emitEffect({
		type: EffectType.EFFECT_TELEMETRY,
		telemetry: {
			event: click.event,
			click
		},
		analytics: click
	});
}
/**
* This function is expensive. It does heavy DOM work.
* @internal
*/
function TelemetryClickPayload(eventTarget, isTrusted) {
	const { definition, elemTrackId } = analyzeClickTarget(eventTarget, isTrusted);
	return {
		event: "click",
		definition,
		elemTrackId
	};
}
function analyzeClickTarget(eventTarget, isTrusted) {
	const targetElement = getTargetElement(eventTarget);
	if (!targetElement) return {
		definition: "default",
		elemTrackId: void 0
	};
	let definition = "default";
	if (isTrusted) {
		if (globalThis.window.getComputedStyle(targetElement)?.getPropertyValue("cursor") === "pointer") definition = "strict";
	}
	let elemTrackId;
	let currentElement = targetElement;
	while (currentElement) {
		if (elemTrackId === void 0) {
			const dataTrackId = currentElement.getAttribute("data-track-id");
			if (dataTrackId) elemTrackId = dataTrackId;
			else if (currentElement.id) elemTrackId = currentElement.id;
		}
		if (isTrusted && definition === "default" && elementIsStrictClickTarget(currentElement)) definition = "strict";
		if (elemTrackId !== void 0 && (!isTrusted || definition === "strict")) break;
		currentElement = currentElement.parentElement;
	}
	return {
		definition,
		elemTrackId
	};
}
function getTargetElement(eventTarget) {
	if (!eventTarget || typeof eventTarget !== "object" || !("nodeType" in eventTarget)) return;
	const node = eventTarget;
	return node.nodeType === 1 ? node : node.parentElement ?? void 0;
}
function elementIsStrictClickTarget(element) {
	return [
		"A",
		"BUTTON",
		"CANVAS",
		"INPUT",
		"SELECT",
		"TEXTAREA",
		"LABEL"
	].includes(element.tagName) || ["true", "plaintext-only"].includes(element.getAttribute("contenteditable") ?? "");
}
//#endregion
//#region crystal-cascade/node_modules/@devvit/shared-types/webbit.js
/**
* `RequestContext` token query parameter name on `DevvitPost.entrypointUrl`.
*/
var tokenParam = "token";
//#endregion
//#region crystal-cascade/node_modules/@devvit/client/effects/web-view-mode.js
var modeListeners = /* @__PURE__ */ new Set();
/**
* Represents the current web view mode state for the application.
*
* @experimental
*/
function getWebViewMode() {
	return webViewMode(devvit.webViewMode);
}
/**
* Requests expanded mode for the web view.
* This will display the web view in a larger modal presentation.
*
* @param event The gesture that triggered the request, must be a trusted event.
* @param entry The destination URI name. Eg, `'splash'` or `'default'`. Entry
*              names are the `devvit.json` `post.entrypoints` keys. Passing the
*              same entrypoint as currently loaded may cause a reload.
* @returns A promise that resolves request has been received.
* @throws When already expanded.
*
* @experimental
* @example
* ```ts
* button.addEventListener('click', async (event) => {
*   await requestExpandedMode(event);
* });
* ```
*/
function requestExpandedMode(event, entry) {
	if (devvit.webViewMode === WebViewImmersiveMode$1.IMMERSIVE_MODE) throw Error("web view is already expanded");
	emitTelemetryClickEffect(event);
	emitModeEffect(WebViewImmersiveMode$1.IMMERSIVE_MODE, event, entry);
}
/**
* Exits expanded mode for the web view.
* This will display the web view in an inline presentation.
*
* @param event The event that triggered the request, must be a trusted event.
* @returns A promise that resolves request has been received.
* @throws When already inlined.
*
* @experimental
* @example
* ```ts
* button.addEventListener('click', async (event) => {
*   await exitExpandedMode(event);
* });
* ```
*/
function exitExpandedMode(event) {
	if (devvit.webViewMode === WebViewImmersiveMode$1.INLINE_MODE) throw Error("web view is already inlined");
	emitTelemetryClickEffect(event);
	emitModeEffect(WebViewImmersiveMode$1.INLINE_MODE, event, void 0);
}
/**
* @deprecated This API is deprecated and will be removed in a future release.
* use window.addEventListener("focus", () => { }) to receive notifications
* when the web view mode changes back to inline.
* Adds a listener that is called when the web view mode changes. Initial mode
* is not reported. Web views in the process of destruction may not receive a
* mode change event.
*
* @param callback The callback to be called when the mode changes.
* @experimental
*/
function addWebViewModeListener(callback) {
	modeListeners.add(callback);
}
/**
* @deprecated This API is deprecated and will be removed in a future release.
* Removes a listener that was previously added with `addWebViewModeListener`.
*
* @param callback The callback to be removed.
* @experimental
*/
function removeWebViewModeListener(callback) {
	modeListeners.delete(callback);
}
function emitModeEffect(mode, event, entry) {
	if (!event.isTrusted || event.type !== "click") {
		console.error("Expanded mode effect ignored due to untrusted event");
		throw new Error("Untrusted event");
	}
	if (entry != null && !devvit.entrypoints[entry]) throw Error(`no entrypoint named "${entry}"; all entrypoints must appear in \`devvit.json\` \`post.entrypoints\``);
	let entryUrl;
	if (entry) {
		const url = new URL(devvit.entrypoints[entry]);
		url.searchParams.set(tokenParam, devvit.token);
		entryUrl = `${url}`;
	}
	emitEffect({
		type: EffectType.EFFECT_WEB_VIEW,
		immersiveMode: {
			entryUrl,
			immersiveMode: mode
		}
	});
}
/** @internal */
function initWebViewMode() {
	addEventListener("message", onWebViewMessage);
}
/** @internal */
function onWebViewMessage(ev) {
	if (ev.data?.type !== "devvit-message") return;
	if (!ev.data?.data?.immersiveModeEvent) return;
	const mode = getWebViewMode();
	for (const listener of modeListeners) listener(mode);
}
function webViewMode(mode) {
	switch (mode) {
		case WebViewImmersiveMode$1.IMMERSIVE_MODE: return "expanded";
		case WebViewImmersiveMode$1.INLINE_MODE:
		case WebViewImmersiveMode$1.UNRECOGNIZED:
		case WebViewImmersiveMode$1.UNSPECIFIED:
		case void 0: return "inline";
		default: throw Error(`${mode} not a WebViewImmersiveMode`);
	}
}
//#endregion
//#region crystal-cascade/node_modules/@devvit/client/clientContext.js
var context = globalThis.devvit?.context;
//#endregion
//#region crystal-cascade/node_modules/@devvit/client/effects/ad-payload.js
/** Render-time ad data for promoted posts. Returns undefined for non-ad posts. */
function getAdPayload() {
	return devvit.adPayload;
}
//#endregion
//#region crystal-cascade/node_modules/@devvit/client/effects/login-prompt.js
/**
* Opens a Reddit login/signup prompt.
*/
function showLoginPrompt() {
	emitEffect({
		type: EffectType.EFFECT_LOGIN_PROMPT,
		loginPrompt: {}
	});
}
//#endregion
//#region crystal-cascade/node_modules/@devvit/shared-types/thing-navigation.js
/**
* Resolves a `navigateTo` argument (a raw URL string or a Reddit Thing-like
* object) to the URL string the platform should navigate to.
*
* - Strings pass through unchanged.
* - Things with only `url` (no `permalink`) pass through `url` unchanged. This
*   keeps backward compatibility with callers that produce `{ url: '...' }`
*   shapes (e.g. `UiResponse.navigateTo`).
* - Things with both `url` and `permalink` get a heuristic:
*   - If `url`'s pathname equals `permalink`, navigate to `url`. This is the
*     case for Subreddit/Comment/User and self/text Posts, and using `url`
*     preserves the source origin (e.g. Snoodev mocks return snoodev-rooted
*     URLs and we want to keep the user on Snoodev).
*   - Otherwise, `url` is an asset URL such as `https://i.redd.it/foo.jpg`
*     (typical for link/image/video Posts). Fall back to `permalink` resolved
*     against `https://www.reddit.com` so we land on the comments page rather
*     than the asset.
*/
function resolveNavigationInput(thingOrUrl) {
	if (typeof thingOrUrl === "string") return thingOrUrl;
	const { url, permalink } = thingOrUrl;
	if (permalink === void 0) return url;
	try {
		if (new URL(url).pathname !== permalink) return new URL(permalink, "https://www.reddit.com").toString();
	} catch {
		return new URL(permalink, "https://www.reddit.com").toString();
	}
	return url;
}
//#endregion
//#region crystal-cascade/node_modules/@devvit/client/effects/navigate-to.js
/**
* Navigates to a URL, subreddit, post, comment, or user.
*
* @param url - The URL, subreddit, post, comment, or user to navigate to
*/
function navigateTo(url) {
	const inputUrl = resolveNavigationInput(url);
	let normalizedUrl;
	try {
		normalizedUrl = new URL(inputUrl).toString();
	} catch {
		throw new TypeError(`Invalid URL: ${inputUrl}`);
	}
	emitEffect({
		navigateToUrl: { url: normalizedUrl },
		type: 5
	});
}
//#endregion
//#region crystal-cascade/node_modules/@devvit/protos/json/reddit/devvit/app_permission/v1/app_permission.js
/**
* #app_permission.ts
*
* Code generated by ts-proto. DO NOT EDIT.
* @packageDocumentation
*/
/** This enum is used to represent the consent status of an app permission. */
var ConsentStatus;
(function(ConsentStatus) {
	/** CONSENT_STATUS_UNKNOWN - The consent status is unknown, which means it has not been set or is not applicable. */
	ConsentStatus[ConsentStatus["CONSENT_STATUS_UNKNOWN"] = 0] = "CONSENT_STATUS_UNKNOWN";
	/** REVOKED - The user has explicitly denied consent for the app permissions. */
	ConsentStatus[ConsentStatus["REVOKED"] = 1] = "REVOKED";
	/** GRANTED - The user has granted consent for the app permissions. */
	ConsentStatus[ConsentStatus["GRANTED"] = 2] = "GRANTED";
	ConsentStatus[ConsentStatus["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(ConsentStatus || (ConsentStatus = {}));
/** This enum is used to represent the scopes of permissions that can be granted to an app. */
var Scope;
(function(Scope) {
	/** SCOPE_UNKNOWN - The scope is unknown, which means it has not been set or is not applicable. */
	Scope[Scope["SCOPE_UNKNOWN"] = 0] = "SCOPE_UNKNOWN";
	/** SUBMIT_POST - Allows the app to submit posts on behalf of the user. */
	Scope[Scope["SUBMIT_POST"] = 1] = "SUBMIT_POST";
	/** SUBMIT_COMMENT - Allows the app to submit comments on behalf of the user. */
	Scope[Scope["SUBMIT_COMMENT"] = 2] = "SUBMIT_COMMENT";
	/** SUBSCRIBE_TO_SUBREDDIT - Allows the app to subscribe the user to a subreddit. */
	Scope[Scope["SUBSCRIBE_TO_SUBREDDIT"] = 3] = "SUBSCRIBE_TO_SUBREDDIT";
	Scope[Scope["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(Scope || (Scope = {}));
//#endregion
//#region crystal-cascade/node_modules/@devvit/protos/json/devvit/ui/effects/web_view/v1alpha/context.js
/**
* #context.ts
*
* Code generated by ts-proto. DO NOT EDIT.
* @packageDocumentation
*/
/** The user client hosting the web view. */
var Client;
(function(Client) {
	Client[Client["CLIENT_UNSPECIFIED"] = 0] = "CLIENT_UNSPECIFIED";
	Client[Client["ANDROID"] = 1] = "ANDROID";
	Client[Client["IOS"] = 2] = "IOS";
	Client[Client["SHREDDIT"] = 3] = "SHREDDIT";
	Client[Client["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(Client || (Client = {}));
/**
* Inline height of post.
*
* @deprecated
*/
var Height;
(function(Height) {
	Height[Height["HEIGHT_UNSPECIFIED"] = 0] = "HEIGHT_UNSPECIFIED";
	Height[Height["REGULAR"] = 1] = "REGULAR";
	Height[Height["TALL"] = 2] = "TALL";
	Height[Height["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(Height || (Height = {}));
//#endregion
//#region crystal-cascade/node_modules/@devvit/shared-types/tid.js
/**
* - **C**omment (T1).
* - **A**ccount (T2).
* - **L**ink (T3).
* - **M**essage (T4).
* - **S**ubreddit (T5).
* - **T**rophies (T6).
*/
var T_PREFIX;
(function(T_PREFIX) {
	T_PREFIX["COMMENT"] = "t1_";
	T_PREFIX["ACCOUNT"] = "t2_";
	T_PREFIX["LINK"] = "t3_";
	T_PREFIX["MESSAGE"] = "t4_";
	T_PREFIX["SUBREDDIT"] = "t5_";
	T_PREFIX["AWARD"] = "t6_";
})(T_PREFIX || (T_PREFIX = {}));
//#endregion
//#region crystal-cascade/node_modules/jwt-decode/build/esm/index.js
var InvalidTokenError = class extends Error {};
InvalidTokenError.prototype.name = "InvalidTokenError";
//#endregion
//#region crystal-cascade/node_modules/@devvit/web-view-scripts/devvit-global.js
function updateConsentStatus(consentStatus) {
	if (!globalThis.devvit.appPermissionState) return;
	globalThis.devvit.appPermissionState.consentStatus = consentStatus;
}
//#endregion
//#region crystal-cascade/node_modules/@devvit/client/effects/run-as-user.js
/**
* This method is used to check if the app has been granted all the requested scopes.
*
* @experimental
* @param event The event that triggered the request, must be a trusted event.
* @returns true if the app has been granted all the requested scopes, false otherwise.
*/
var canRunAsUser = async (event) => {
	if (!event.isTrusted) {
		console.error("CanRunAsUser effect ignored due to untrusted event");
		throw new Error("Untrusted event");
	}
	const appPermissionState = devvit.appPermissionState;
	if (!appPermissionState) return true;
	if (appPermissionState.requestedScopes.length === 0) return false;
	switch (appPermissionState.consentStatus) {
		case ConsentStatus.REVOKED: return false;
		case ConsentStatus.GRANTED:
			if (appPermissionState.requestedScopes.every((scope) => appPermissionState.grantedScopes.includes(scope))) return true;
			break;
		case ConsentStatus.CONSENT_STATUS_UNKNOWN:
		case ConsentStatus.UNRECOGNIZED: break;
		default:
			appPermissionState.consentStatus;
			break;
	}
	const response = await emitEffectWithResponse({
		canRunAsUser: {
			postId: devvit.context.postId,
			appSlug: devvit.context.appName,
			subredditId: devvit.context.subredditId
		},
		type: 11
	});
	if (response?.consentStatus) updateConsentStatus(response.consentStatus.consentStatus);
	return response?.consentStatus?.consentStatus === ConsentStatus.GRANTED;
};
//#endregion
//#region crystal-cascade/node_modules/@devvit/shared-types/client/share.js
/** Max length of ShareParam.userData. */
var maxShareParamUserDataChars = 1024;
//#endregion
//#region crystal-cascade/node_modules/@devvit/shared-types/constants.js
/** When an app's icon is present in the media assets list, it'll be with this special name. */
var ICON_FILE_PATH = "$devvit_icon.png";
//#endregion
//#region crystal-cascade/node_modules/@devvit/client/effects/share.js
/** Show the native share sheet or copy to clipboard depending on device. */
async function showShareSheet(opts) {
	if (opts.data && opts.data.length > 1024) throw Error(`data must be <= ${maxShareParamUserDataChars} characters but was ${opts.data.length} characters`);
	const iconURL = `${new URL(ICON_FILE_PATH, location.origin)}`;
	let iconRsp;
	try {
		iconRsp = await fetch(iconURL, { method: "HEAD" });
	} catch {}
	const url = `https://reddit.com/r/_/comments/${(opts.post ?? devvit.context.postId).replace("t3_", "")}`;
	emitEffect({
		type: EffectType.EFFECT_WEB_VIEW,
		share: {
			appIconUri: iconRsp?.ok ? iconURL : void 0,
			userData: opts.data,
			text: opts.text,
			title: opts.title,
			url
		}
	});
}
function getShareData() {
	return devvit.share?.userData;
}
//#endregion
//#region crystal-cascade/node_modules/@devvit/client/effects/helpers/assert-valid-form-fields.js
/**
* Make sure that the form fields have unique names.
*
* This is a carbon copy of the assertValidFormFields function in the public-api package
* We copy it here so that @devvit/client does not need to depend on public-api
* Any changes to this function should be reflected in the public-api version
*/
function assertValidFormFields(fields, seenNames = /* @__PURE__ */ new Set()) {
	for (const field of fields) {
		if (field.type === "group") {
			assertValidFormFields(field.fields, seenNames);
			continue;
		}
		const fieldName = field.name;
		if (seenNames.has(fieldName)) throw new Error(`Duplicate field name: ${fieldName}`);
		seenNames.add(fieldName);
	}
	assertAppSecretsOnly(fields);
}
function assertAppSecretsOnly(fields) {
	for (const field of fields) if (field.type === "string" && field.isSecret && field.scope !== "app") throw `Invalid setting: only app settings can be secrets. Add "scope: SettingScope.App" to field "${field.name}"`;
}
//#endregion
//#region crystal-cascade/node_modules/@devvit/protos/json/devvit/ui/form_builder/v1alpha/type.js
/**
* #type.ts
*
* Code generated by ts-proto. DO NOT EDIT.
* @packageDocumentation
*/
var FormFieldType;
(function(FormFieldType) {
	/** STRING - Single-line string */
	FormFieldType[FormFieldType["STRING"] = 0] = "STRING";
	/** PARAGRAPH - Multi-line string */
	FormFieldType[FormFieldType["PARAGRAPH"] = 1] = "PARAGRAPH";
	/** NUMBER - Number input */
	FormFieldType[FormFieldType["NUMBER"] = 2] = "NUMBER";
	/** BOOLEAN - Checkbox, switch */
	FormFieldType[FormFieldType["BOOLEAN"] = 3] = "BOOLEAN";
	/** LIST - Collect multiple entries of a single type */
	FormFieldType[FormFieldType["LIST"] = 4] = "LIST";
	/** SELECTION - Provide a list of options for the user to choose from */
	FormFieldType[FormFieldType["SELECTION"] = 5] = "SELECTION";
	/** GROUP - Create a labeled subsection of the form with nested fields */
	FormFieldType[FormFieldType["GROUP"] = 6] = "GROUP";
	/** IMAGE - Image upload - value is a URL for the uploaded image */
	FormFieldType[FormFieldType["IMAGE"] = 7] = "IMAGE";
	FormFieldType[FormFieldType["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(FormFieldType || (FormFieldType = {}));
//#endregion
//#region crystal-cascade/node_modules/@devvit/protos/json/devvit/events/v1alpha/events.js
/**
* #events.ts
*
* Code generated by ts-proto. DO NOT EDIT.
* @packageDocumentation
*/
var EventSource;
(function(EventSource) {
	EventSource[EventSource["UNKNOWN_EVENT_SOURCE"] = 0] = "UNKNOWN_EVENT_SOURCE";
	EventSource[EventSource["USER"] = 1] = "USER";
	EventSource[EventSource["ADMIN"] = 2] = "ADMIN";
	EventSource[EventSource["MODERATOR"] = 3] = "MODERATOR";
	EventSource[EventSource["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(EventSource || (EventSource = {}));
var DeletionReason;
(function(DeletionReason) {
	DeletionReason[DeletionReason["UNSPECIFIED_DELETION_REASON"] = 0] = "UNSPECIFIED_DELETION_REASON";
	DeletionReason[DeletionReason["SPAM"] = 1] = "SPAM";
	DeletionReason[DeletionReason["LEGAL"] = 2] = "LEGAL";
	DeletionReason[DeletionReason["OTHER"] = 3] = "OTHER";
	DeletionReason[DeletionReason["UNKNOWN"] = 4] = "UNKNOWN";
	DeletionReason[DeletionReason["EXPLICIT_CONTENT"] = 5] = "EXPLICIT_CONTENT";
	DeletionReason[DeletionReason["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(DeletionReason || (DeletionReason = {}));
//#endregion
//#region crystal-cascade/node_modules/@devvit/protos/json/devvit/reddit/v2alpha/postv2.js
/**
* #postv2.ts
*
* Code generated by ts-proto. DO NOT EDIT.
* @packageDocumentation
*/
var CrowdControlLevel;
(function(CrowdControlLevel) {
	CrowdControlLevel[CrowdControlLevel["OFF"] = 0] = "OFF";
	CrowdControlLevel[CrowdControlLevel["LENIENT"] = 1] = "LENIENT";
	CrowdControlLevel[CrowdControlLevel["MEDIUM"] = 2] = "MEDIUM";
	CrowdControlLevel[CrowdControlLevel["STRICT"] = 3] = "STRICT";
	CrowdControlLevel[CrowdControlLevel["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(CrowdControlLevel || (CrowdControlLevel = {}));
var DistinguishType;
(function(DistinguishType) {
	DistinguishType[DistinguishType["NULL_VALUE"] = 0] = "NULL_VALUE";
	DistinguishType[DistinguishType["ADMIN"] = 1] = "ADMIN";
	DistinguishType[DistinguishType["GOLD"] = 2] = "GOLD";
	DistinguishType[DistinguishType["GOLD_AUTO"] = 3] = "GOLD_AUTO";
	DistinguishType[DistinguishType["YES"] = 4] = "YES";
	DistinguishType[DistinguishType["SPECIAL"] = 5] = "SPECIAL";
	DistinguishType[DistinguishType["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(DistinguishType || (DistinguishType = {}));
//#endregion
//#region crystal-cascade/node_modules/@devvit/protos/json/devvit/reddit/v2alpha/subredditv2.js
/**
* #subredditv2.ts
*
* Code generated by ts-proto. DO NOT EDIT.
* @packageDocumentation
*/
var SubredditType;
(function(SubredditType) {
	SubredditType[SubredditType["ARCHIVED"] = 0] = "ARCHIVED";
	SubredditType[SubredditType["EMPLOYEES_ONLY"] = 1] = "EMPLOYEES_ONLY";
	SubredditType[SubredditType["GOLD_ONLY"] = 2] = "GOLD_ONLY";
	SubredditType[SubredditType["GOLD_RESTRICTED"] = 3] = "GOLD_RESTRICTED";
	SubredditType[SubredditType["PRIVATE"] = 4] = "PRIVATE";
	SubredditType[SubredditType["PUBLIC"] = 5] = "PUBLIC";
	SubredditType[SubredditType["RESTRICTED"] = 6] = "RESTRICTED";
	SubredditType[SubredditType["USER"] = 7] = "USER";
	SubredditType[SubredditType["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(SubredditType || (SubredditType = {}));
/** used with data-api */
var SubredditRating;
(function(SubredditRating) {
	SubredditRating[SubredditRating["UNKNOWN_SUBREDDIT_RATING"] = 0] = "UNKNOWN_SUBREDDIT_RATING";
	/** E - everyone */
	SubredditRating[SubredditRating["E"] = 1] = "E";
	/** M1 - mature 1 */
	SubredditRating[SubredditRating["M1"] = 2] = "M1";
	/** M2 - mature 2 */
	SubredditRating[SubredditRating["M2"] = 3] = "M2";
	/** D - high-risk drug use */
	SubredditRating[SubredditRating["D"] = 4] = "D";
	/** V - violence & gore */
	SubredditRating[SubredditRating["V"] = 5] = "V";
	/** X - sexually explicit */
	SubredditRating[SubredditRating["X"] = 6] = "X";
	SubredditRating[SubredditRating["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(SubredditRating || (SubredditRating = {}));
/** creating new enums so that we can have unspecified and unset values */
var SubredditTypeV2;
(function(SubredditTypeV2) {
	SubredditTypeV2[SubredditTypeV2["SUBREDDIT_TYPE_UNSPECIFIED"] = 0] = "SUBREDDIT_TYPE_UNSPECIFIED";
	SubredditTypeV2[SubredditTypeV2["SUBREDDIT_TYPE_UNKNOWN"] = 1] = "SUBREDDIT_TYPE_UNKNOWN";
	SubredditTypeV2[SubredditTypeV2["SUBREDDIT_TYPE_ARCHIVED"] = 2] = "SUBREDDIT_TYPE_ARCHIVED";
	SubredditTypeV2[SubredditTypeV2["SUBREDDIT_TYPE_EMPLOYEES_ONLY"] = 3] = "SUBREDDIT_TYPE_EMPLOYEES_ONLY";
	SubredditTypeV2[SubredditTypeV2["SUBREDDIT_TYPE_GOLD_ONLY"] = 4] = "SUBREDDIT_TYPE_GOLD_ONLY";
	SubredditTypeV2[SubredditTypeV2["SUBREDDIT_TYPE_GOLD_RESTRICTED"] = 5] = "SUBREDDIT_TYPE_GOLD_RESTRICTED";
	SubredditTypeV2[SubredditTypeV2["SUBREDDIT_TYPE_PRIVATE"] = 6] = "SUBREDDIT_TYPE_PRIVATE";
	SubredditTypeV2[SubredditTypeV2["SUBREDDIT_TYPE_PUBLIC"] = 7] = "SUBREDDIT_TYPE_PUBLIC";
	SubredditTypeV2[SubredditTypeV2["SUBREDDIT_TYPE_RESTRICTED"] = 8] = "SUBREDDIT_TYPE_RESTRICTED";
	SubredditTypeV2[SubredditTypeV2["SUBREDDIT_TYPE_USER"] = 9] = "SUBREDDIT_TYPE_USER";
	SubredditTypeV2[SubredditTypeV2["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(SubredditTypeV2 || (SubredditTypeV2 = {}));
var SubredditRatingV2;
(function(SubredditRatingV2) {
	SubredditRatingV2[SubredditRatingV2["SUBREDDIT_RATING_UNSPECIFIED"] = 0] = "SUBREDDIT_RATING_UNSPECIFIED";
	SubredditRatingV2[SubredditRatingV2["SUBREDDIT_RATING_UNKNOWN"] = 1] = "SUBREDDIT_RATING_UNKNOWN";
	/** SUBREDDIT_RATING_E - everyone */
	SubredditRatingV2[SubredditRatingV2["SUBREDDIT_RATING_E"] = 2] = "SUBREDDIT_RATING_E";
	/** SUBREDDIT_RATING_M1 - mature 1 */
	SubredditRatingV2[SubredditRatingV2["SUBREDDIT_RATING_M1"] = 3] = "SUBREDDIT_RATING_M1";
	/** SUBREDDIT_RATING_M2 - mature 2 */
	SubredditRatingV2[SubredditRatingV2["SUBREDDIT_RATING_M2"] = 4] = "SUBREDDIT_RATING_M2";
	/** SUBREDDIT_RATING_D - high-risk drug use */
	SubredditRatingV2[SubredditRatingV2["SUBREDDIT_RATING_D"] = 5] = "SUBREDDIT_RATING_D";
	/** SUBREDDIT_RATING_V - violence & gore */
	SubredditRatingV2[SubredditRatingV2["SUBREDDIT_RATING_V"] = 6] = "SUBREDDIT_RATING_V";
	/** SUBREDDIT_RATING_X - sexually explicit */
	SubredditRatingV2[SubredditRatingV2["SUBREDDIT_RATING_X"] = 7] = "SUBREDDIT_RATING_X";
	SubredditRatingV2[SubredditRatingV2["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(SubredditRatingV2 || (SubredditRatingV2 = {}));
//#endregion
//#region crystal-cascade/node_modules/@devvit/shared-types/shared/form.js
var SettingScope;
(function(SettingScope) {
	SettingScope["Installation"] = "installation";
	SettingScope["App"] = "app";
})(SettingScope || (SettingScope = {}));
//#endregion
//#region crystal-cascade/node_modules/@devvit/client/effects/helpers/get-form-values.js
function flattenFormFieldValue(value) {
	switch (value.fieldType) {
		case FormFieldType.STRING: return value.stringValue;
		case FormFieldType.IMAGE: return value.stringValue;
		case FormFieldType.PARAGRAPH: return value.stringValue;
		case FormFieldType.NUMBER: return value.numberValue;
		case FormFieldType.BOOLEAN: return value.boolValue;
		case FormFieldType.SELECTION: return value.selectionValue?.values ?? [];
		default: return;
	}
}
function getFormValues(results) {
	return Object.keys(results).reduce((acc, key) => {
		const val = flattenFormFieldValue(results[key]);
		if (val !== void 0) acc[key] = val;
		return acc;
	}, {});
}
//#endregion
//#region crystal-cascade/node_modules/@devvit/client/effects/helpers/transform-form.js
function transformFormFields(fields) {
	return fields.map((field) => {
		switch (field.type) {
			case "string": return transformStringField(field);
			case "image": return transformImageField(field);
			case "paragraph": return transformParagraphField(field);
			case "number": return transformNumberField(field);
			case "select": return transformSelectField(field);
			case "boolean": return transformBooleanField(field);
			case "group": return transformGroupField(field);
			default: throw new Error("Unknown field type.");
		}
	});
}
function transformStringField(field) {
	return {
		defaultValue: {
			fieldType: FormFieldType.STRING,
			stringValue: field.defaultValue
		},
		disabled: field.disabled,
		fieldConfig: { stringConfig: { placeholder: field.placeholder } },
		fieldId: field.name,
		fieldType: FormFieldType.STRING,
		helpText: field.helpText,
		label: field.label,
		required: field.required,
		isSecret: field.isSecret
	};
}
function transformImageField(field) {
	return {
		disabled: field.disabled,
		fieldId: field.name,
		fieldType: FormFieldType.IMAGE,
		helpText: field.helpText,
		label: field.label,
		required: field.required
	};
}
function transformParagraphField(field) {
	return {
		defaultValue: {
			fieldType: FormFieldType.PARAGRAPH,
			stringValue: field.defaultValue
		},
		disabled: field.disabled,
		fieldConfig: { paragraphConfig: {
			lineHeight: field.lineHeight,
			placeholder: field.placeholder
		} },
		fieldId: field.name,
		fieldType: FormFieldType.PARAGRAPH,
		helpText: field.helpText,
		label: field.label,
		required: field.required
	};
}
function transformNumberField(field) {
	return {
		defaultValue: {
			fieldType: FormFieldType.NUMBER,
			numberValue: field.defaultValue
		},
		disabled: field.disabled,
		fieldConfig: { numberConfig: {} },
		fieldId: field.name,
		fieldType: FormFieldType.NUMBER,
		helpText: field.helpText,
		label: field.label,
		required: field.required
	};
}
function transformSelectField(field) {
	return {
		defaultValue: {
			fieldType: FormFieldType.SELECTION,
			selectionValue: { values: field.defaultValue ?? [] }
		},
		disabled: field.disabled,
		fieldConfig: { selectionConfig: {
			choices: field.options,
			multiSelect: field.multiSelect
		} },
		fieldId: field.name,
		fieldType: FormFieldType.SELECTION,
		helpText: field.helpText,
		label: field.label,
		required: field.required
	};
}
function transformBooleanField(field) {
	return {
		defaultValue: {
			fieldType: FormFieldType.BOOLEAN,
			boolValue: field.defaultValue
		},
		disabled: field.disabled,
		fieldId: field.name,
		fieldType: FormFieldType.BOOLEAN,
		helpText: field.helpText,
		label: field.label
	};
}
function transformGroupField(field) {
	return {
		fieldId: "",
		fieldType: FormFieldType.GROUP,
		fieldConfig: { groupConfig: { fields: transformFormFields(field.fields) } },
		label: field.label,
		helpText: field.helpText
	};
}
//#endregion
//#region crystal-cascade/node_modules/@devvit/client/effects/show-form.js
var _formKey = 1;
var getNextFormKey = () => {
	_formKey++;
	return `form.${_formKey}`;
};
/**
* Opens a form in a modal.
* Returns a promise that resolves with the form submission results.
* The form can be submitted or canceled by the user.
*
* @param formDefinition - The form configuration
* @returns A promise that resolves to either:
*   - An object with `action: FormAction.SUBMITTED` and the submitted form values
*   - An object with `action: FormAction.CANCELED` if the user canceled the form
* @throws Will throw if the form fields are invalid
*/
var showForm = async (formDefinition) => {
	const form = {
		fields: [],
		id: getNextFormKey(),
		title: formDefinition.title,
		acceptLabel: formDefinition.acceptLabel,
		cancelLabel: formDefinition.cancelLabel,
		shortDescription: formDefinition.description
	};
	assertValidFormFields(formDefinition.fields);
	form.fields = transformFormFields(formDefinition.fields);
	const response = await emitEffectWithResponse({
		showForm: { form },
		type: 3
	});
	if (!response || !response.formSubmitted) return { action: "CANCELED" };
	return {
		action: "SUBMITTED",
		values: getFormValues(response.formSubmitted.results)
	};
};
//#endregion
//#region crystal-cascade/node_modules/@devvit/client/effects/show-toast.js
function showToast(textOrToast) {
	let toast;
	if (textOrToast instanceof Object) toast = {
		text: textOrToast.text,
		appearance: textOrToast.appearance === "success" ? 1 : 0
	};
	else toast = { text: textOrToast };
	emitEffect({
		showToast: { toast },
		type: 4
	});
}
//#endregion
//#region crystal-cascade/node_modules/@devvit/client/index.js
initWebViewMode();
//#endregion
//#region crystal-cascade/node_modules/@devvit/payments/client/purchase.js
/**
* Initiates a purchase for one or more products.
*
* @param skuOrSkus - The SKU, or an array of SKUs, to purchase
* @param metadata - Optional, custom metadata attached to this purchase
* @experimental - This is not finalized yet, may not work, and may change.
*/
async function purchase(skuOrSkus, metadata) {
	const resp = await emitEffectWithResponse({
		createOrder: {
			id: crypto.randomUUID(),
			skus: typeof skuOrSkus === "string" ? [skuOrSkus] : skuOrSkus,
			metadata: metadata ?? {}
		},
		type: EffectType.EFFECT_CREATE_ORDER
	});
	if (!resp) throw new Error(`Purchase didn't resolve correctly, got no response!`);
	const result = resp.orderResult;
	if (!result) throw new Error(`Purchase didn't resolve correctly, had no order result in the response!`);
	return result;
}
//#endregion
//#region crystal-cascade/node_modules/@devvit/protos/json/devvit/ui/effect_types/v1alpha/create_order.js
/**
* #create_order.ts
*
* Code generated by ts-proto. DO NOT EDIT.
* @packageDocumentation
*/
var OrderResultStatus;
(function(OrderResultStatus) {
	OrderResultStatus[OrderResultStatus["STATUS_CANCELLED"] = 0] = "STATUS_CANCELLED";
	OrderResultStatus[OrderResultStatus["STATUS_SUCCESS"] = 1] = "STATUS_SUCCESS";
	OrderResultStatus[OrderResultStatus["STATUS_ERROR"] = 2] = "STATUS_ERROR";
	OrderResultStatus[OrderResultStatus["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(OrderResultStatus || (OrderResultStatus = {}));
//#endregion
//#region crystal-cascade/node_modules/@devvit/protos/json/devvit/ui/effects/v1alpha/realtime_subscriptions.js
/**
* #realtime_subscriptions.ts
*
* Code generated by ts-proto. DO NOT EDIT.
* @packageDocumentation
*/
var RealtimeSubscriptionStatus;
(function(RealtimeSubscriptionStatus) {
	RealtimeSubscriptionStatus[RealtimeSubscriptionStatus["REALTIME_SUBSCRIBED"] = 0] = "REALTIME_SUBSCRIBED";
	RealtimeSubscriptionStatus[RealtimeSubscriptionStatus["REALTIME_UNSUBSCRIBED"] = 1] = "REALTIME_UNSUBSCRIBED";
	RealtimeSubscriptionStatus[RealtimeSubscriptionStatus["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(RealtimeSubscriptionStatus || (RealtimeSubscriptionStatus = {}));
//#endregion
//#region crystal-cascade/node_modules/@devvit/realtime/client/realtime.js
var connectionsByChannel = /* @__PURE__ */ new Map();
/**
* Connects to a realtime channel for receiving messages.
*
* @param opts - Connection options including channel name and callbacks
* @returns A Connection object with a disconnect method
*
* @example
*
* ```ts
* import {connectRealtime, context} from '@devvit/web/client'
* import type {T2} from '@devvit/web/shared'
*
* type RealtimeMessage = {x: number, y: number, t2: T2}
*
* connectRealtime<RealtimeMessage>({
*   channel: context.postId,
*   onConnect() {
*     console.log(`${context.userId} connected`)
*   },
*   onDisconnect() {
*     console.log(`${context.userId} disconnected`)
*   },
*   onMessage(msg) {
*     // Filter out current user across all sessions.
*     if (msg.t2 === context.userId) return
*
*     console.log(`user ${msg.t2} at (${msg.x}, ${msg.y})`)
*   }
* })
* ```
*/
var connectRealtime = (opts) => {
	if (!opts.channel || /[^a-zA-Z0-9_]/.test(opts.channel)) throw Error(`invalid channel name "${opts.channel}"; channels may only contain letters, numbers, and underscores`);
	if (connectionsByChannel.has(opts.channel)) return connectionsByChannel.get(opts.channel);
	const connection = new Connection(opts);
	connectionsByChannel.set(opts.channel, connection);
	addEventListener("message", connection.onMessage);
	emitConnectionsEffect();
	return connection;
};
function disconnectRealtime(channel) {
	const connection = connectionsByChannel.get(channel);
	if (!connection) return;
	connectionsByChannel.delete(channel);
	removeEventListener("message", connection.onMessage);
	if (connection.connected) connection.opts.onDisconnect?.(channel);
	connection.connected = false;
	emitConnectionsEffect();
}
/** True if the channel socket is connected. */
function isRealtimeConnected(channel) {
	return !!connectionsByChannel.get(channel)?.connected;
}
/** Emits open connections which may cause subscribe / unsubscribe. */
function emitConnectionsEffect() {
	emitEffect({
		realtime: { subscriptionIds: [...connectionsByChannel.keys()] },
		type: EffectType.EFFECT_REALTIME_SUB
	});
}
var Connection = class {
	constructor(opts) {
		/** @internal */
		this.connected = false;
		/** @internal */
		this.onMessage = (ev) => {
			const { type, data } = ev.data;
			if (type !== "devvit-message") return;
			if (!data?.realtimeEvent) return;
			const { status, event } = data.realtimeEvent;
			if (!event?.channel.endsWith(this.opts.channel)) return;
			event.channel = this.opts.channel;
			if (status === RealtimeSubscriptionStatus.REALTIME_SUBSCRIBED) {
				this.connected = true;
				this.opts.onConnect?.(this.opts.channel);
			} else if (status === RealtimeSubscriptionStatus.REALTIME_UNSUBSCRIBED) {
				this.connected = false;
				this.opts.onDisconnect?.(this.opts.channel);
			} else if (event.data) this.opts.onMessage(event.data.msg);
			else console.error("[realtime] Received event without data:", {
				channel: this.opts.channel,
				event
			});
		};
		this.opts = opts;
	}
	/**
	* Disconnects from the realtime channel.
	*
	* @deprecated Use `disconnectRealtime()`.
	*/
	async disconnect() {
		await disconnectRealtime(this.opts.channel);
	}
};
//#endregion
export { OrderResultStatus, addWebViewModeListener, canRunAsUser, connectRealtime, context, disconnectRealtime, exitExpandedMode, getAdPayload, getShareData, getWebViewMode, initWebViewMode, isRealtimeConnected, navigateTo, onWebViewMessage, purchase, removeWebViewModeListener, requestExpandedMode, showForm, showLoginPrompt, showShareSheet, showToast };
